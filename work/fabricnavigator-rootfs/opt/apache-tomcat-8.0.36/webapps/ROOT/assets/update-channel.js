(function(){'use strict';function init(){if(location.pathname!='/admin/update.jsp')return;var schedule=document.querySelector('.schedule-form'),csrf=(document.querySelector('input[name="csrfToken"]')||{}).value;if(!schedule||!csrf)return;var label=document.createElement('label');label.className='fn-update-channel';label.innerHTML='<span>Update channel</span><select><option value="stable">Stable</option><option value="beta">Beta (GitHub pre-release)</option></select><small></small>';schedule.insertBefore(label,schedule.querySelector('button'));var select=label.querySelector('select'),status=label.querySelector('small');function saveChannel(){var body=new URLSearchParams();body.set('csrfToken',csrf);body.set('channel',select.value);status.textContent='…';return fetch('/admin/update-channel.jsp',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:body.toString()}).then(function(r){if(!r.ok)throw Error('HTTP '+r.status);return r.json();}).then(function(){status.textContent=document.documentElement.lang==='de'?' Gespeichert':' Saved';});}fetch('/admin/update-channel.jsp',{credentials:'same-origin',cache:'no-store'}).then(function(r){return r.json();}).then(function(p){select.value=p.channel==='beta'?'beta':'stable';});select.onchange=function(){saveChannel().catch(function(e){status.textContent=' '+e.message;});};schedule.addEventListener('submit',function(event){event.preventDefault();var body=new URLSearchParams(new FormData(schedule));fetch('/admin/update.jsp',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:body.toString()}).then(saveChannel).then(function(){location.reload();}).catch(function(e){status.textContent=' '+e.message;});});}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})();
