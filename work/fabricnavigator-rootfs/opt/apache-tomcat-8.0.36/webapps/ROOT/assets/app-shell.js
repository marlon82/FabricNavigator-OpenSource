(function(){'use strict';if(window.__fabricNavigatorShellV170)return;window.__fabricNavigatorShellV170=true;
var LANG_KEY='edmLanguage',THEME_KEY='edmTheme',SSH_STATE_KEY='fnSshState',TOOLBAR_KEY='fnSshToolbar',BUILD_VERSION='26.08.10.181',PREFERENCE_KEYS=[LANG_KEY,THEME_KEY,SSH_STATE_KEY,TOOLBAR_KEY,'fnSshFontSize','fnConnectionsCollapsed','edmSavedTopologiesV1','edmDefaultTopologyV1','fnNodeLabel','fnAdminTab','fnAdminCredentialTab','fnAdminDesignTab','fnProfileDesignTab','fnExtremeSwitchImages','fnTopologyGroupsV1'],preferenceCsrf='',preferenceReady=false,preferenceTimer=null;
function saved(key){try{return localStorage.getItem(key);}catch(e){return null;}}
function preferenceSnapshot(){var result={};PREFERENCE_KEYS.forEach(function(key){var value=saved(key);if(value!==null)result[key]=value;});return result;}
function uploadPreferences(){if(!preferenceReady||!preferenceCsrf)return Promise.resolve();var body='csrfToken='+encodeURIComponent(preferenceCsrf)+'&settings='+encodeURIComponent(JSON.stringify(preferenceSnapshot()));return fetch('/user-preferences.jsp',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'},body:body}).catch(function(){});}
function schedulePreferenceUpload(){if(!preferenceReady)return;clearTimeout(preferenceTimer);preferenceTimer=setTimeout(uploadPreferences,350);}
function store(key,value){try{localStorage.setItem(key,value);if(PREFERENCE_KEYS.indexOf(key)>=0)schedulePreferenceUpload();}catch(e){}}
function removeStored(key){try{localStorage.removeItem(key);if(PREFERENCE_KEYS.indexOf(key)>=0)schedulePreferenceUpload();}catch(e){}}
window.FabricNavigatorPreferences={set:store,remove:removeStored,sync:schedulePreferenceUpload};
function loadUserPreferences(){fetch('/user-preferences.jsp',{credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(response){if(!response.ok)throw new Error();return response.json();}).then(function(payload){preferenceCsrf=String(payload.csrf||'');var remote=payload.settings&&typeof payload.settings==='object'&&!Array.isArray(payload.settings)?payload.settings:{},changed=false;if(payload.exists){PREFERENCE_KEYS.forEach(function(key){if(Object.prototype.hasOwnProperty.call(remote,key)){var value=String(remote[key]);if(saved(key)!==value){try{localStorage.setItem(key,value);changed=true;}catch(e){}}}else if(saved(key)!==null){try{localStorage.removeItem(key);changed=true;}catch(e){}}});}preferenceReady=true;if(!payload.exists)uploadPreferences();if(changed&&!sessionSaved('fn-preferences-reloaded')){sessionStore('fn-preferences-reloaded','1');location.reload();}else sessionStore('fn-preferences-reloaded',null);}).catch(function(){});}
loadUserPreferences();
function sessionSaved(key){try{var value=sessionStorage.getItem(key);return value!==null?value:(key===SSH_STATE_KEY?saved(key):null);}catch(e){return key===SSH_STATE_KEY?saved(key):null;}}
function sessionStore(key,value){try{if(value===null)sessionStorage.removeItem(key);else sessionStorage.setItem(key,value);}catch(e){}if(key===SSH_STATE_KEY){if(value===null)removeStored(key);else store(key,value);}}
var lang=saved(LANG_KEY);if(lang!=='de'&&lang!=='en'){lang=(navigator.language||'en').toLowerCase().indexOf('de')===0?'de':'en';store(LANG_KEY,lang);if(!sessionStorage.getItem('fn-language-ready')){sessionStorage.setItem('fn-language-ready','1');location.reload();return;}}
var theme=saved(THEME_KEY)||((window.matchMedia&&matchMedia('(prefers-color-scheme: dark)').matches)?'dark':'light');document.documentElement.setAttribute('data-theme',theme);document.documentElement.lang=lang;if(location.pathname.indexOf('/topology/')===0)document.documentElement.classList.add('fn-topology-page');
if(!document.querySelector('link[href^="/assets/app-shell.css"]')){var css=document.createElement('link');css.rel='stylesheet';css.href='/assets/app-shell.css?v=20260826-157';document.head.appendChild(css);}
var tr={de:{theme:'Hell/Dunkel',language:'English',system:'System',close:'Schließen',minimize:'Minimieren',restore:'Wiederherstellen',ssh:'SSH-Sitzungen',name:'Name',ip:'IP',mlt:'MLT',links:'Links',isis:'IS-IS'},en:{theme:'Light/Dark',language:'Deutsch',system:'System',close:'Close',minimize:'Minimize',restore:'Restore',ssh:'SSH sessions',name:'Name',ip:'IP',mlt:'MLT',links:'links',isis:'IS-IS'}};
function text(k){return tr[lang][k]||k;}
function button(label,cls){var b=document.createElement('button');b.type='button';b.className='fn-shell-button '+(cls||'');b.textContent=label;return b;}
var ICON_SUN='<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.66 6.34l1.41-1.41"></path></svg>',ICON_MOON='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20.5 14.2A8.5 8.5 0 0 1 9.8 3.5 8.5 8.5 0 1 0 20.5 14.2Z"></path></svg>',ICON_LANGUAGE='<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><path d="M3 12h18M12 3a14 14 0 0 1 0 18M12 3a14 14 0 0 0 0 18"></path></svg>';
function setIconButton(control,type){if(!control)return;var dark=document.documentElement.getAttribute('data-theme')==='dark',currentLang=(document.documentElement.lang||saved(LANG_KEY)||lang).toLowerCase().indexOf('de')===0?'de':'en',label;if(type==='theme'){label=dark?(currentLang==='de'?'Helles Farbschema':'Use light theme'):(currentLang==='de'?'Dunkles Farbschema':'Use dark theme');control.innerHTML=dark?ICON_SUN:ICON_MOON;}else{label=currentLang==='de'?'Switch to English':'Auf Deutsch wechseln';control.innerHTML=ICON_LANGUAGE;}control.classList.add('fn-icon-button');control.title=label;control.setAttribute('aria-label',label);}
function refreshShellIcons(){document.querySelectorAll('.fn-theme-toggle,#theme-toggle').forEach(function(control){setIconButton(control,'theme');});document.querySelectorAll('.fn-language-toggle,#language-toggle,#language').forEach(function(control){setIconButton(control,'language');});}
function translateStatic(){if(lang!=='en')return;var map={'Abmelden':'Sign out','Admin':'Admin','Topologie':'Topology','Dunkel':'Dark','Hell':'Light','Administration':'Administration','Ersteinrichtung':'Initial setup','Administrator anlegen':'Create administrator','Sicher einrichten':'Complete setup','Anmelden':'Sign in','Benutzername':'Username','Passwort':'Password','Passwort wiederholen':'Repeat password','Benutzerverwaltung':'User management','Benutzer':'User','Rolle':'Role','Status':'Status','Rolle ändern':'Change role','Passwort zurücksetzen':'Reset password','Aktionen':'Actions','Aktiv':'Active','Speichern':'Save','Setzen':'Set','Neues Passwort':'New password','Neuen Benutzer anlegen':'Create user','Initiales Passwort':'Initial password','Benutzer anlegen':'Create user','Profil aktualisieren (optional)':'Update profile (optional)','Neues Profil':'New profile','Bezeichnung':'Name','Version':'Version','SNMP-Profil speichern':'Save SNMP profile','Vorhanden':'Existing','Profil':'Profile','Aktion':'Action','SSH-Benutzer':'SSH user','Für Webterminal freigegeben':'Enabled for web terminal','Privater OpenSSH-/PEM-Schlüssel (alternativ zum Passwort)':'Private OpenSSH/PEM key (alternative to password)','SSH-Profil speichern':'Save SSH profile','Freigegeben':'Enabled','Gerätezuordnung':'Device assignment','Gerät':'Device','SNMP-Profil':'SNMP profile','SSH-Profil':'SSH profile','Bitte wählen':'Please select','Kein Webterminal':'No web terminal','Zuweisen':'Assign','SSH-Host-Keys':'SSH host keys','Freigegebener Fingerprint':'Approved fingerprint','Neu geprüft':'Newly verified','Host-Key prüfen':'Verify host key','Auditprotokoll':'Audit log','Zeit':'Time','Ziel':'Target','Adresse':'Address','Statische Routen':'Static routes','Werkseinstellungen':'Factory defaults','Route hinzufügen':'Add route','Löschen':'Delete','Zurücksetzen':'Reset','Zielnetz':'Destination network','Gateway':'Gateway','Aktive Kernel-Routen':'Active kernel routes','Aktive Routen des Docker-Hosts':'Active routes on the Docker host','Routen werden sofort auf dem Docker-Host aktiviert und bei jedem Start des Host-Dienstes erneut abgeglichen.':'Routes are applied directly on the Docker host and reconciled whenever the host service starts.','Aktuelles Gateway des Docker-Hosts:':'Current Docker host gateway:','Statische Route wurde dem Docker-Host hinzugefügt.':'The static route was added to the Docker host.','Statische Route wurde vom Docker-Host gelöscht.':'The static route was removed from the Docker host.','Gespeicherte statische Routen':'Saved static routes','Keine statischen Routen gespeichert.':'No static routes saved.','Auf Factory Default zurücksetzen':'Reset to factory defaults'};var walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT);var n;while((n=walker.nextNode())){var v=n.nodeValue.trim();if(map[v])n.nodeValue=n.nodeValue.replace(v,map[v]);}}
function enhanceNav(){var nav=document.querySelector('.header .nav,.topbar-inner,.nav-actions,html[data-app="fabricnavigator-devices"] .header-inner');if(!nav)return;var forms=Array.prototype.slice.call(nav.querySelectorAll('form'));var admin=Array.prototype.slice.call(nav.querySelectorAll('a')).filter(function(a){return /\/admin\/?$/.test(a.pathname);})[0],spacer=nav.querySelector('.fn-nav-spacer');if(!spacer){spacer=document.createElement('span');spacer.className='fn-nav-spacer';nav.insertBefore(spacer,admin||forms[0]||null);}if(admin)nav.appendChild(admin);forms.forEach(function(f){if(/logout/.test(f.action))nav.appendChild(f);});if(!document.getElementById('theme-toggle')){var tb=button(text('theme'),'fn-theme-toggle');tb.addEventListener('click',toggleTheme);nav.insertBefore(tb,spacer);}if(!document.getElementById('language-toggle')&&!document.getElementById('language')){var lb=button(text('language'),'fn-language-toggle');lb.addEventListener('click',toggleLanguage);nav.insertBefore(lb,spacer);}if(location.pathname.indexOf('/admin/')===0&&!/system\.jsp$/.test(location.pathname)){var system=document.createElement('a');system.href='/admin/system.jsp';system.className='button navlink fn-system-link';system.textContent=text('system');nav.insertBefore(system,spacer);}}
function mainMenuIcon(type){var paths={topology:'<circle cx="6" cy="6" r="2.2"/><circle cx="18" cy="6" r="2.2"/><circle cx="12" cy="18" r="2.2"/><path d="M8 7.2l3 8M16 7.2l-3 8M8.3 6h7.4"/>',devices:'<rect x="3" y="4" width="18" height="6" rx="1.8"/><rect x="3" y="14" width="18" height="6" rx="1.8"/><path d="M7 7h.01M7 17h.01M11 7h7M11 17h7"/>',admin:'<path d="M12 3l2 2.1 2.9-.2.8 2.8 2.5 1.5-1.2 2.7 1.2 2.7-2.5 1.5-.8 2.8-2.9-.2L12 21l-2-2.1-2.9.2-.8-2.8-2.5-1.5L5 12.1 3.8 9.4l2.5-1.5.8-2.8 2.9.2L12 3z"/><circle cx="12" cy="12" r="3"/>'};return '<span class="fn-main-nav-glyph" aria-hidden="true"><svg viewBox="0 0 24 24">'+paths[type]+'</svg></span>';}
function initMainMenu(){if(window.self!==window.top||document.querySelector('.fn-main-menu')||location.pathname.indexOf('/terminal/')===0)return;var header=document.querySelector('header'),host=header&&header.querySelector('.header-inner,.topbar-inner'),nav=header&&header.querySelector('.nav,.nav-actions');host=host||nav;if(!header||!host)return;var menu=document.createElement('nav');menu.className='fn-main-menu';menu.setAttribute('aria-label',lang==='de'?'Hauptnavigation':'Main navigation');var items=[['/topology/',lang==='de'?'Topologie':'Topology','topology'],['/devices/','Devices','devices'],['/admin/','Administration','admin']];items.forEach(function(item){var a=document.createElement('a');a.href=item[0];a.className='fn-main-nav-item';a.title=item[1];a.setAttribute('aria-label',item[1]);a.innerHTML=mainMenuIcon(item[2]);if(location.pathname.indexOf(item[0])===0)a.classList.add('active');menu.appendChild(a);});var controls=nav||host;controls.querySelectorAll('.fn-theme-toggle,.fn-language-toggle,#theme-toggle,#language-toggle,#language,form').forEach(function(x){if(x.tagName!=='FORM'||/logout/.test(x.action))menu.appendChild(x);});function normalizeControls(){var themeButton=menu.querySelector('.fn-theme-toggle,#theme-toggle'),languageButton=menu.querySelector('.fn-language-toggle,#language-toggle,#language'),logout=menu.querySelector('form[action*="logout"]');menu.querySelectorAll('button').forEach(function(control){control.classList.add('fn-shell-button');});if(themeButton){themeButton.classList.add('fn-theme-toggle');setIconButton(themeButton,'theme');}if(languageButton){languageButton.classList.add('fn-language-toggle');setIconButton(languageButton,'language');}if(logout){logout.classList.add('fn-menu-logout');var logoutButton=logout.querySelector('button');if(logoutButton){logoutButton.classList.remove('danger','secondary','button');logoutButton.classList.add('fn-shell-button','fn-menu-logout-button');logoutButton.textContent=lang==='de'?'Abmelden':'Sign out';}if(!logout.closest('.fn-account-menu'))menu.appendChild(logout);}if(themeButton)menu.appendChild(themeButton);if(languageButton)menu.appendChild(languageButton);}normalizeControls();menu.addEventListener('click',function(){setTimeout(normalizeControls,0);});if(nav&&nav!==host)host.insertBefore(menu,nav);else host.appendChild(menu);var logout=menu.querySelector('.fn-menu-logout');function addUserBadge(info){if(!info||!info.authenticated||!logout||menu.querySelector('.fn-current-user,.fn-account-menu'))return;var badge=document.createElement('span');badge.className='fn-current-user';badge.textContent=info.user+':'+String(info.role||'user').toLowerCase();badge.title=(lang==='de'?'Angemeldet als ':'Signed in as ')+info.user+' ('+info.role+')';menu.insertBefore(badge,logout);}var embeddedUser=document.documentElement.getAttribute('data-fn-user'),embeddedRole=document.documentElement.getAttribute('data-fn-role');if(embeddedUser)addUserBadge({authenticated:true,user:embeddedUser,role:embeddedRole||'user'});else fetch('/session-info.jsp',{credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(response){if(!response.ok)throw new Error();return response.json();}).then(addUserBadge).catch(function(){});host.querySelectorAll('a').forEach(function(a){if(a.closest('.fn-main-menu')||a.closest('.brand-logo')||a.classList.contains('logo'))return;if(a.pathname==='/devices/'||a.pathname.indexOf('/topology/')===0||a.pathname.indexOf('/admin/')===0)a.classList.add('fn-promoted-main-link');});}
function translateExtended(){if(lang!=='en')return;var map={'Routen werden sofort im Container aktiviert und bei jedem Containerstart erneut angewendet.':'Routes are activated in the container immediately and restored on every container start.','Entfernt Benutzer, Zugangsdaten, Gerätezuordnungen und statische Routen. Diese Aktion kann nicht rückgängig gemacht werden.':'Removes users, credentials, device assignments, and static routes. This action cannot be undone.','Zur Bestätigung':'To confirm, enter','eingeben':'','Lokale Benutzer, verschlüsselte SNMP-/SSH-Profile und Gerätezuordnungen. Geheimnisse werden nach dem Speichern nie wieder angezeigt.':'Local users, encrypted SNMP/SSH profiles, and device assignments. Secrets are never displayed again after saving.','Communities und SNMPv3-Passphrasen werden mit AES-GCM verschlüsselt. Leere Geheimnisfelder erhalten beim Aktualisieren den bestehenden Wert.':'Communities and SNMPv3 passphrases are encrypted with AES-GCM. Empty secret fields retain their existing value when updated.','Passwort oder privater Schlüssel werden nur serverseitig entschlüsselt. Host-Key-Prüfung bleibt zwingend aktiv.':'Passwords and private keys are decrypted only on the server. Host key verification remains mandatory.','Ein Gerät verwendet genau die hier zugewiesenen SNMP- und SSH-Profile. In den Gerätedateien selbst stehen nur nicht geheime Profil-IDs.':'A device uses exactly the SNMP and SSH profiles assigned here. Device files contain only non-secret profile IDs.','Ein Terminal startet erst nach expliziter Fingerprint-Prüfung. Vergleichen Sie den angezeigten SHA-256-Fingerprint über einen unabhängigen Kanal mit dem Switch.':'A terminal starts only after explicit fingerprint verification. Compare the displayed SHA-256 fingerprint with the switch through an independent channel.','Die letzten 100 administrativen Vorgänge. Passwörter, Communities und Schlüssel werden nicht protokolliert.':'The latest 100 administrative operations. Passwords, communities, and keys are not logged.','Lokale, geschützte Anmeldung für Topologie, Geräteverwaltung und Webterminal.':'Secure local sign-in for topology, device management, and web terminal.','Diese Seite schließt sich dauerhaft, sobald der erste lokale Administrator angelegt wurde.':'This page closes permanently after the first local administrator has been created.'};document.querySelectorAll('p,label').forEach(function(n){var v=n.childNodes.length===1?n.textContent.trim():'';if(map[v])n.textContent=map[v];});var placeholders={'10.20.0.0/16 oder default':'10.20.0.0/16 or default','Neues Passwort':'New password'};document.querySelectorAll('[placeholder]').forEach(function(n){if(placeholders[n.placeholder])n.placeholder=placeholders[n.placeholder];});}
function enhanceAuthControls(){if(document.body.classList.contains('fn-terminal-embedded')||(location.pathname.indexOf('/terminal/')===0&&window.self!==window.top)||document.querySelector('.header .nav,.header-inner,.topbar-inner,.nav-actions,.fn-auth-controls'))return;var c=document.createElement('div');c.className='fn-auth-controls';var tb=button('','fn-theme-toggle'),lb=button('','fn-language-toggle');tb.addEventListener('click',toggleTheme);lb.addEventListener('click',toggleLanguage);c.appendChild(tb);c.appendChild(lb);document.body.appendChild(c);setIconButton(tb,'theme');setIconButton(lb,'language');}
function useInformalGerman(){if(lang!=='de')return;var replacements={'Die Änderung wurde abgelehnt. Prüfen Sie Eingaben, Abhängigkeiten und den Schutz des letzten Administrators.':'Die Änderung wurde abgelehnt. Prüfe deine Eingaben, Abhängigkeiten und den Schutz des letzten Administrators.','Ein Terminal startet erst nach expliziter Fingerprint-Prüfung. Vergleichen Sie den angezeigten SHA-256-Fingerprint über einen unabhängigen Kanal mit dem Switch.':'Ein Terminal startet erst nach expliziter Fingerprint-Prüfung. Vergleiche den angezeigten SHA-256-Fingerprint über einen unabhängigen Kanal mit dem Switch.','Prüfen Sie die SSH-Zuordnung im Adminbereich.':'Prüfe die SSH-Zuordnung im Adminbereich.','(Sie)':'(du)'};var walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT),node;while((node=walker.nextNode())){var value=node.nodeValue.trim();if(replacements[value])node.nodeValue=node.nodeValue.replace(value,replacements[value]);}}
function guardInvalidNodeContextMenu(){if(location.pathname.indexOf('/topology/')!==0)return;document.addEventListener('contextmenu',function(event){var node=event.target.closest&&event.target.closest('#topology-graph .node'),menu=document.querySelector('.fn-node-context-menu');if(!node||!menu)return;var parts=String(node.dataset.fnIp||'').trim().split('.'),valid=parts.length===4&&parts.every(function(part){return /^(?:0|[1-9]\d{0,2})$/.test(part)&&Number(part)<=255;});if(!valid)menu.hidden=true;},true);}
function toggleTheme(){theme=theme==='dark'?'light':'dark';store(THEME_KEY,theme);document.documentElement.setAttribute('data-theme',theme);document.querySelectorAll('.fn-admin-system-frame').forEach(function(frame){try{frame.contentDocument.documentElement.setAttribute('data-theme',theme);}catch(ignored){}});refreshShellIcons();}
function toggleLanguage(){store(LANG_KEY,lang==='de'?'en':'de');clearTimeout(preferenceTimer);uploadPreferences().then(function(){location.reload();});}
function initDialogWindow(d){var win=d.querySelector('.fn-ssh-window'),head=d.querySelector('.fn-ssh-head'),min=d.querySelector('.fn-dialog-minimize'),max=d.querySelector('.fn-dialog-maximize'),drag=null;function repaintActiveTerminal(){var frame=d.querySelector('.fn-ssh-frame.active');if(!frame)return;[0,80,220,500].forEach(function(delay){setTimeout(function(){try{frame.contentWindow.postMessage({type:'fn-terminal-activate',force:true},location.origin);}catch(e){}},delay);});}function restoreVisibleGeometry(){d.hidden=false;win.style.removeProperty('right');win.style.removeProperty('bottom');if(win.style.position==='fixed'){var r=win.getBoundingClientRect(),width=Math.min(Math.max(420,r.width),Math.max(420,innerWidth-16)),height=Math.min(Math.max(260,r.height),Math.max(260,innerHeight-16)),left=Math.max(8,Math.min(innerWidth-width-8,parseFloat(win.style.left)||r.left)),top=Math.max(8,Math.min(innerHeight-height-8,parseFloat(win.style.top)||r.top));win.style.width=width+'px';win.style.height=height+'px';win.style.left=left+'px';win.style.top=top+'px';}repaintActiveTerminal();}function setMaximized(active){win.classList.toggle('maximized',active);if(max){max.textContent=active?'❐':'⛶';max.title=active?(lang==='de'?'Vorherige Größe':'Restore size'):(lang==='de'?'Maximieren':'Maximize');max.setAttribute('aria-label',max.title);}requestAnimationFrame(repaintActiveTerminal);}function toggleMinimize(){var active=win.classList.toggle('minimized');if(active)setMaximized(false);d.classList.toggle('minimized-shell',active);min.textContent=active?'↗':'−';min.title=active?text('restore'):text('minimize');min.setAttribute('aria-label',min.title);if(!active)requestAnimationFrame(restoreVisibleGeometry);saveSshState();}min.onclick=toggleMinimize;if(max)max.onclick=function(){setMaximized(!win.classList.contains('maximized'));};head.addEventListener('dblclick',function(e){if(!e.target.closest('button'))toggleMinimize();});head.addEventListener('pointerdown',function(e){if(win.classList.contains('minimized')){if(!e.target.closest('.fn-dialog-minimize'))toggleMinimize();return;}if(win.classList.contains('maximized')||e.button!==0||e.target.closest('button')||e.target.closest('.fn-ssh-tabs'))return;var r=win.getBoundingClientRect();win.style.position='fixed';win.style.left=r.left+'px';win.style.top=r.top+'px';win.style.width=r.width+'px';win.style.height=r.height+'px';drag={id:e.pointerId,dx:e.clientX-r.left,dy:e.clientY-r.top};head.setPointerCapture(e.pointerId);e.preventDefault();});head.addEventListener('pointermove',function(e){if(!drag||e.pointerId!==drag.id)return;var maxX=Math.max(0,innerWidth-win.offsetWidth),maxY=Math.max(0,innerHeight-80);win.style.left=Math.max(0,Math.min(maxX,e.clientX-drag.dx))+'px';win.style.top=Math.max(0,Math.min(maxY,e.clientY-drag.dy))+'px';});function end(e){if(!drag||e.pointerId!==drag.id)return;try{head.releasePointerCapture(e.pointerId);}catch(ignored){}drag=null;}head.addEventListener('pointerup',end);head.addEventListener('pointercancel',end);}
function toolbarConfig(){var fallback={labelMode:'ip',buttons:[]};try{var value=JSON.parse(saved(TOOLBAR_KEY)||'null');if(!value||!Array.isArray(value.buttons))return fallback;value.labelMode=value.labelMode==='name'?'name':'ip';value.buttons=value.buttons.slice(0,16).filter(function(x){return x&&String(x.label||'').trim()&&String(x.command||'').trim();}).map(function(x){return{label:String(x.label).slice(0,40),command:String(x.command).slice(0,500)};});return value;}catch(e){return fallback;}}
function sendTerminalInput(data){var frame=document.querySelector('.fn-ssh-frame.active');if(frame)try{frame.contentWindow.postMessage({type:'fn-terminal-input',data:data},location.origin);}catch(e){}}
function updateTerminalModeButton(d){if(!d)return;var mode=d.querySelector('.fn-terminal-mode'),frame=d.querySelector('.fn-ssh-frame.active'),interactive=!!(frame&&frame.dataset.interactive==='true'),connected=!!(frame&&frame.dataset.connectionState==='connected');if(!mode)return;mode.textContent=interactive?'Interactive':'Transparent';mode.title=interactive?(lang==='de'?'Interactive-Modus aktiv – mit Strg+T in den Transparent-Modus wechseln':'Interactive mode active – press Ctrl+T to switch to transparent mode'):(lang==='de'?'Transparent-Modus aktiv – mit Strg+T in den Interactive-Modus wechseln':'Transparent mode active – press Ctrl+T to switch to interactive mode');mode.setAttribute('aria-pressed',interactive?'true':'false');mode.disabled=!connected;mode.classList.toggle('active',interactive);}
function renderSshToolbar(d){var bar=d.querySelector('.fn-ssh-toolbar');if(!bar)return;bar.textContent='';function action(label,title,handler,className){var b=button(label,'fn-terminal-action'+(className?' '+className:''));b.title=title;b.onclick=handler;bar.appendChild(b);return b;}action('Ctrl+C',lang==='de'?'Markierten Terminaltext kopieren':'Copy selected terminal text',async function(){var frame=d.querySelector('.fn-ssh-frame.active'),selection='';try{if(frame&&typeof frame.contentWindow.fnTerminalSelection==='function')selection=frame.contentWindow.fnTerminalSelection();if(!selection){this.title=lang==='de'?'Kein Terminaltext markiert':'No terminal text selected';return;}await navigator.clipboard.writeText(selection);this.title=lang==='de'?'Auswahl kopiert':'Selection copied';}catch(e){this.title=lang==='de'?'Zwischenablagezugriff wurde vom Browser abgelehnt':'Clipboard access was denied by the browser';}});action('Ctrl+V',lang==='de'?'Zwischenablage in aktive Sitzung einfügen':'Paste clipboard into active session',async function(){try{var value=await navigator.clipboard.readText();if(value)sendTerminalInput(value);}catch(e){this.title=lang==='de'?'Zwischenablagezugriff wurde vom Browser abgelehnt':'Clipboard access was denied by the browser';}});action('Transparent','Ctrl+T',function(){var frame=d.querySelector('.fn-ssh-frame.active');if(!frame||frame.dataset.connectionState!=='connected')return;sendTerminalInput('\x14');frame.dataset.interactive=frame.dataset.interactive==='true'?'false':'true';updateTerminalModeButton(d);},'fn-terminal-mode');toolbarConfig().buttons.forEach(function(item){action(item.label,item.command,function(){sendTerminalInput(item.command+'\r');});});updateTerminalModeButton(d);}
function updateReconnectUi(d){if(!d)return;var frame=d.querySelector('.fn-ssh-frame.active'),notice=d.querySelector('.fn-ssh-reconnect');if(!notice)return;notice.hidden=!(frame&&frame.dataset.disconnected==='true');if(frame)notice.dataset.device=frame.dataset.device;}
function ensureDialog(){var d=document.querySelector('.fn-ssh-dialog');if(d)return d;d=document.createElement('div');d.className='fn-ssh-dialog';d.hidden=true;d.innerHTML='<section class="fn-ssh-window" role="dialog" aria-modal="true" aria-label="'+text('ssh')+'"><header class="fn-ssh-head"><strong>'+text('ssh')+'</strong><div class="fn-ssh-tabs"></div><button type="button" class="fn-shell-button fn-dialog-minimize" aria-label="'+text('minimize')+'" title="'+text('minimize')+'">−</button><button type="button" class="fn-shell-button fn-dialog-close" aria-label="'+text('close')+'">×</button></header><div class="fn-ssh-body"><div class="fn-ssh-reconnect" hidden><span>'+(lang==='de'?'Die SSH-Sitzung wurde getrennt.':'The SSH session was disconnected.')+'</span><button type="button" class="fn-shell-button">'+(lang==='de'?'Neu verbinden':'Reconnect')+'</button></div></div><footer class="fn-ssh-toolbar" aria-label="'+(lang==='de'?'Terminal-Werkzeugleiste':'Terminal toolbar')+'"></footer></section>';d.querySelector('.fn-dialog-close').onclick=function(){d.querySelectorAll('.fn-ssh-frame,.fn-ssh-tab').forEach(function(item){item.remove();});d.hidden=true;sessionStore(SSH_STATE_KEY,null);updateReconnectUi(d);};d.querySelector('.fn-ssh-reconnect button').onclick=function(){var frame=d.querySelector('.fn-ssh-frame.active');if(!frame)return;var source=new URL(frame.getAttribute('src'),location.href);source.searchParams.set('reconnect',String(Date.now()));frame.dataset.disconnected='false';frame.dataset.connectionState='connecting';frame.src=source.pathname+source.search;updateReconnectUi(d);};d.addEventListener('click',function(e){if(e.target===d){d.hidden=true;saveSshState();}});window.addEventListener('message',function(event){if(event.origin!==location.origin||!event.data||event.data.type!=='fn-terminal-state')return;var frame=Array.prototype.slice.call(d.querySelectorAll('.fn-ssh-frame')).filter(function(item){return item.contentWindow===event.source;})[0];if(!frame)return;frame.dataset.connectionState=event.data.state||'';frame.dataset.disconnected=event.data.state==='disconnected'?'true':'false';var tab=d.querySelector('.fn-ssh-tab[data-device="'+CSS.escape(frame.dataset.device)+'"]');if(tab)tab.classList.toggle('disconnected',frame.dataset.disconnected==='true');updateReconnectUi(d);});document.body.appendChild(d);renderSshToolbar(d);initDialogWindow(d);return d;}
function openSsh(url,nameHint){var d=ensureDialog(),u=new URL(url,location.href),device=u.searchParams.get('device')||'SSH',key=encodeURIComponent(device),tab=d.querySelector('.fn-ssh-tab[data-device="'+CSS.escape(key)+'"]'),body=d.querySelector('.fn-ssh-body'),label=toolbarConfig().labelMode==='name'&&nameHint?nameHint:device,created=false;if(!tab){created=true;tab=document.createElement('button');tab.type='button';tab.className='fn-ssh-tab';tab.dataset.device=key;tab.dataset.label=label;tab.innerHTML='<span></span><span class="fn-tab-close" aria-label="'+text('close')+'">×</span>';tab.querySelector('span').textContent=label;var frame=document.createElement('iframe');frame.className='fn-ssh-frame';frame.dataset.device=key;frame.dataset.label=label;frame.dataset.connectionState='connecting';frame.setAttribute('scrolling','no');u.searchParams.set('embed','1');frame.src=u.pathname+u.search;frame.title='SSH '+label;body.appendChild(frame);d.querySelector('.fn-ssh-tabs').appendChild(tab);tab.onclick=function(e){if(e.target.classList.contains('fn-tab-close')){frame.remove();tab.remove();var next=d.querySelector('.fn-ssh-tab');if(next)next.click();else{d.hidden=true;sessionStore(SSH_STATE_KEY,null);}saveSshState();return;}activate(key);};}activate(key);var activeFrame=d.querySelector('.fn-ssh-frame[data-device="'+CSS.escape(key)+'"]');if(!created&&activeFrame&&activeFrame.dataset.connectionState!=='connected'){var reconnectUrl=new URL(activeFrame.getAttribute('src'),location.href);reconnectUrl.searchParams.set('reconnect',String(Date.now()));activeFrame.dataset.disconnected='false';activeFrame.dataset.connectionState='connecting';activeFrame.src=reconnectUrl.pathname+reconnectUrl.search;tab.classList.remove('disconnected');updateReconnectUi(d);}d.hidden=false;saveSshState();}
function activate(key){document.querySelectorAll('.fn-ssh-tab,.fn-ssh-frame').forEach(function(x){x.classList.toggle('active',x.dataset.device===key);});var frame=document.querySelector('.fn-ssh-frame.active');if(frame)[30,120,320].forEach(function(delay){setTimeout(function(){try{frame.contentWindow.postMessage({type:'fn-terminal-activate',force:true},location.origin);}catch(e){}},delay);});var dialog=document.querySelector('.fn-ssh-dialog');updateReconnectUi(dialog);updateTerminalModeButton(dialog);saveSshState();}
function saveSshState(){var d=document.querySelector('.fn-ssh-dialog');if(!d||d.hidden)return;var sessions=Array.prototype.slice.call(d.querySelectorAll('.fn-ssh-frame')).map(function(f){return{url:f.getAttribute('src'),label:f.dataset.label||''};}),active=d.querySelector('.fn-ssh-frame.active');if(!sessions.length){sessionStore(SSH_STATE_KEY,null);return;}sessionStore(SSH_STATE_KEY,JSON.stringify({sessions:sessions,active:active&&active.getAttribute('src'),minimized:d.querySelector('.fn-ssh-window').classList.contains('minimized')}));}
function restoreSshState(){if(window.self!==window.top||location.pathname.indexOf('/terminal/')===0)return;var raw=sessionSaved(SSH_STATE_KEY),state;try{state=JSON.parse(raw||'null');}catch(e){sessionStore(SSH_STATE_KEY,null);return;}if(!state)return;var sessions=Array.isArray(state.sessions)?state.sessions:(Array.isArray(state.urls)?state.urls.map(function(url){return{url:url,label:''};}):[]);if(!sessions.length)return;sessions.forEach(function(item){openSsh(item.url,item.label);});if(state.active){var activeUrl=new URL(state.active,location.href),key=encodeURIComponent(activeUrl.searchParams.get('device')||'SSH');activate(key);}var d=ensureDialog(),win=d.querySelector('.fn-ssh-window');if(state.minimized&&!win.classList.contains('minimized'))d.querySelector('.fn-dialog-minimize').click();d.hidden=false;}
function ensureWebviewDialog(){var d=document.querySelector('.fn-webview-dialog');if(d)return d;d=document.createElement('div');d.className='fn-webview-dialog';d.hidden=true;d.innerHTML='<section class="fn-webview-window" role="dialog" aria-modal="true" aria-label="WebView"><header class="fn-webview-head"><strong>WebView</strong><div class="fn-webview-tabs"></div><button type="button" class="fn-shell-button fn-webview-close" aria-label="'+text('close')+'">×</button></header><div class="fn-webview-body"></div></section>';d.querySelector('.fn-webview-close').onclick=function(){d.hidden=true;};d.addEventListener('click',function(event){if(event.target===d)d.hidden=true;});document.body.appendChild(d);return d;}
function activateWebview(key){var d=ensureWebviewDialog();d.querySelectorAll('.fn-webview-tab,.fn-webview-frame,.fn-webview-loading').forEach(function(item){item.classList.toggle('active',item.dataset.device===key);});d.hidden=false;}
function openTunneledWebview(device,name){var d=ensureWebviewDialog(),key=encodeURIComponent(device),tab=d.querySelector('.fn-webview-tab[data-device="'+CSS.escape(key)+'"]'),body=d.querySelector('.fn-webview-body'),label=name||device;if(tab){activateWebview(key);return;}tab=document.createElement('button');tab.type='button';tab.className='fn-webview-tab';tab.dataset.device=key;tab.innerHTML='<span></span><span class="fn-tab-close" aria-label="'+text('close')+'">×</span>';tab.querySelector('span').textContent=label;var loading=document.createElement('div');loading.className='fn-webview-loading';loading.dataset.device=key;loading.textContent=lang==='de'?'WebView wird sicher verbunden …':'Connecting WebView securely …';body.appendChild(loading);d.querySelector('.fn-webview-tabs').appendChild(tab);tab.onclick=function(event){if(event.target.classList.contains('fn-tab-close')){var frame=d.querySelector('.fn-webview-frame[data-device="'+CSS.escape(key)+'"]');if(frame)frame.remove();loading.remove();tab.remove();var next=d.querySelector('.fn-webview-tab');if(next)next.click();else d.hidden=true;return;}activateWebview(key);};activateWebview(key);fetch('/webview-ticket.jsp?device='+encodeURIComponent(device),{credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(response){if(!response.ok)throw new Error(String(response.status));return response.json();}).then(function(result){if(!result.url)throw new Error('url');var frame=document.createElement('iframe');frame.className='fn-webview-frame';frame.dataset.device=key;frame.title='WebView '+label;frame.src=result.url;body.appendChild(frame);loading.remove();activateWebview(key);}).catch(function(){loading.textContent=lang==='de'?'WebView konnte nicht autorisiert werden. Bitte erneut anmelden.':'WebView authorization failed. Please sign in again.';loading.classList.add('error');});}
document.addEventListener('click',function(e){var a=e.target.closest&&e.target.closest('a[href*="/terminal/"]');if(!a)return;e.preventDefault();e.stopPropagation();var node=a.closest('.node'),card=a.closest('.device-card,.device,.card'),name=a.dataset.deviceName||(node&&node.dataset.fnName)||(card&&card.querySelector('.device-name,h2,h3')&&card.querySelector('.device-name,h2,h3').textContent.trim())||'';openSsh(a.getAttribute('href')||a.href,name);},true);
if(location.pathname.indexOf('/terminal/')===0&&window.self!==window.top)document.body.classList.add('fn-terminal-embedded');
function topologyEnhance(){var svg=document.getElementById('topology-graph'),rows=document.querySelectorAll('#connection-rows .connection-row');if(!svg||!rows.length)return;svg.querySelectorAll('.fn-decoration').forEach(function(x){x.remove();});var ns='http://www.w3.org/2000/svg';svg.querySelectorAll('.node').forEach(function(g){var c=g.querySelector('circle');if(c){var r=document.createElementNS(ns,'rect');r.setAttribute('x','-78');r.setAttribute('y','-50');r.setAttribute('width','156');r.setAttribute('height','100');r.setAttribute('rx','12');r.setAttribute('class','fn-node-box');c.replaceWith(r);}var title=(g.querySelector('title')||{}).textContent||'',parts=title.split(' · '),main=g.querySelector('text:not(.sub):not(.edm-link)'),sub=g.querySelector('text.sub');if(main){g.dataset.fnName=parts[0]||main.textContent;g.dataset.fnIp=parts[1]||'';main.textContent=(saved('fnNodeLabel')==='ip'&&g.dataset.fnIp)?g.dataset.fnIp:g.dataset.fnName;}if(sub)sub.style.display='none';var model=title.toLowerCase(),img=model.match(/(5320|5420|5520|5720)/)?'/assets/switch-front-universal.svg':model.match(/(vsp|fabric engine|voss)/)?'/assets/switch-front-fabric.svg':model.match(/(x4[0-9]{2}|switch engine|exos|4[12]20)/)?'/assets/switch-front-switchengine.svg':'';if(img&&!g.querySelector('.fn-node-front')){var image=document.createElementNS(ns,'image');image.setAttribute('href',img);image.setAttribute('x','-59');image.setAttribute('y','-37');image.setAttribute('width','118');image.setAttribute('height','26');image.setAttribute('class','fn-node-front');g.insertBefore(image,main);if(main)main.setAttribute('y','3');}}
);var groups={};rows.forEach(function(row){var cells=row.cells,key=[cells[0].textContent,cells[4].textContent].sort().join('|');(groups[key]||(groups[key]=[])).push(row);var idx=row.dataset.linkIndex,line=svg.querySelector('.link[data-link-index="'+idx+'"]');if(row.classList.contains('poe-powered')&&line){var p=midpoint(line),bolt=document.createElementNS(ns,'text');bolt.setAttribute('x',p.x);bolt.setAttribute('y',p.y-9);bolt.setAttribute('class','fn-decoration fn-poe-bolt');bolt.textContent='⚡';svg.appendChild(bolt);}});Object.keys(groups).forEach(function(key){var set=groups[key],explicit=set.some(function(r){return /(?:mlt|lag|trunk)/i.test(r.cells[1].textContent+' '+r.cells[3].textContent);});if(set.length<2&&!explicit)return;var pts=[];set.forEach(function(row,i){var line=svg.querySelector('.link[data-link-index="'+row.dataset.linkIndex+'"]');if(!line)return;var p=midpoint(line),off=(i-(set.length-1)/2)*13,lineP=endpoints(line),dx=lineP.x2-lineP.x1,dy=lineP.y2-lineP.y1,len=Math.sqrt(dx*dx+dy*dy)||1,nx=-dy/len,ny=dx/len;line.setAttribute('transform','translate('+(nx*off)+' '+(ny*off)+')');svg.querySelectorAll('.port-badge[data-link-index="'+row.dataset.linkIndex+'"]').forEach(function(b){var base=(b.getAttribute('transform')||'').replace(/ translate\([^)]*\)$/,'');b.setAttribute('transform',base+' translate('+(nx*off)+' '+(ny*off)+')');});pts.push({x:p.x+nx*off,y:p.y+ny*off});});if(!pts.length)return;var x=pts.reduce(function(s,p){return s+p.x;},0)/pts.length,y=pts.reduce(function(s,p){return s+p.y;},0)/pts.length,g=document.createElementNS(ns,'g');g.setAttribute('class','fn-decoration fn-mlt-badge');var el=document.createElementNS(ns,'ellipse');el.setAttribute('cx',x);el.setAttribute('cy',y);el.setAttribute('rx','64');el.setAttribute('ry','22');var label=document.createElementNS(ns,'text');label.setAttribute('x',x);label.setAttribute('y',y);label.textContent=text('mlt')+' · '+set.length+' '+text('links')+(set.some(function(r){return r.classList.contains('isis-adjacent');})?' · '+text('isis'):'');g.appendChild(el);g.appendChild(label);svg.appendChild(g);});}
function endpoints(line){if(line.tagName.toLowerCase()==='line')return{x1:+line.getAttribute('x1'),y1:+line.getAttribute('y1'),x2:+line.getAttribute('x2'),y2:+line.getAttribute('y2')};var nums=(line.getAttribute('d')||'').match(/-?\d+(?:\.\d+)?/g)||[0,0,0,0];return{x1:+nums[0],y1:+nums[1],x2:+nums[nums.length-2],y2:+nums[nums.length-1]};}
function midpoint(line){var p=endpoints(line);return{x:(p.x1+p.x2)/2,y:(p.y1+p.y2)/2};}
function addLabelToggle(){if(location.pathname.indexOf('/topology/')!==0||document.querySelector('.fn-label-toggle'))return;var host=document.querySelector('.view-filters,.type-filters');if(!host)return;var b=button(saved('fnNodeLabel')==='ip'?text('ip'):text('name'),'fn-label-toggle');b.setAttribute('aria-pressed',saved('fnNodeLabel')==='ip'?'true':'false');b.title=lang==='de'?'Anzeige zwischen Name und IP wechseln':'Switch display between name and IP';b.onclick=function(){store('fnNodeLabel',saved('fnNodeLabel')==='ip'?'name':'ip');document.querySelectorAll('.node').forEach(function(g){var main=g.querySelector('text:not(.sub):not(.edm-link)');if(main)main.textContent=saved('fnNodeLabel')==='ip'?(g.dataset.fnIp||g.dataset.fnName):g.dataset.fnName;});b.textContent=saved('fnNodeLabel')==='ip'?text('ip'):text('name');b.setAttribute('aria-pressed',saved('fnNodeLabel')==='ip'?'true':'false');};host.appendChild(b);}
function correctSwitchImages(svg){var ns='http://www.w3.org/2000/svg',xlink='http://www.w3.org/1999/xlink';svg.querySelectorAll('.node:not(.fn-ap-node):not(.fn-phone-node)').forEach(function(g){var title=((g.querySelector('title')||{}).textContent||'').toLowerCase(),src='';if(/5320-24p/.test(title))src='/assets/extreme-5320-24p-front.png';else if(/5320-16p/.test(title))src='/assets/extreme-5320-16p-front.png';else if(/fabricengine|fabric engine|\bvsp|voss|5420|5520|5720/.test(title))src='/assets/switch-front-fabric.svg';else if(/switchengine|switch engine|extremexos|\bexos\b|x4[0-9]{2}|4[12]20/.test(title))src='/assets/switch-front-switchengine.svg';if(!src)return;var image=g.querySelector('.fn-node-front');if(!image){image=document.createElementNS(ns,'image');image.setAttribute('class','fn-node-front');var main=g.querySelector('text:not(.sub):not(.edm-link)');g.insertBefore(image,main||null);}image.setAttribute('href',src);image.setAttributeNS(xlink,'xlink:href',src);image.setAttribute('x','-57');image.setAttribute('y','-34');image.setAttribute('width','114');image.setAttribute('height','27');image.setAttribute('preserveAspectRatio','xMidYMid meet');});}
function decodeLldpText(value){return String(value||'').replace(/0x(?:[0-9a-f]{2}:){3,}[0-9a-f]{2}/gi,function(encoded){try{return encoded.slice(2).split(':').map(function(part){return String.fromCharCode(parseInt(part,16));}).join('');}catch(e){return encoded;}});}
topologyEnhance=function(){var svg=document.getElementById('topology-graph'),ns='http://www.w3.org/2000/svg';if(!svg)return;svg.querySelectorAll('.node').forEach(function(g){var title=(g.querySelector('title')||{}).textContent||'',decoded=decodeLldpText(title),parts=title.split(' · '),isPhone=g.classList.contains('type-phone')||/(?:ip\s*phone|ip.?deskphone|deskphone|telephone|voip|avaya\s+(?:j\d|vantage|96\d{2})|polycom|yealink|grandstream|snom|cisco\s+(?:ip\s+phone|cp-\d))/i.test(decoded),isAp=!isPhone&&(g.classList.contains('type-accesspoint')||/(?:wlan|wireless|wi-fi|wifi|access.?point|wlan.?bridge)/i.test(decoded)),circle=g.querySelector('circle'),main=g.querySelector('text:not(.sub):not(.edm-link)'),sub=g.querySelector('text.sub');if(circle&&isPhone){var phone=document.createElementNS(ns,'g');phone.setAttribute('class','fn-phone-symbol');phone.innerHTML='<rect x="-34" y="-43" width="68" height="82" rx="13"></rect><rect class="fn-phone-screen" x="-23" y="-29" width="46" height="20" rx="4"></rect><path d="M -22 9 Q -12 22 0 22 Q 12 22 22 9"></path><circle cx="-15" cy="1" r="3"></circle><circle cx="0" cy="1" r="3"></circle><circle cx="15" cy="1" r="3"></circle>';circle.replaceWith(phone);g.classList.add('fn-phone-node');}else if(circle&&isAp){var symbol=document.createElementNS(ns,'g');symbol.setAttribute('class','fn-ap-symbol');symbol.innerHTML='<ellipse cx="0" cy="8" rx="48" ry="24"></ellipse><path d="M -29 -13 Q 0 -45 29 -13"></path><path d="M -17 -10 Q 0 -29 17 -10"></path><circle cx="0" cy="-7" r="3"></circle>';circle.replaceWith(symbol);g.classList.add('fn-ap-node');}else if(circle){var rect=document.createElementNS(ns,'rect');rect.setAttribute('x','-66');rect.setAttribute('y','-42');rect.setAttribute('width','132');rect.setAttribute('height','84');rect.setAttribute('rx','10');rect.setAttribute('class','fn-node-box');circle.replaceWith(rect);}if(main){g.dataset.fnName=parts[0]||main.textContent;g.dataset.fnIp=parts[1]||'';main.textContent=saved('fnNodeLabel')==='ip'&&g.dataset.fnIp?g.dataset.fnIp:g.dataset.fnName;if(isAp)main.setAttribute('y','14');if(isPhone)main.setAttribute('y','55');}if(sub)sub.style.display='none';g.querySelectorAll('a').forEach(function(a){var href=a.getAttribute('href')||'';if(href.indexOf('/terminal/')===0)g.dataset.fnSsh=href;a.remove();});});correctSwitchImages(svg);};
function observeTopology(){var svg=document.getElementById('topology-graph');if(!svg)return;var timer,observer=new MutationObserver(function(){clearTimeout(timer);timer=setTimeout(function(){observer.disconnect();topologyEnhance();addLabelToggle();observer.observe(svg,{childList:true,subtree:true});},80);});observer.observe(svg,{childList:true,subtree:true});topologyEnhance();addLabelToggle();}
function enhanceTopologyZoom(){if(location.pathname.indexOf('/topology/')!==0)return;var svg=document.getElementById('topology-graph'),wrap=document.querySelector('.graph-wrap');if(!svg||!wrap||document.querySelector('.fn-zoom-controls'))return;var key='fnTopologyZoom',min=50,max=200,step=10,value=parseInt(saved(key),10);if(!isFinite(value))value=100;value=Math.max(min,Math.min(max,Math.round(value/step)*step));var controls=document.createElement('div');controls.className='fn-zoom-controls fn-topology-bottom-controls';controls.setAttribute('role','group');controls.setAttribute('aria-label',lang==='de'?'Topologie-Zoom und Link-Legende':'Topology zoom and link legend');var minus=button('\u2212','fn-zoom-out'),range=document.createElement('input'),display=document.createElement('output'),plus=button('+','fn-zoom-in'),reset=button('100 %','fn-zoom-reset');minus.title=lang==='de'?'Topologie verkleinern':'Zoom out';plus.title=lang==='de'?'Topologie vergr\u00f6\u00dfern':'Zoom in';reset.title=lang==='de'?'Zoom zur\u00fccksetzen':'Reset zoom';minus.setAttribute('aria-label',minus.title);plus.setAttribute('aria-label',plus.title);reset.setAttribute('aria-label',reset.title);range.type='range';range.min=String(min);range.max=String(max);range.step=String(step);range.value=String(value);range.setAttribute('aria-label',lang==='de'?'Zoomstufe':'Zoom level');display.className='fn-zoom-value';controls.appendChild(minus);controls.appendChild(range);controls.appendChild(display);controls.appendChild(plus);controls.appendChild(reset);wrap.insertAdjacentElement('afterend',controls);
function dimensions(){var width=parseFloat(svg.getAttribute('width')),height=parseFloat(svg.getAttribute('height')),box=svg.viewBox&&svg.viewBox.baseVal;if(!width&&box)width=box.width;if(!height&&box)height=box.height;return{width:width||1350,height:height||900};}
function apply(next,preserveCenter){next=Math.max(min,Math.min(max,Math.round(next/step)*step));var oldWidth=Math.max(svg.getBoundingClientRect().width,1),oldHeight=Math.max(svg.getBoundingClientRect().height,1),centerX=(wrap.scrollLeft+wrap.clientWidth/2)/oldWidth,centerY=(wrap.scrollTop+wrap.clientHeight/2)/oldHeight,d=dimensions(),scale=next/100,logicalWidth=Math.max(d.width,Math.ceil(wrap.clientWidth/scale)),logicalHeight=Math.max(d.height,Math.ceil(wrap.clientHeight/scale)),box=svg.viewBox&&svg.viewBox.baseVal,newWidth=Math.round(logicalWidth*scale),newHeight=Math.round(logicalHeight*scale);value=next;range.value=String(value);display.value=value+' %';display.textContent=value+' %';svg.dataset.fnZoom=String(value);if(!box||Math.abs(box.width-logicalWidth)>.5||Math.abs(box.height-logicalHeight)>.5)svg.setAttribute('viewBox','0 0 '+logicalWidth+' '+logicalHeight);if(typeof graphSize!=='undefined'){graphSize.width=logicalWidth;graphSize.height=logicalHeight;}svg.style.width=newWidth+'px';svg.style.height=newHeight+'px';svg.style.minWidth=newWidth+'px';svg.style.minHeight=newHeight+'px';store(key,String(value));if(preserveCenter){wrap.scrollLeft=Math.max(0,centerX*newWidth-wrap.clientWidth/2);wrap.scrollTop=Math.max(0,centerY*newHeight-wrap.clientHeight/2);}}
minus.onclick=function(){apply(value-step,true);};plus.onclick=function(){apply(value+step,true);};reset.onclick=function(){apply(100,true);};range.addEventListener('input',function(){apply(parseInt(range.value,10),true);});wrap.addEventListener('wheel',function(e){if(!e.ctrlKey)return;e.preventDefault();apply(value+(e.deltaY<0?step:-step),true);},{passive:false});var timer,observer=new MutationObserver(function(){clearTimeout(timer);timer=setTimeout(function(){apply(value,false);},50);});observer.observe(svg,{attributes:true,attributeFilter:['width','height','viewBox']});if(window.ResizeObserver)new ResizeObserver(function(){clearTimeout(timer);timer=setTimeout(function(){apply(value,false);},50);}).observe(wrap);apply(value,false);}
function arrangeTopologyControls(){var controls=document.querySelector('.fn-zoom-controls'),toggle=document.querySelector('.fn-label-toggle');if(controls&&toggle&&toggle.parentNode!==controls)controls.insertBefore(toggle,controls.firstChild);}
function relocateTopologyLegends(){if(location.pathname.indexOf('/topology/')!==0)return;var controls=document.querySelector('.fn-zoom-controls'),legends=document.querySelector('.link-legends'),toggle=controls&&controls.querySelector('.fn-label-toggle');if(!controls||!legends||legends.parentNode===controls)return;if(toggle&&toggle.nextSibling)controls.insertBefore(legends,toggle.nextSibling);else if(toggle)controls.appendChild(legends);else controls.insertBefore(legends,controls.firstChild);}
function relocateTopologyFilters(){if(location.pathname.indexOf('/topology/')!==0||document.querySelector('.fn-side-filter'))return;var aside=document.querySelector('.storage-panel'),view=document.querySelector('.view-filters');if(!aside||!view)return;[['topology-type-filters',lang==='de'?'Gerätetypen anzeigen':'Show device types'],['link-view-filters',lang==='de'?'Link-Ansicht':'Link view']].forEach(function(definition){var field=document.getElementById(definition[0]);if(!field)return;var legend=field.querySelector('legend'),content=field.querySelector('.filter-options'),details=document.createElement('details'),summary=document.createElement('summary'),body=document.createElement('div');details.className='card fn-side-filter';summary.textContent=legend&&legend.textContent.trim()||definition[1];body.className='fn-side-filter-content';if(content)body.appendChild(content);details.appendChild(summary);details.appendChild(body);aside.appendChild(details);field.remove();});if(!view.children.length)view.remove();}
function relocateTopologyDiscovery(){if(location.pathname.indexOf('/topology/')!==0)return;var aside=document.querySelector('.storage-panel'),discovery=document.querySelector('.discovery-card');if(!aside||!discovery||discovery.parentElement===aside)return;var hint=discovery.querySelector('.summary-hint');if(hint)hint.remove();discovery.classList.add('fn-side-discovery');aside.insertBefore(discovery,aside.firstChild);}
function initTopologySidebarCollapse(){if(location.pathname.indexOf('/topology/')!==0)return;var aside=document.querySelector('.storage-panel'),layout=document.querySelector('.topology-layout');if(!aside||!layout)return;var details=Array.prototype.slice.call(aside.querySelectorAll('details.storage-card,details.fn-side-filter'));if(details.length<3)return;var icons=['▣','◫','⇄'];details.forEach(function(item,index){var summary=item.querySelector('summary');if(!summary||summary.querySelector('.fn-sidebar-icon'))return;var label=document.createElement('span'),icon=document.createElement('span'),labelText=summary.textContent.trim();label.className='fn-sidebar-label';label.textContent=labelText;icon.className='fn-sidebar-icon';icon.setAttribute('aria-hidden','true');icon.textContent=icons[index]||'•';summary.textContent='';summary.appendChild(icon);summary.appendChild(label);summary.setAttribute('aria-label',labelText);item.addEventListener('toggle',sync);});function sync(){var expanded=details.some(function(item){return item.open;});layout.classList.toggle('fn-sidebar-collapsed',!expanded);aside.classList.toggle('fn-sidebar-collapsed',!expanded);aside.classList.add('fn-sidebar-ready');window.setTimeout(function(){try{window.dispatchEvent(new Event('resize'));}catch(e){}},240);}sync();}
function initBuildNumber(){if(document.querySelector('.fn-build-number'))return;var badge=document.createElement('div');badge.className='fn-build-number';badge.textContent='Build '+BUILD_VERSION;badge.title=(lang==='de'?'Interne Buildnummer ':'Internal build number ')+BUILD_VERSION;document.body.appendChild(badge);}

/* Build 26.08.10.25: durable dialog lifecycle and real switch product imagery. */
function updateReconnectUiV45(d){if(!d)return;var frame=d.querySelector('.fn-ssh-frame.active'),notice=d.querySelector('.fn-ssh-reconnect'),message=notice&&notice.querySelector('span'),action=notice&&notice.querySelector('button'),loginRequired=!!(frame&&frame.dataset.loginRequired==='true');if(!notice)return;notice.hidden=!(frame&&frame.dataset.disconnected==='true');if(frame)notice.dataset.device=frame.dataset.device;if(message)message.textContent=loginRequired?(lang==='de'?'Deine Web-Anmeldung ist abgelaufen. Melde dich erneut an.':'Your web session expired. Sign in again.'):(lang==='de'?'Die SSH-Sitzung wurde getrennt.':'The SSH session was disconnected.');if(action)action.textContent=loginRequired?(lang==='de'?'Anmelden':'Sign in'):(lang==='de'?'Neu verbinden':'Reconnect');}
updateReconnectUi=updateReconnectUiV45;

function ensureDialogV45(){var d=document.querySelector('.fn-ssh-dialog');if(d)return d;d=document.createElement('div');d.className='fn-ssh-dialog';d.hidden=true;d.innerHTML='<section class="fn-ssh-window" role="dialog" aria-modal="true" aria-label="'+text('ssh')+'"><header class="fn-ssh-head"><strong>'+text('ssh')+'</strong><div class="fn-ssh-tabs"></div><button type="button" class="fn-shell-button fn-dialog-minimize" aria-label="'+text('minimize')+'" title="'+text('minimize')+'">&minus;</button><button type="button" class="fn-shell-button fn-dialog-close" aria-label="'+text('close')+'">&times;</button></header><div class="fn-ssh-body"><div class="fn-ssh-reconnect" hidden><span></span><button type="button" class="fn-shell-button"></button></div></div><footer class="fn-ssh-toolbar" aria-label="'+(lang==='de'?'Terminal-Werkzeugleiste':'Terminal toolbar')+'"></footer></section>';d.querySelector('.fn-dialog-close').onclick=function(){d.querySelectorAll('.fn-ssh-frame,.fn-ssh-tab').forEach(function(item){item.remove();});d.classList.remove('minimized-shell');d.querySelector('.fn-ssh-window').classList.remove('minimized');d.hidden=true;sessionStore(SSH_STATE_KEY,null);updateReconnectUi(d);};d.querySelector('.fn-ssh-reconnect button').onclick=function(){var frame=d.querySelector('.fn-ssh-frame.active');if(!frame)return;if(frame.dataset.loginRequired==='true'){location.href='/login.jsp?next='+encodeURIComponent(location.pathname+location.search);return;}var source=new URL(frame.getAttribute('src'),location.href);source.searchParams.set('reconnect',String(Date.now()));frame.dataset.disconnected='false';frame.dataset.connectionState='connecting';frame.src=source.pathname+source.search;updateReconnectUi(d);};d.addEventListener('click',function(e){if(e.target===d){d.hidden=true;saveSshState();}});window.addEventListener('message',function(event){if(event.origin!==location.origin||!event.data||event.data.type!=='fn-terminal-state')return;var frame=Array.prototype.slice.call(d.querySelectorAll('.fn-ssh-frame')).filter(function(item){return item.contentWindow===event.source;})[0];if(!frame)return;frame.dataset.loginRequired='false';frame.style.visibility='';frame.dataset.connectionState=event.data.state||'';frame.dataset.disconnected=event.data.state==='disconnected'?'true':'false';var tab=d.querySelector('.fn-ssh-tab[data-device="'+CSS.escape(frame.dataset.device)+'"]');if(tab)tab.classList.toggle('disconnected',frame.dataset.disconnected==='true');updateReconnectUi(d);});document.body.appendChild(d);renderSshToolbar(d);initDialogWindow(d);return d;}
ensureDialog=ensureDialogV45;

var ensureDialogBeforeMaximizeV69=ensureDialog;
ensureDialog=function(){var d=ensureDialogBeforeMaximizeV69(),win=d.querySelector('.fn-ssh-window'),head=d.querySelector('.fn-ssh-head'),button=d.querySelector('.fn-dialog-maximize');if(!button){button=document.createElement('button');button.type='button';button.className='fn-shell-button fn-dialog-maximize';button.textContent='⛶';head.insertBefore(button,d.querySelector('.fn-dialog-close'));button.onclick=function(){var active=win.classList.toggle('maximized');button.textContent=active?'❐':'⛶';button.title=active?(lang==='de'?'Vorherige Größe':'Restore size'):(lang==='de'?'Maximieren':'Maximize');button.setAttribute('aria-label',button.title);var frame=d.querySelector('.fn-ssh-frame.active');if(frame)requestAnimationFrame(function(){try{frame.contentWindow.postMessage({type:'fn-terminal-activate',force:true},location.origin);}catch(e){}});};var min=d.querySelector('.fn-dialog-minimize');min.addEventListener('click',function(){if(!win.classList.contains('minimized'))win.classList.remove('maximized');},true);d.querySelector('.fn-dialog-close').addEventListener('click',function(){win.classList.remove('maximized');button.textContent='⛶';},true);}button.title=win.classList.contains('maximized')?(lang==='de'?'Vorherige Größe':'Restore size'):(lang==='de'?'Maximieren':'Maximize');button.setAttribute('aria-label',button.title);return d;};

function syncTerminalModeV51(event){if(event.origin!==location.origin||!event.data||['fn-terminal-state','fn-terminal-mode'].indexOf(event.data.type)<0)return;var d=document.querySelector('.fn-ssh-dialog');if(!d)return;var frame=Array.prototype.slice.call(d.querySelectorAll('.fn-ssh-frame')).filter(function(item){return item.contentWindow===event.source;})[0];if(!frame)return;if(event.data.type==='fn-terminal-mode')frame.dataset.interactive=event.data.interactive?'true':'false';else if(event.data.state==='connected'&&!frame.dataset.interactive)frame.dataset.interactive='true';updateTerminalModeButton(d);}
window.addEventListener('message',syncTerminalModeV51);

function openSshV45(url,nameHint){var d=ensureDialog(),u=new URL(url,location.href),device=u.searchParams.get('device')||'SSH',key=encodeURIComponent(device),tab=d.querySelector('.fn-ssh-tab[data-device="'+CSS.escape(key)+'"]'),body=d.querySelector('.fn-ssh-body'),label=toolbarConfig().labelMode==='name'&&nameHint?nameHint:device,created=false;if(!tab){created=true;tab=document.createElement('button');tab.type='button';tab.className='fn-ssh-tab';tab.dataset.device=key;tab.dataset.label=label;tab.innerHTML='<span></span><span class="fn-tab-close" aria-label="'+text('close')+'">&times;</span>';tab.querySelector('span').textContent=label;var frame=document.createElement('iframe');frame.className='fn-ssh-frame';frame.dataset.device=key;frame.dataset.label=label;frame.dataset.connectionState='connecting';frame.setAttribute('scrolling','no');u.searchParams.set('embed','1');frame.src=u.pathname+u.search;frame.title='SSH '+label;frame.addEventListener('load',function(){if(!frame.isConnected)return;var path='';try{path=frame.contentWindow.location.pathname||'';}catch(e){}var loginRequired=/\/(?:login|setup)\.jsp$/.test(path);frame.dataset.loginRequired=loginRequired?'true':'false';if(loginRequired){frame.dataset.disconnected='true';frame.dataset.connectionState='login-required';frame.style.visibility='hidden';tab.classList.add('disconnected');}else frame.style.visibility='';updateReconnectUi(d);});body.appendChild(frame);d.querySelector('.fn-ssh-tabs').appendChild(tab);tab.onclick=function(e){if(e.target.classList.contains('fn-tab-close')){frame.remove();tab.remove();var next=d.querySelector('.fn-ssh-tab');if(next)next.click();else{d.hidden=true;sessionStore(SSH_STATE_KEY,null);}updateReconnectUi(d);saveSshState();return;}activate(key);};}activate(key);var activeFrame=d.querySelector('.fn-ssh-frame[data-device="'+CSS.escape(key)+'"]');if(!created&&activeFrame&&activeFrame.dataset.connectionState!=='connected'&&activeFrame.dataset.loginRequired!=='true'){var reconnectUrl=new URL(activeFrame.getAttribute('src'),location.href);reconnectUrl.searchParams.set('reconnect',String(Date.now()));activeFrame.dataset.disconnected='false';activeFrame.dataset.connectionState='connecting';activeFrame.src=reconnectUrl.pathname+reconnectUrl.search;tab.classList.remove('disconnected');updateReconnectUi(d);}d.hidden=false;saveSshState();}
openSsh=openSshV45;

function restoreSshStateV45(){if(window.self!==window.top||location.pathname.indexOf('/terminal/')===0||/\/(?:login|setup)\.jsp$/.test(location.pathname))return;var raw=sessionSaved(SSH_STATE_KEY),state;try{state=JSON.parse(raw||'null');}catch(e){sessionStore(SSH_STATE_KEY,null);return;}if(!state)return;var sessions=Array.isArray(state.sessions)?state.sessions:(Array.isArray(state.urls)?state.urls.map(function(url){return{url:url,label:''};}):[]);if(!sessions.length)return;sessions.forEach(function(item){openSsh(item.url,item.label);});if(state.active){var activeUrl=new URL(state.active,location.href),key=encodeURIComponent(activeUrl.searchParams.get('device')||'SSH');activate(key);}var d=ensureDialog(),win=d.querySelector('.fn-ssh-window');if(state.minimized&&!win.classList.contains('minimized'))d.querySelector('.fn-dialog-minimize').click();d.hidden=false;}
restoreSshState=restoreSshStateV45;

function ensureWebviewDialogV45(){var d=document.querySelector('.fn-webview-dialog');if(d)return d;d=document.createElement('div');d.className='fn-webview-dialog';d.hidden=true;d.innerHTML='<section class="fn-webview-window" role="dialog" aria-modal="true" aria-label="WebView"><header class="fn-webview-head"><strong>WebView</strong><div class="fn-webview-tabs"></div><button type="button" class="fn-shell-button fn-webview-minimize" aria-label="'+text('minimize')+'" title="'+text('minimize')+'">&minus;</button><button type="button" class="fn-shell-button fn-webview-close" aria-label="'+text('close')+'">&times;</button></header><div class="fn-webview-body"></div></section>';var win=d.querySelector('.fn-webview-window'),head=d.querySelector('.fn-webview-head'),min=d.querySelector('.fn-webview-minimize');function toggleMinimize(){var active=win.classList.toggle('minimized');d.classList.toggle('minimized-shell',active);min.textContent=active?'↗':'−';min.title=active?text('restore'):text('minimize');min.setAttribute('aria-label',min.title);}min.onclick=toggleMinimize;head.addEventListener('dblclick',function(event){if(!event.target.closest('button'))toggleMinimize();});head.addEventListener('click',function(event){if(win.classList.contains('minimized')&&!event.target.closest('button'))toggleMinimize();});d.querySelector('.fn-webview-close').onclick=function(){d.querySelectorAll('.fn-webview-frame,.fn-webview-loading,.fn-webview-tab').forEach(function(item){item.remove();});win.classList.remove('minimized');d.classList.remove('minimized-shell');d.hidden=true;};d.addEventListener('click',function(event){if(event.target===d)d.hidden=true;});document.body.appendChild(d);return d;}
ensureWebviewDialog=ensureWebviewDialogV45;

var ensureWebviewDialogBeforeMaximizeV69=ensureWebviewDialog;
ensureWebviewDialog=function(){var d=ensureWebviewDialogBeforeMaximizeV69(),win=d.querySelector('.fn-webview-window'),head=d.querySelector('.fn-webview-head'),button=d.querySelector('.fn-webview-maximize');if(!button){button=document.createElement('button');button.type='button';button.className='fn-shell-button fn-webview-maximize';button.textContent='⛶';head.insertBefore(button,d.querySelector('.fn-webview-close'));button.onclick=function(){var active=win.classList.toggle('maximized');button.textContent=active?'❐':'⛶';button.title=active?(lang==='de'?'Vorherige Größe':'Restore size'):(lang==='de'?'Maximieren':'Maximize');button.setAttribute('aria-label',button.title);};var min=d.querySelector('.fn-webview-minimize');min.addEventListener('click',function(){if(!win.classList.contains('minimized'))win.classList.remove('maximized');},true);d.querySelector('.fn-webview-close').addEventListener('click',function(){win.classList.remove('maximized');button.textContent='⛶';},true);}button.title=win.classList.contains('maximized')?(lang==='de'?'Vorherige Größe':'Restore size'):(lang==='de'?'Maximieren':'Maximize');button.setAttribute('aria-label',button.title);return d;};

var ensureWebviewDialogBeforeBackdropFixV52=ensureWebviewDialog;
ensureWebviewDialog=function(){var d=ensureWebviewDialogBeforeBackdropFixV52();if(d.dataset.backdropStable!=='true'){d.dataset.backdropStable='true';d.addEventListener('click',function(event){if(event.target===d){event.preventDefault();event.stopImmediatePropagation();}},true);}return d;};

function openTunneledWebviewV45(device,name){var d=ensureWebviewDialog(),key=encodeURIComponent(device),tab=d.querySelector('.fn-webview-tab[data-device="'+CSS.escape(key)+'"]'),body=d.querySelector('.fn-webview-body'),label=name||device;if(tab){activateWebview(key);return;}tab=document.createElement('button');tab.type='button';tab.className='fn-webview-tab';tab.dataset.device=key;tab.innerHTML='<span></span><span class="fn-tab-close" aria-label="'+text('close')+'">&times;</span>';tab.querySelector('span').textContent=label;var loading=document.createElement('div');loading.className='fn-webview-loading';loading.dataset.device=key;loading.textContent=lang==='de'?'WebView wird sicher verbunden ...':'Connecting WebView securely ...';body.appendChild(loading);d.querySelector('.fn-webview-tabs').appendChild(tab);tab.onclick=function(event){if(event.target.classList.contains('fn-tab-close')){var frame=d.querySelector('.fn-webview-frame[data-device="'+CSS.escape(key)+'"]');if(frame)frame.remove();loading.remove();tab.remove();var next=d.querySelector('.fn-webview-tab');if(next)next.click();else d.hidden=true;return;}activateWebview(key);};activateWebview(key);fetch('/webview-ticket.jsp?device='+encodeURIComponent(device),{credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(response){if(!response.ok)throw new Error(String(response.status));return response.json();}).then(function(result){if(!tab.isConnected)return;if(!result.url)throw new Error('url');var frame=document.createElement('iframe');frame.className='fn-webview-frame';frame.dataset.device=key;frame.title='WebView '+label;frame.src=result.url;body.appendChild(frame);loading.remove();activateWebview(key);}).catch(function(){if(!tab.isConnected)return;loading.textContent=lang==='de'?'WebView konnte nicht autorisiert werden. Bitte melde dich erneut an.':'WebView authorization failed. Please sign in again.';loading.classList.add('error');});}
openTunneledWebview=openTunneledWebviewV45;

var topologyEnhanceBaseV45=topologyEnhance;
topologyEnhance=function(){topologyEnhanceBaseV45();var svg=document.getElementById('topology-graph');if(!svg)return;var ns='http://www.w3.org/2000/svg';svg.querySelectorAll('.node').forEach(function(g){var title=((g.querySelector('title')||{}).textContent||'').toLowerCase(),exact='',generic='',x=-57,y=-34,width=114,height=50,labelY=30,insetX=57,insetTop=34,insetBottom=16;if(title.indexOf('5320-24p-8xe')>=0)exact='/assets/product-5320-24p-8xe.png';else if(title.indexOf('5320-16p-2mxt-2x')>=0)exact='/assets/product-5320-16p-2mxt-2x.png';if(!exact){if(g.classList.contains('fn-phone-node')||g.classList.contains('type-phone')){generic='/assets/generic-phone.png';x=-38;y=-40;width=76;height=76;labelY=51;insetX=38;insetTop=40;insetBottom=36;}else if(g.classList.contains('fn-ap-node')||g.classList.contains('type-accesspoint')){generic='/assets/generic-wlan-ap.png';x=-40;y=-40;width=80;height=80;labelY=52;insetX=40;insetTop=40;insetBottom=40;}else{generic='/assets/generic-switch.png';x=-63;y=-27;width=126;height=46;labelY=32;insetX=63;insetTop=27;insetBottom=19;}}var photo=exact||generic;g.classList.add('fn-node-has-photo');if(generic)g.classList.add('fn-node-has-generic');var image=g.querySelector('.fn-node-front');if(!image){image=document.createElementNS(ns,'image');g.insertBefore(image,g.querySelector('text:not(.sub):not(.edm-link)'));}image.setAttribute('href',photo);image.setAttribute('x',String(x));image.setAttribute('y',String(y));image.setAttribute('width',String(width));image.setAttribute('height',String(height));image.setAttribute('preserveAspectRatio','xMidYMid meet');image.setAttribute('class','fn-node-front '+(exact?'fn-node-product-photo':'fn-node-generic-image'));g.dataset.fnInsetX=String(insetX);g.dataset.fnInsetTop=String(insetTop);g.dataset.fnInsetBottom=String(insetBottom);var main=g.querySelector('text:not(.sub):not(.edm-link)');if(main)main.setAttribute('y',String(labelY));});if(typeof linkViews!=='undefined'&&typeof updateLinkGeometry==='function')linkViews.forEach(updateLinkGeometry);};

var topologyEnhanceBaseV60=topologyEnhance;
topologyEnhance=function(){topologyEnhanceBaseV60();var svg=document.getElementById('topology-graph');if(!svg)return;var ns='http://www.w3.org/2000/svg';svg.querySelectorAll('.node.fn-node-has-photo').forEach(function(g){var image=g.querySelector('.fn-node-front');if(!image)return;if(g.classList.contains('type-firewall')){image.setAttribute('href','/assets/generic-firewall.png');image.setAttribute('x','-60');image.setAttribute('y','-36');image.setAttribute('width','120');image.setAttribute('height','64');image.setAttribute('class','fn-node-front fn-node-generic-image');g.classList.add('fn-node-has-generic');g.dataset.fnInsetX='60';g.dataset.fnInsetTop='36';g.dataset.fnInsetBottom='28';var main=g.querySelector('text:not(.sub):not(.edm-link)');if(main)main.setAttribute('y','43');}var hit=g.querySelector('.fn-node-hitbox');if(!hit){hit=document.createElementNS(ns,'rect');hit.setAttribute('class','fn-node-hitbox');g.insertBefore(hit,image);}hit.setAttribute('x',image.getAttribute('x')||'-60');hit.setAttribute('y',image.getAttribute('y')||'-36');hit.setAttribute('width',image.getAttribute('width')||'120');hit.setAttribute('height',image.getAttribute('height')||'64');hit.setAttribute('rx','8');});if(typeof linkViews!=='undefined'&&typeof updateLinkGeometry==='function')linkViews.forEach(updateLinkGeometry);};

var EXTREME_PRODUCT_FILES=['4220-12p-4x-front.png','4220-24t-4x-front.png','4220-48t-4x-front.png','5120-24x-4y-front.png','5120-24xt-4y-front.png','5120-44x-4y-2c-front.png','5320-16p-2mxt-2x-front.png','5320-16p-4xe-ac-front.png','5320-16p-4xe-dc-front.png','5320-24p-8xe-front.png','5320-24t-24s-4xe-xt-front.png','5320-24t-4x-xt-front.png','5320-48p-8xe-front.png','5420f-16mw-32p-4xe-front.png','5420f-16w-32p-4xe-front.png','5420f-24p-4xe-front.png','5420f-24s-4xe-front.png','5420f-24t-4xe-front.png','5420f-48p-4xe-front.png','5420f-48p-4xl-front.png','5420f-48t-4xe-front.png','5420f-8w-16p-4xe-front.png','5420m-16mw-32p-4ye-front.png','5420m-24t-4ye-front.png','5420m-24w-24s-4ye-front.png','5420m-24w-4ye-front.png','5420m-48t-4ye-front.png','5420m-48w-4ye-front.png','5520-12mw-36w.png','5520-24t.png','5520-24w.png','5520-24x.png','5520-48se.png','5520-48t.png','5520-48w.png','5720-24mw-front.png','5720-24mxw-front.png','5720-48mw-front.png','5720-48mxw-front.png','7520-48xt.png','7520-48y-front.png','7520-48ye-front.png','7720-32c-front.png','8820-80c-front.png','isw-4-10-100p-2-10-100t-2-sfp-front-small.png','isw-4gbp-2gbt-2-sfp-front-small.png','isw-8-10-100p-4-sfp-front-small.png','isw-8gbp-4-sfp-front-small.png','slx9150-48xt-front-small.png','slx9150-48y-front-small.png','slx9540-front-small.png','slx9640-front.png','vsp4900-48p-front.png','vsp-7400-48y-8c-front.png','vsp-7432cq-front.png','x435-24p-4s.png','x435-24t-4s.png','x435-8p-2t-w.png','x435-8p-4s.png','x435-8t-4s.png','x465-24mu-24w-front.png','x465-24mu-front.png','x465-24w-front.png','x465-48p-front.png','x465-48t-front.png','x465-48w-front.png'];
var extremeProductPluginAvailable=false;fetch('/assets/product-switches/plugin.json',{cache:'no-store',credentials:'same-origin'}).then(function(response){if(!response.ok)throw new Error('not installed');return response.json();}).then(function(plugin){extremeProductPluginAvailable=plugin&&plugin.id==='extreme-device-images';if(extremeProductPluginAvailable&&saved('fnExtremeSwitchImages')==='product'&&typeof topologyEnhance==='function')topologyEnhance();}).catch(function(){extremeProductPluginAvailable=false;});
function extremeProductImage(title){var normalized=String(title||'').toLowerCase().replace(/[^a-z0-9]+/g,'-'),best='';EXTREME_PRODUCT_FILES.forEach(function(file){var token=file.replace(/\.png$/,'').replace(/-front(?:-left|-small)?$/,'').replace(/-acdc$/,'');if(normalized.indexOf(token)>=0&&token.length>best.length)best=token;});if(!best)return'';return'/assets/product-switches/'+EXTREME_PRODUCT_FILES.filter(function(file){var token=file.replace(/\.png$/,'').replace(/-front(?:-left|-small)?$/,'').replace(/-acdc$/,'');return token===best;})[0];}
var topologyEnhanceBaseV75=topologyEnhance;
topologyEnhance=function(){topologyEnhanceBaseV75();var svg=document.getElementById('topology-graph');if(!svg)return;var xlink='http://www.w3.org/1999/xlink',imageMode=saved('fnExtremeSwitchImages')==='product'&&extremeProductPluginAvailable?'product':'generic';svg.querySelectorAll('.node.type-switch').forEach(function(g){var title=((g.querySelector('title')||{}).textContent||g.dataset.fnTitle||'').toLowerCase(),fabricEngine=g.getAttribute('data-tunnel-webview')==='true'||/(?:fabricengine|fabric engine|\bvsp\b|voss|\b(?:5120|5320|5420|5520|5720|7520|7720|7830)\b)/.test(title),switchEngine=!fabricEngine&&/(?:switchengine|switch engine|extremexos|\bexos\b|x4[0-9]{2}|\b4[12]20\b)/.test(title),extreme=/(?:extreme|switchengine|fabricengine|extremexos|\bexos\b|\bvoss\b|\bvsp\b|\bslx\b|\bisw\b|\bx4[0-9]{2}\b|\b(?:4[12]20|5120|5320|5420|5520|5720|7520|7720|7830|8820)\b)/.test(title),source='';if(!extreme)return;if(imageMode==='product')source=extremeProductImage(title);if(!source)source=switchEngine?'/assets/switch-front-switchengine.svg':'/assets/switch-front-fabric.svg';var image=g.querySelector('.fn-node-front');if(!image)return;image.setAttribute('href',source);image.setAttributeNS(xlink,'xlink:href',source);image.setAttribute('x','-66');image.setAttribute('y','-30');image.setAttribute('width','132');image.setAttribute('height','32');image.setAttribute('class','fn-node-front '+(source.indexOf('/product-switches/')===0?'fn-node-product-photo':'fn-node-generic-image'));g.classList.toggle('fn-node-has-generic',source.indexOf('/product-switches/')!==0);g.dataset.fnInsetX='66';g.dataset.fnInsetTop='30';g.dataset.fnInsetBottom='2';var main=g.querySelector('text:not(.sub):not(.edm-link)');if(main)main.setAttribute('y','21');var hit=g.querySelector('.fn-node-hitbox');if(hit){hit.setAttribute('x','-66');hit.setAttribute('y','-30');hit.setAttribute('width','132');hit.setAttribute('height','32');}});if(typeof linkViews!=='undefined'&&typeof updateLinkGeometry==='function')linkViews.forEach(updateLinkGeometry);};

initTopologySidebarCollapse=function(){if(location.pathname.indexOf('/topology/')!==0)return;var aside=document.querySelector('.storage-panel'),layout=document.querySelector('.topology-layout');if(!aside||!layout)return;var details=Array.prototype.slice.call(aside.querySelectorAll('details.discovery-card,details.storage-card,details.fn-side-filter'));if(details.length<4)return;var icons=['&#8981;','&#9635;','&#9707;','&#8644;'];details.forEach(function(item,index){var summary=item.querySelector('summary');if(!summary||summary.querySelector('.fn-sidebar-icon'))return;var label=document.createElement('span'),icon=document.createElement('span'),labelText=summary.textContent.trim();label.className='fn-sidebar-label';label.textContent=labelText;icon.className='fn-sidebar-icon';icon.setAttribute('aria-hidden','true');icon.innerHTML=icons[index]||'&bull;';summary.textContent='';summary.appendChild(icon);summary.appendChild(label);summary.setAttribute('aria-label',labelText);summary.dataset.tooltip=labelText;item.addEventListener('toggle',sync);});function sync(){var expanded=details.some(function(item){return item.open;});details.forEach(function(item){var summary=item.querySelector('summary');if(summary)summary.title=expanded?'':summary.dataset.tooltip;});layout.classList.toggle('fn-sidebar-collapsed',!expanded);aside.classList.toggle('fn-sidebar-collapsed',!expanded);aside.classList.add('fn-sidebar-ready');window.setTimeout(function(){try{window.dispatchEvent(new Event('resize'));}catch(e){}},240);}sync();};
function labelAdminTabs(){var labels=lang==='de'?{users:'Benutzerverwaltung',snmp:'SNMP-Zugangsdaten',ssh:'SSH-Zugangsdaten',webview:'WebView-Zugangsdaten',devices:'Gerätezuordnung',hostkeys:'SSH-Hostschlüssel',audit:'Auditprotokoll',toolbar:'SSH-Werkzeugleiste',discovery:'Discovery',updates:'Updates',system:'System'}:{users:'User management',snmp:'SNMP credentials',ssh:'SSH credentials',webview:'WebView credentials',devices:'Device assignment',hostkeys:'SSH host keys',audit:'Audit log',toolbar:'SSH toolbar',discovery:'Discovery',updates:'Updates',system:'System'};document.querySelectorAll('.fn-admin-tab[role="tab"]').forEach(function(tab){if(labels[tab.dataset.tab])tab.textContent=labels[tab.dataset.tab];});}
function polishToolbarAdmin(){if(location.pathname!=='/admin/')return;var nav=document.querySelector('.fn-admin-tabs'),tab=nav&&nav.querySelector('[data-tab="toolbar"]'),panel=document.querySelector('.fn-toolbar-admin-panel'),sshTab=nav&&nav.querySelector('[data-tab="ssh"]');if(!nav||!tab||!panel)return;if(sshTab&&sshTab.nextSibling!==tab)nav.insertBefore(tab,sshTab.nextSibling);panel.classList.add('card','wide');var intro=panel.querySelector('p');if(intro)intro.classList.add('secret-note');var rows=panel.querySelector('.fn-toolbar-rows');if(rows&&!panel.querySelector('.fn-toolbar-section-title')){var title=document.createElement('h3');title.className='fn-toolbar-section-title';title.textContent=lang==='de'?'Benutzerdefinierte Toolbar-Buttons':'Custom toolbar buttons';rows.parentNode.insertBefore(title,rows);}tab.title=lang==='de'?'SSH-Toolbar konfigurieren':'Configure SSH toolbar';}
function detectedSoftwareVersion(value){var source=String(value||''),match=source.match(/(?:software(?:\s+version)?|version|release|voss|exos|fabric\s+engine|switch\s+engine)\s*[:=\/ -]*([0-9]+(?:\.[0-9A-Za-z_-]+)+)/i);if(match)return match[1];match=source.match(/\(([^()]*(?:\d+\.)+\d[^()]*)\)/);return match?match[1].trim():'';}
function sysDescriptionWithoutVersion(value,version){var source=String(value||'').trim();if(!version)return source;var escaped=String(version).replace(/[.*+?^${}()|[\]\\]/g,'\\$&');source=source.replace(new RegExp('(?:software(?:\\s+version)?|sw(?:\\s+version)?|version|release)\\s*[:=\\/ -]*'+escaped,'ig'),'');source=source.replace(new RegExp('(?:\\(|\\[)?\\s*'+escaped+'\\s*(?:\\)|\\])?','g'),'');return source.replace(/\s+([,;])/g,'$1').replace(/([,;])\s*([,;])/g,'$1').replace(/\s{2,}/g,' ').replace(/^[\s,;:-]+|[\s,;:-]+$/g,'').trim();}
var topologyEnhanceBeforeHoverCard=topologyEnhance;
topologyEnhance=function(){var svg=document.getElementById('topology-graph'),ns='http://www.w3.org/2000/svg';if(svg)svg.querySelectorAll('.node').forEach(function(g){if(!g.querySelector('title')&&g.dataset.fnTitle){var title=document.createElementNS(ns,'title');title.textContent=g.dataset.fnTitle;g.insertBefore(title,g.firstChild);}});topologyEnhanceBeforeHoverCard();if(!svg)return;svg.querySelectorAll('.node').forEach(function(g){var titleNode=g.querySelector('title'),raw=titleNode?titleNode.textContent:(g.dataset.fnTitle||''),parts=raw.split(' · '),description=decodeLldpText(parts.slice(2).join(' · ')).trim(),version=detectedSoftwareVersion(description),hoverDescription=sysDescriptionWithoutVersion(description,version),candidateIp=(g.dataset.fnIp||parts[1]||'').trim(),validIp=/^(?:\d{1,3}\.){3}\d{1,3}$/.test(candidateIp)&&candidateIp.split('.').every(function(part){return Number(part)<=255;});g.dataset.fnTitle=raw;g.dataset.fnName=g.dataset.fnName||parts[0]||'';g.dataset.fnIp=validIp?candidateIp:'';g.dataset.fnSysDescr=hoverDescription;g.dataset.fnSoftwareVersion=version;g.setAttribute('aria-label',(g.dataset.fnName||g.dataset.fnIp)+' · '+(g.dataset.fnIp||'unknown')+(hoverDescription?' · '+hoverDescription:'')+(version?' · '+version:''));if(titleNode)titleNode.remove();});};
function initNodeHoverCard(){if(location.pathname.indexOf('/topology/')!==0||document.querySelector('.fn-topology-node-hover-card'))return;var svg=document.getElementById('topology-graph');if(!svg)return;var card=document.createElement('div');card.className='fn-node-hover-card fn-topology-node-hover-card';card.setAttribute('role','tooltip');card.hidden=true;card.innerHTML='<strong class="fn-node-hover-name"></strong><dl><div><dt></dt><dd class="fn-node-hover-ip"></dd></div><div><dt></dt><dd class="fn-node-hover-type"></dd></div><div class="fn-node-hover-version-row"><dt></dt><dd class="fn-node-hover-version"></dd></div></dl>';document.body.appendChild(card);var labels=card.querySelectorAll('dt'),unknown=lang==='de'?'Unbekannt':'Unknown';function deviceLabels(){labels[0].textContent=lang==='de'?'IP-Adresse':'IP address';labels[1].textContent=lang==='de'?'Gerätetyp':'Device type';labels[2].textContent=lang==='de'?'Softwareversion':'Software version';}deviceLabels();function show(node,x,y){var version=node.dataset.fnSoftwareVersion||'',versionRow=card.querySelector('.fn-node-hover-version-row'),isGroup=node.classList.contains('type-group');card.classList.toggle('fn-group-hover',isGroup);card.querySelector('.fn-node-hover-name').textContent=node.dataset.fnName||node.dataset.fnIp||unknown;if(isGroup){labels[0].textContent=lang==='de'?'Enthaltene Geräte':'Contained devices';labels[1].textContent=lang==='de'?'Navigation':'Navigation';card.querySelector('.fn-node-hover-ip').textContent=node.dataset.memberCount||'0';card.querySelector('.fn-node-hover-type').textContent=lang==='de'?'Mit Doppelklick öffnen':'Double-click to open';card.querySelector('.fn-node-hover-version').textContent='';versionRow.hidden=true;}else{deviceLabels();card.querySelector('.fn-node-hover-ip').textContent=node.dataset.fnIp||unknown;card.querySelector('.fn-node-hover-type').textContent=node.dataset.fnSysDescr||unknown;card.querySelector('.fn-node-hover-version').textContent=version;versionRow.hidden=!version;}card.hidden=false;place(x,y);}function place(x,y){if(card.hidden)return;var width=card.offsetWidth,height=card.offsetHeight;card.style.left=Math.max(8,Math.min(innerWidth-width-8,x+16))+'px';card.style.top=Math.max(8,Math.min(innerHeight-height-8,y+16))+'px';}function nodeFrom(event){return event.target.closest&&event.target.closest('#topology-graph .node');}svg.addEventListener('pointerover',function(event){var node=nodeFrom(event);if(node)show(node,event.clientX,event.clientY);});svg.addEventListener('pointermove',function(event){if(!card.hidden)place(event.clientX,event.clientY);});svg.addEventListener('pointerout',function(event){var node=nodeFrom(event);if(node&&(!event.relatedTarget||!node.contains(event.relatedTarget)))card.hidden=true;});svg.addEventListener('focusin',function(event){var node=nodeFrom(event);if(!node)return;var rect=node.getBoundingClientRect();show(node,rect.right,rect.top);});svg.addEventListener('focusout',function(){card.hidden=true;});}
function deviceActionSvg(kind){var icons={web:'<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><path d="M3 12h18M12 3c3 3 3 15 0 18M12 3c-3 3-3 15 0 18"></path></svg>',tunnel:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 7h13l-3-3M20 17H7l3 3M17 7l-3 3M7 17l3-3"></path></svg>',ssh:'<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="4" width="18" height="16" rx="2"></rect><path d="m7 9 3 3-3 3M12 15h5"></path></svg>',refresh:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20 7v5h-5M4 17v-5h5M6.1 8.2A7 7 0 0 1 18.7 7M17.9 15.8A7 7 0 0 1 5.3 17"></path></svg>',delete:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 7h16M9 7V4h6v3M7 7l1 13h8l1-13M10 11v5M14 11v5"></path></svg>'};return icons[kind]||'';}
function initDeviceListActions(){if(location.pathname!=='/devices/'&&location.pathname!=='/devices/index.jsp')return;document.querySelectorAll('.device').forEach(function(row){var actions=row.querySelector('.actions'),ip=row.dataset.ip,name=(row.querySelector('.device-name')||{}).textContent||ip,type=row.querySelector('[data-role="type"]');if(!actions||!ip)return;var direct=actions.querySelector('.direct-webview');if(direct){direct.href='https://'+ip+'/';direct.target='_blank';direct.rel='noopener';direct.className='button fn-device-action fn-device-web-action';direct.removeAttribute('data-i18n');direct.innerHTML=deviceActionSvg('web');direct.title=lang==='de'?'WebView öffnen':'Open WebView';direct.setAttribute('aria-label',direct.title);var tunnel=document.createElement('button');tunnel.type='button';tunnel.className='fn-device-action fn-device-tunnel-action';tunnel.innerHTML=deviceActionSvg('tunnel');tunnel.title=lang==='de'?'Getunnelten WebView öffnen':'Open tunneled WebView';tunnel.setAttribute('aria-label',tunnel.title);tunnel.hidden=true;function updateTunnelVisibility(){var identity=(type&&type.textContent||'').toLowerCase();tunnel.hidden=!/(?:fabric\s*engine|\bvsp\b|\bvoss\b)/i.test(identity);}if(type)new MutationObserver(updateTunnelVisibility).observe(type,{childList:true,subtree:true,characterData:true});updateTunnelVisibility();tunnel.onclick=function(){openTunneledWebview(ip,(row.querySelector('.device-name')||{}).textContent||name);};direct.insertAdjacentElement('afterend',tunnel);}var ssh=actions.querySelector('a[href*="/terminal/"]');if(ssh){ssh.className='button terminal fn-device-action fn-device-ssh-action';ssh.removeAttribute('data-i18n');ssh.innerHTML=deviceActionSvg('ssh');ssh.title=lang==='de'?'SSH öffnen':'Open SSH';ssh.setAttribute('aria-label',ssh.title);}var refresh=actions.querySelector('.refresh');if(refresh){refresh.classList.add('fn-device-action');refresh.removeAttribute('data-i18n');refresh.innerHTML=deviceActionSvg('refresh');refresh.title=lang==='de'?'Geräteinformationen aktualisieren':'Refresh device information';refresh.setAttribute('aria-label',refresh.title);}var remove=actions.querySelector('.remove-device');if(remove){remove.classList.add('fn-device-action');remove.removeAttribute('data-i18n');remove.innerHTML=deviceActionSvg('delete');remove.title=lang==='de'?'Gerät löschen':'Delete device';remove.setAttribute('aria-label',remove.title);}});}

/* Keep native resize gestures from being mistaken for a backdrop click. */
function stabilizeSshDialogV48(d){if(!d||d.dataset.resizeStable==='true')return d;d.dataset.resizeStable='true';d.addEventListener('click',function(event){if(event.target===d){event.preventDefault();event.stopImmediatePropagation();}},true);var win=d.querySelector('.fn-ssh-window'),resizeTimer;if(win&&window.ResizeObserver)new ResizeObserver(function(){if(d.hidden||win.classList.contains('minimized'))return;clearTimeout(resizeTimer);resizeTimer=setTimeout(function(){if(win.style.position==='fixed'){var rect=win.getBoundingClientRect(),width=Math.min(rect.width,Math.max(420,innerWidth-16)),height=Math.min(rect.height,Math.max(260,innerHeight-16)),left=Math.max(8,Math.min(innerWidth-width-8,rect.left)),top=Math.max(8,Math.min(innerHeight-height-8,rect.top));win.style.width=width+'px';win.style.height=height+'px';win.style.left=left+'px';win.style.top=top+'px';}var frame=d.querySelector('.fn-ssh-frame.active');if(frame)[0,80,220].forEach(function(delay){setTimeout(function(){try{frame.contentWindow.postMessage({type:'fn-terminal-activate',force:true},location.origin);}catch(e){}},delay);});},100);}).observe(win);return d;}
var ensureDialogBaseV48=ensureDialog;
ensureDialog=function(){return stabilizeSshDialogV48(ensureDialogBaseV48());};
function initAdminTabs(){if(location.pathname!=='/admin/'||document.querySelector('.fn-admin-tabs'))return;var grid=document.querySelector('main.page .grid');if(!grid)return;var sections=Array.prototype.slice.call(grid.children).filter(function(x){return x.tagName==='SECTION';}),definitions=[['users',lang==='de'?'Benutzer':'Users'],['snmp','SNMP'],['ssh','SSH'],['devices',lang==='de'?'Geräte':'Devices'],['hostkeys','Host-Keys'],['audit','Audit']];if(sections.length<definitions.length)return;var nav=document.createElement('nav');nav.className='fn-admin-tabs';nav.setAttribute('role','tablist');nav.setAttribute('aria-label',lang==='de'?'Administrationsbereiche':'Administration sections');var savedTab=saved('fnAdminTab'),initial=definitions.some(function(x){return x[0]===savedTab;})?savedTab:'users';function activate(key,focus){sections.forEach(function(section,index){var active=definitions[index]&&definitions[index][0]===key;section.hidden=!active;section.classList.add('fn-admin-tab-panel');section.setAttribute('role','tabpanel');section.setAttribute('aria-labelledby','fn-admin-tab-'+(definitions[index]?definitions[index][0]:index));});nav.querySelectorAll('[role="tab"]').forEach(function(tab){var active=tab.dataset.tab===key;tab.classList.toggle('active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.tabIndex=active?0:-1;if(active&&focus)tab.focus();});store('fnAdminTab',key);}definitions.forEach(function(def,index){var tab=button(def[1],'fn-admin-tab');tab.id='fn-admin-tab-'+def[0];tab.dataset.tab=def[0];tab.setAttribute('role','tab');tab.onclick=function(){activate(def[0],false);};tab.onkeydown=function(event){if(event.key!=='ArrowLeft'&&event.key!=='ArrowRight')return;event.preventDefault();var next=(index+(event.key==='ArrowRight'?1:-1)+definitions.length)%definitions.length;activate(definitions[next][0],true);};nav.appendChild(tab);});var system=document.createElement('a');system.href='/admin/system.jsp';system.className='fn-admin-tab fn-admin-system-tab';system.textContent='System';nav.appendChild(system);grid.parentNode.insertBefore(nav,grid);activate(initial,false);if(savedTab&&savedTab!==initial)try{localStorage.setItem('fnAdminTab',savedTab);}catch(ignored){}}
function integrateAdminSystem(){if(location.pathname==='/admin/system.jsp'&&new URLSearchParams(location.search).get('embed')==='1'){document.documentElement.classList.add('fn-admin-system-embedded');return;}if(location.pathname!=='/admin/')return;var nav=document.querySelector('.fn-admin-tabs'),grid=document.querySelector('main.page .grid'),old=nav&&nav.querySelector('.fn-admin-system-tab');if(!nav||!grid||!old)return;var tab=button('System','fn-admin-tab fn-admin-system-tab');tab.dataset.tab='system';tab.setAttribute('role','tab');tab.id='fn-admin-tab-system';old.replaceWith(tab);var panel=document.createElement('section');panel.className='fn-admin-tab-panel fn-admin-system-panel';panel.hidden=true;panel.setAttribute('role','tabpanel');panel.setAttribute('aria-labelledby',tab.id);panel.innerHTML='<iframe class="fn-admin-system-frame" title="System" src="/admin/system.jsp?embed=1"></iframe>';grid.appendChild(panel);function show(){grid.querySelectorAll('.fn-admin-tab-panel').forEach(function(x){x.hidden=x!==panel;});nav.querySelectorAll('[role="tab"]').forEach(function(x){var active=x===tab;x.classList.toggle('active',active);x.setAttribute('aria-selected',active?'true':'false');x.tabIndex=active?0:-1;});store('fnAdminTab','system');}tab.onclick=show;nav.addEventListener('click',function(event){var other=event.target.closest('[role="tab"]');if(other&&other!==tab)panel.hidden=true;},true);if(saved('fnAdminTab')==='system')show();}
function initToolbarAdmin(){if(location.pathname!=='/admin/'||document.querySelector('.fn-toolbar-admin-panel'))return;var nav=document.querySelector('.fn-admin-tabs'),grid=document.querySelector('main.page .grid');if(!nav||!grid)return;var tab=button(lang==='de'?'SSH-Toolbar':'SSH toolbar','fn-admin-tab');tab.dataset.tab='toolbar';tab.setAttribute('role','tab');tab.id='fn-admin-tab-toolbar';nav.appendChild(tab);var panel=document.createElement('section');panel.className='fn-admin-tab-panel fn-toolbar-admin-panel';panel.hidden=true;panel.setAttribute('role','tabpanel');panel.setAttribute('aria-labelledby',tab.id);panel.innerHTML='<h2>'+(lang==='de'?'SSH-Toolbar':'SSH toolbar')+'</h2><p>'+(lang==='de'?'Legt den Standardnamen der SSH-Tabs und eigene Terminal-Kommandos fest.':'Configure the default SSH tab label and custom terminal commands.')+'</p><label class="fn-toolbar-mode">'+(lang==='de'?'SSH-Tab standardmäßig anzeigen als':'Default SSH tab label')+'<select><option value="ip">IP</option><option value="name">'+(lang==='de'?'Gerätename':'Device name')+'</option></select></label><div class="fn-toolbar-rows"></div><div class="fn-toolbar-admin-actions"><button type="button" class="fn-shell-button fn-toolbar-add">'+(lang==='de'?'Button hinzufügen':'Add button')+'</button><button type="button" class="fn-shell-button fn-toolbar-save">'+(lang==='de'?'Speichern':'Save')+'</button><span class="fn-toolbar-status" role="status"></span></div>';grid.appendChild(panel);var rows=panel.querySelector('.fn-toolbar-rows'),select=panel.querySelector('select');function addRow(item){var row=document.createElement('div');row.className='fn-toolbar-row';row.innerHTML='<input class="fn-toolbar-label" maxlength="40" placeholder="'+(lang==='de'?'Kurzbeschreibung':'Short description')+'"><input class="fn-toolbar-command" maxlength="500" placeholder="'+(lang==='de'?'Kommando':'Command')+'"><button type="button" class="fn-shell-button" aria-label="'+(lang==='de'?'Entfernen':'Remove')+'">×</button>';row.querySelector('.fn-toolbar-label').value=item&&item.label||'';row.querySelector('.fn-toolbar-command').value=item&&item.command||'';row.querySelector('button').onclick=function(){row.remove();};rows.appendChild(row);}var cfg=toolbarConfig();select.value=cfg.labelMode;cfg.buttons.forEach(addRow);panel.querySelector('.fn-toolbar-add').onclick=function(){addRow({});};panel.querySelector('.fn-toolbar-save').onclick=function(){var buttons=[];rows.querySelectorAll('.fn-toolbar-row').forEach(function(row){var label=row.querySelector('.fn-toolbar-label').value.trim(),command=row.querySelector('.fn-toolbar-command').value.trim();if(label&&command)buttons.push({label:label,command:command});});store(TOOLBAR_KEY,JSON.stringify({labelMode:select.value==='name'?'name':'ip',buttons:buttons.slice(0,16)}));panel.querySelector('.fn-toolbar-status').textContent=lang==='de'?'Gespeichert.':'Saved.';var dialog=document.querySelector('.fn-ssh-dialog');if(dialog)renderSshToolbar(dialog);};function show(){grid.querySelectorAll('.fn-admin-tab-panel').forEach(function(x){x.hidden=x!==panel;});nav.querySelectorAll('[role="tab"]').forEach(function(x){var active=x===tab;x.classList.toggle('active',active);x.setAttribute('aria-selected',active?'true':'false');x.tabIndex=active?0:-1;});store('fnAdminTab','toolbar');}tab.onclick=show;nav.addEventListener('click',function(event){var other=event.target.closest('[role="tab"]');if(other&&other!==tab)panel.hidden=true;},true);if(saved('fnAdminTab')==='toolbar')show();}
function initNodeContextMenu(){if(location.pathname.indexOf('/topology/')!==0||document.querySelector('.fn-node-context-menu'))return;var menu=document.createElement('div'),activeNodes=[];menu.className='fn-node-context-menu';menu.setAttribute('role','menu');menu.tabIndex=-1;menu.hidden=true;function item(label,icon){var a=document.createElement('a');a.href='#';a.className='fn-node-menu-item';a.setAttribute('role','menuitem');var symbol=document.createElement('span');symbol.className='fn-node-menu-icon';symbol.setAttribute('aria-hidden','true');symbol.textContent=icon;a.appendChild(symbol);a.appendChild(document.createTextNode(label));menu.appendChild(a);return a;}function validIpv4(value){var parts=String(value||'').split('.');return parts.length===4&&parts.every(function(part){return /^(?:0|[1-9]\d{0,2})$/.test(part)&&Number(part)<=255;});}function nodesFor(clicked){var selected=Array.prototype.slice.call(document.querySelectorAll('#topology-graph .node.fn-selected'));if(selected.length>1&&selected.indexOf(clicked)>=0)return selected;var id=clicked.dataset.nodeId;if(typeof setNodeSelection==='function'&&id)setNodeSelection([id]);return[clicked];}function hide(){menu.hidden=true;}function openWindow(url){var opened=window.open(url,'_blank','noopener');if(opened)try{opened.opener=null;}catch(ignored){}}var web=item('Open WebView','🌐'),tunneledWeb=item(lang==='de'?'Getunnelten WebView öffnen':'Open tunneled WebView','⇄'),ssh=item(lang==='de'?'SSH öffnen':'Open SSH','>_');document.body.appendChild(menu);document.addEventListener('contextmenu',function(event){var node=event.target.closest&&event.target.closest('#topology-graph .node');if(!node)return;var candidates=nodesFor(node),allValid=candidates.every(function(item){return validIpv4((item.dataset.fnIp||'').trim());});if(!allValid){hide();return;}activeNodes=candidates;web.hidden=false;tunneledWeb.hidden=!candidates.every(function(item){return item.dataset.tunnelWebview==='true'||!!item.dataset.fnSsh;});ssh.hidden=!candidates.every(function(item){return !!item.dataset.fnSsh;});event.preventDefault();if(document.activeElement)document.activeElement.blur();menu.hidden=false;menu.style.left='0px';menu.style.top='0px';var x=Math.min(event.clientX,innerWidth-menu.offsetWidth-8),y=Math.min(event.clientY,innerHeight-menu.offsetHeight-8);menu.style.left=Math.max(8,x)+'px';menu.style.top=Math.max(8,y)+'px';},true);web.addEventListener('click',function(event){event.preventDefault();var nodes=activeNodes.slice();hide();nodes.forEach(function(node){openWindow('https://'+node.dataset.fnIp+'/');});});tunneledWeb.addEventListener('click',function(event){event.preventDefault();var nodes=activeNodes.slice();hide();nodes.forEach(function(node){openTunneledWebview(node.dataset.fnIp,node.dataset.fnName||node.dataset.fnIp);});});ssh.addEventListener('click',function(event){event.preventDefault();var nodes=activeNodes.slice();hide();nodes.forEach(function(node){if(node.dataset.fnSsh)openSsh(node.dataset.fnSsh,node.dataset.fnName||node.dataset.fnIp||'');});});document.addEventListener('pointerdown',function(event){if(!menu.hidden&&!menu.contains(event.target))hide();},true);document.addEventListener('keydown',function(event){if(event.key==='Escape')hide();});window.addEventListener('blur',hide);}
function initTopologyNotices(){if(location.pathname.indexOf('/topology/')!==0)return;var warnings=document.getElementById('topology-warnings'),summary=document.getElementById('topology-summary');if(!warnings||!summary)return;var warningTimer,summaryTimer,allowSshNotice=typeof wasPosted!=='undefined'&&wasPosted===true&&typeof data!=='undefined'&&!!data,toast=document.createElement('div');toast.className='fn-topology-summary-toast';toast.setAttribute('role','status');toast.hidden=true;document.body.appendChild(toast);function ring(host,duration){var progress=host.querySelector(':scope > .fn-expiry-ring');if(!progress){progress=document.createElement('span');progress.className='fn-expiry-ring';progress.setAttribute('aria-hidden','true');host.appendChild(progress);}progress.title=lang==='de'?'Verschwindet in etwa '+Math.round(duration/1000)+' Sekunden':'Disappears in about '+Math.round(duration/1000)+' seconds';progress.style.setProperty('--fn-expiry-duration',duration+'ms');progress.classList.remove('running');void progress.offsetWidth;progress.classList.add('running');}function sshStatus(item){return /^(?:SSH connected|SSH unavailable|SSH auto-bind)/i.test(item.textContent.trim());}function syncWarningVisibility(){var visible=Array.prototype.some.call(warnings.querySelectorAll('li'),function(item){return !item.classList.contains('fn-notice-expired');});warnings.classList.toggle('hidden',!visible);}function showWarnings(){clearTimeout(warningTimer);warnings.classList.remove('fn-notice-expired');var items=Array.prototype.slice.call(warnings.querySelectorAll('li')),sshItems=items.filter(sshStatus);if(!sshItems.length){syncWarningVisibility();return;}if(!allowSshNotice){sshItems.forEach(function(item){item.classList.add('fn-notice-expired');});syncWarningVisibility();return;}sshItems.forEach(function(item,index){item.classList.remove('fn-notice-expired');var existing=item.querySelector(':scope > .fn-expiry-ring');if(index===0)ring(item,10000);else if(existing)existing.remove();});syncWarningVisibility();warningTimer=setTimeout(function(){allowSshNotice=false;sshItems.forEach(function(item){item.classList.add('fn-notice-expired');});syncWarningVisibility();},10000);}function showSummary(){clearTimeout(summaryTimer);var value=summary.textContent.trim();if(!value)return;toast.textContent=value;ring(toast,60000);toast.hidden=false;summary.classList.add('fn-summary-source-hidden');summaryTimer=setTimeout(function(){toast.hidden=true;},60000);}new MutationObserver(showWarnings).observe(warnings,{childList:true,subtree:true,characterData:true});new MutationObserver(showSummary).observe(summary,{childList:true,subtree:true,characterData:true});showWarnings();showSummary();}
function initConnectionsCollapse(){if(location.pathname.indexOf('/topology/')!==0)return;var panel=document.querySelector('.connections-panel'),workspace=document.querySelector('.topology-workspace'),heading=panel&&panel.querySelector('h3');if(!panel||!workspace||!heading||panel.dataset.fnCollapsible==='true')return;panel.dataset.fnCollapsible='true';var head=document.createElement('div'),content=document.createElement('div'),toggle=button('−','fn-connections-toggle');head.className='fn-connections-head';content.className='fn-connections-content';heading.parentNode.insertBefore(head,heading);head.appendChild(heading);while(head.nextSibling)content.appendChild(head.nextSibling);panel.appendChild(content);head.appendChild(toggle);function apply(collapsed,persist){panel.classList.toggle('fn-connections-collapsed',collapsed);workspace.classList.toggle('fn-connections-collapsed',collapsed);toggle.textContent=collapsed?'↗':'−';toggle.title=collapsed?(lang==='de'?'Verbindungstabelle maximieren':'Expand connections table'):(lang==='de'?'Verbindungstabelle minimieren':'Collapse connections table');toggle.setAttribute('aria-label',toggle.title);toggle.setAttribute('aria-expanded',collapsed?'false':'true');if(persist)store('fnConnectionsCollapsed',collapsed?'true':'false');window.setTimeout(function(){window.dispatchEvent(new CustomEvent('fn-topology-layout'));},220);}toggle.onclick=function(){apply(!panel.classList.contains('fn-connections-collapsed'),true);};head.addEventListener('dblclick',function(event){if(!event.target.closest('button'))toggle.click();});apply(saved('fnConnectionsCollapsed')==='true',false);}
function initWebviewAdmin(){if(location.pathname!=='/admin/'||document.querySelector('.fn-webview-admin-panel'))return;var nav=document.querySelector('.fn-admin-tabs'),grid=document.querySelector('main.page .grid'),sshTab=nav&&nav.querySelector('[data-tab="ssh"]');if(!nav||!grid)return;var tab=button(lang==='de'?'WebView-Credentials':'WebView credentials','fn-admin-tab');tab.dataset.tab='webview';tab.setAttribute('role','tab');tab.id='fn-admin-tab-webview';if(sshTab&&sshTab.nextSibling)nav.insertBefore(tab,sshTab.nextSibling);else nav.appendChild(tab);var panel=document.createElement('section');panel.className='fn-admin-tab-panel fn-admin-system-panel fn-webview-admin-panel';panel.hidden=true;panel.setAttribute('role','tabpanel');panel.setAttribute('aria-labelledby',tab.id);panel.innerHTML='<iframe class="fn-admin-system-frame" title="WebView credentials" src="/admin/webview-profiles.jsp"></iframe>';grid.appendChild(panel);function show(){grid.querySelectorAll('.fn-admin-tab-panel').forEach(function(x){x.hidden=x!==panel;});nav.querySelectorAll('[role="tab"]').forEach(function(x){var active=x===tab;x.classList.toggle('active',active);x.setAttribute('aria-selected',active?'true':'false');x.tabIndex=active?0:-1;});store('fnAdminTab','webview');}tab.onclick=show;nav.addEventListener('click',function(event){var other=event.target.closest('[role="tab"]');if(other&&other!==tab)panel.hidden=true;},true);if(saved('fnAdminTab')==='webview')show();}
function initWebviewDeviceAssignments(){if(location.pathname!=='/admin/')return;var grid=document.querySelector('main.page .grid'),deviceInput=grid&&grid.querySelector('input[name="deviceIp"]'),panel=deviceInput&&deviceInput.closest('section'),table=panel&&panel.querySelector('table');if(!table)return;function refresh(){fetch('/admin/webview-profiles.jsp?format=json',{credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json'}}).then(function(response){if(!response.ok)throw new Error('webProfiles');return response.json();}).then(function(data){var heading=table.querySelector('thead tr'),lastHeading=heading&&heading.lastElementChild;if(heading&&!heading.querySelector('[data-fn-webview-heading]')){var th=document.createElement('th');th.dataset.fnWebviewHeading='true';th.textContent=lang==='de'?'WebView-Profil':'WebView profile';heading.insertBefore(th,lastHeading);}table.querySelectorAll('tbody tr').forEach(function(row){var ipInput=row.querySelector('input[name="deviceIp"]'),submit=row.querySelector('button[type="submit"]'),form=submit&&submit.form;if(!ipInput||!form)return;var cell=row.querySelector('[data-fn-webview-cell]'),select;if(!cell){cell=document.createElement('td');cell.dataset.fnWebviewCell='true';select=document.createElement('select');select.setAttribute('aria-label',(lang==='de'?'WebView-Profil für ':'WebView profile for ')+ipInput.value);cell.appendChild(select);row.insertBefore(cell,row.lastElementChild);}else select=cell.querySelector('select');var hidden=form.querySelector('input[data-fn-webview-value]');if(!hidden){hidden=document.createElement('input');hidden.type='hidden';hidden.name='webCredentialId';hidden.dataset.fnWebviewValue='true';form.appendChild(hidden);}var current=(data.assignments&&data.assignments[ipInput.value])||'',selected=select.value||current;select.textContent='';var none=document.createElement('option');none.value='';none.textContent=lang==='de'?'Keine automatische Anmeldung':'No automatic login';select.appendChild(none);(data.profiles||[]).forEach(function(profile){var option=document.createElement('option');option.value=profile.id;option.textContent=profile.label;select.appendChild(option);});select.value=selected;hidden.value=select.value;select.onchange=function(){hidden.value=select.value;};});}).catch(function(){panel.dataset.fnWebviewProfiles='unavailable';});}window.fnRefreshWebAssignments=refresh;var frame=document.querySelector('.fn-webview-admin-panel iframe');if(frame)frame.addEventListener('load',refresh);refresh();}
function translateAdminDom(root){if(!root)return;var de={'ROLE: ADMIN':'ROLLE: ADMIN','User-Management':'Benutzerverwaltung','SNMP-Credentials':'SNMP-Zugangsdaten','SSH-Credentials':'SSH-Zugangsdaten','WebView-Credentials':'WebView-Zugangsdaten','SSH-Host-Keys':'SSH-Hostschlüssel','SSH-Toolbar':'SSH-Werkzeugleiste','Read Community':'Lese-Community','Write Community (optional)':'Schreib-Community (optional)','Security Level':'Sicherheitsstufe','Key-Passphrase (optional)':'Schlüssel-Passphrase (optional)','Privacy-Protokoll':'Verschlüsselungsprotokoll','Privacy-Passphrase':'Verschlüsselungs-Passphrase'},en={'SNMP-Credentials':'SNMP credentials','SSH-Credentials':'SSH credentials','Key-Passphrase (optional)':'Key passphrase (optional)','WebView-Anmeldeprofile':'WebView login profiles','Passwörter werden AES-GCM-verschlüsselt gespeichert und ausschließlich serverseitig an die Geräte-Weboberfläche gesendet.':'Passwords are stored with AES-GCM encryption and sent to the device web interface only by the server.','Bei einer Aktualisierung leer lassen, um das vorhandene Passwort beizubehalten.':'Leave blank when updating to retain the existing password.','Profil speichern':'Save profile','Vorhandene Profile':'Existing profiles','Profil an Geräte binden':'Assign profile to devices','Das Profil wird beim Öffnen des getunnelten WebViews verwendet. Ohne Zuordnung bleibt die normale Geräte-Anmeldeseite sichtbar.':'The profile is used when opening the tunneled WebView. Without an assignment, the normal device sign-in page remains visible.','Keine automatische Anmeldung':'No automatic login','Zur Bestätigung':'To confirm, enter','eingeben':''},map=lang==='de'?de:en,walker=(root.ownerDocument||document).createTreeWalker(root,NodeFilter.SHOW_TEXT),node;while((node=walker.nextNode())){var value=node.nodeValue.trim();if(Object.prototype.hasOwnProperty.call(map,value))node.nodeValue=node.nodeValue.replace(value,map[value]);if(lang==='de'&&node.nodeValue.indexOf('(Sie)')>=0)node.nodeValue=node.nodeValue.replace('(Sie)','(du)');if(lang==='en'&&node.nodeValue.indexOf('(Sie)')>=0)node.nodeValue=node.nodeValue.replace('(Sie)','(you)');}root.querySelectorAll('form[onsubmit*="confirm"]').forEach(function(form){if(form.querySelector('input[name="credentialId"]'))form.onsubmit=function(){return confirm(lang==='de'?'Profil und seine Zuordnungen wirklich löschen?':'Really delete this profile and its assignments?');};});}
function initAdminLocalization(){if(location.pathname.indexOf('/admin/')!==0)return;translateAdminDom(document.body);document.documentElement.classList.add('fn-admin-page');if(window.self!==window.top){document.documentElement.classList.add('fn-admin-embedded');return;}var frame=document.querySelector('.fn-webview-admin-panel iframe');if(!frame)return;function apply(){try{var doc=frame.contentDocument;if(!doc||!doc.body)return;doc.documentElement.lang=lang;doc.documentElement.setAttribute('data-theme',theme);translateAdminDom(doc.body);var stylesheet=doc.getElementById('fn-admin-shell-style');if(!stylesheet){stylesheet=doc.createElement('link');stylesheet.id='fn-admin-shell-style';stylesheet.rel='stylesheet';stylesheet.href='/assets/app-shell.css?v=20260825-146';doc.head.appendChild(stylesheet);}var style=doc.getElementById('fn-admin-button-shape');if(!style){style=doc.createElement('style');style.id='fn-admin-button-shape';style.textContent='button,.button{border-radius:9px!important}.card{background:var(--fn-panel)!important;color:var(--fn-text)!important;border:1px solid var(--fn-border)!important;border-top:4px solid var(--fn-accent)!important;border-radius:14px!important;box-shadow:var(--fn-shadow)!important}.hint{color:var(--fn-muted)!important}.secret-note{background:color-mix(in srgb,var(--fn-accent) 8%,var(--fn-panel))!important;color:var(--fn-text)!important;border-left-color:var(--fn-accent)!important}';doc.head.appendChild(style);}}catch(error){}}frame.addEventListener('load',apply);apply();}
function translateBulkAdmin(){if(lang!=='en'||location.pathname.indexOf('/admin/')!==0)return;var map={'Gerätezuordnung':'Device assignments','Wähle mehrere Geräte aus, um ihnen dieselben SNMP-, SSH- und WebView-Profile gleichzeitig zuzuweisen.':'Select multiple devices to assign the same SNMP, SSH, and WebView profiles at once.','Bitte wählen':'Please select','Kein Webterminal':'No web terminal','Auswahl zuweisen':'Assign selection','Gerät':'Device','Zuweisen':'Assign','SSH-Hostschlüssel':'SSH host keys','Wähle mehrere Geräte aus, prüfe ihre Hostschlüssel und gib die unabhängig verglichenen Fingerprints anschließend gemeinsam frei.':'Select multiple devices, scan their host keys, and then approve the independently verified fingerprints together.','Die Prüfung kann je nach Anzahl der Geräte einige Sekunden dauern.':'Scanning may take a few seconds depending on the number of devices.','Auswahl prüfen':'Scan selection','Auswahl freigeben':'Approve selection','Freigegebener Fingerprint':'Approved fingerprint','Neu geprüft':'New scan','Aktion':'Action','Nicht freigegeben':'Not approved','Hostschlüssel prüfen':'Scan host key','Fingerprint freigeben':'Approve fingerprint'},walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT),node;while((node=walker.nextNode())){var value=node.nodeValue.trim();if(Object.prototype.hasOwnProperty.call(map,value))node.nodeValue=node.nodeValue.replace(value,map[value]);}document.querySelectorAll('input[aria-label]').forEach(function(input){input.setAttribute('aria-label',input.getAttribute('aria-label').replace('Alle Geräte auswählen','Select all devices').replace('Alle Hostschlüssel auswählen','Select all host keys').replace(' auswählen',' select'));});var approve=document.querySelector('form input[value="approveHostKeys"]');if(approve&&approve.form)approve.form.onsubmit=function(){return window.fnCollectSelection(this,'.fn-hostkey-select')&&confirm('Approve all selected, independently verified fingerprints?');};}
function polishGermanAdmin(){if(lang!=='de'||location.pathname.indexOf('/admin/')!==0)return;var map={'Benutzerdefinierte Toolbar-Buttons':'Benutzerdefinierte Schaltflächen','Button hinzufügen':'Schaltfläche hinzufügen','Host-Key-Prüfung bleibt zwingend aktiv.':'Die Hostschlüsselprüfung bleibt zwingend aktiv.','Host-Key prüfen':'Hostschlüssel prüfen','Auf Factory Default zurücksetzen':'Werkseinstellungen wiederherstellen'},walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT),node;while((node=walker.nextNode())){var value=node.nodeValue.trim();if(map[value])node.nodeValue=node.nodeValue.replace(value,map[value]);else if(value.indexOf('Host-Key-Prüfung bleibt zwingend aktiv.')>=0)node.nodeValue=node.nodeValue.replace('Host-Key-Prüfung bleibt zwingend aktiv.','Die Hostschlüsselprüfung bleibt zwingend aktiv.');}}
function initUpdateAdmin(){if(location.pathname!=='/admin/'||document.querySelector('.fn-update-admin-panel'))return;var nav=document.querySelector('.fn-admin-tabs'),grid=document.querySelector('main.page .grid'),system=nav&&nav.querySelector('[data-tab="system"]');if(!nav||!grid)return;var tab=button('Updates','fn-admin-tab');tab.dataset.tab='updates';tab.setAttribute('role','tab');tab.id='fn-admin-tab-updates';if(system)nav.insertBefore(tab,system);else nav.appendChild(tab);var panel=document.createElement('section');panel.className='fn-admin-tab-panel fn-admin-system-panel fn-update-admin-panel';panel.hidden=true;panel.setAttribute('role','tabpanel');panel.setAttribute('aria-labelledby',tab.id);panel.innerHTML='<iframe class="fn-admin-system-frame" title="Updates" src="/admin/update.jsp"></iframe>';grid.appendChild(panel);function show(){grid.querySelectorAll('.fn-admin-tab-panel').forEach(function(item){item.hidden=item!==panel;});nav.querySelectorAll('[role="tab"]').forEach(function(item){var active=item===tab;item.classList.toggle('active',active);item.setAttribute('aria-selected',active?'true':'false');item.tabIndex=active?0:-1;});store('fnAdminTab','updates');}tab.onclick=show;nav.addEventListener('click',function(event){var other=event.target.closest('[role="tab"]');if(other&&other!==tab)panel.hidden=true;},true);if(saved('fnAdminTab')==='updates')show();}
function translateUpdateAdmin(){if(location.pathname!=='/admin/update.jsp')return;var de={'Release Notes':'Versionshinweise','idle':'Bereit','checking':'Prüfung läuft','available':'Update verfügbar','current':'Aktuell','not-configured':'Nicht konfiguriert','waiting':'Wartet','downloading':'Download läuft','installing':'Installation läuft','success':'Erfolgreich','rollback':'Rollback läuft','error':'Fehler'},en={'Softwareupdate':'Software update','Installierte Version':'Installed version','Neueste Version':'Latest version','Noch nicht geprüft':'Not checked yet','Nicht konfiguriert':'Not configured','Auf Updates prüfen':'Check for updates','Update installieren':'Install update','Installationsstatus':'Installation status','Status des Host-Updaters':'Host updater status','Zielversion':'Target version','Letzte Änderung':'Last change','Wartet':'Waiting','Der Windows-Updater validiert Repository, Release-Version und SHA-256-Digest erneut. Bei einem fehlgeschlagenen Healthcheck wird automatisch auf die vorherige Compose-Konfiguration zurückgerollt.':'The host updater independently validates the repository, release version, and SHA-256 digest. A failed health check automatically restores the previous Compose configuration.','Release Notes':'Release notes','GitHub Update-Token':'GitHub update token','Konfiguriert':'Configured','Das Token wird einmalig über HTTPS übertragen, vom privilegierten Host-Dienst mit eingeschränkten Dateirechten gespeichert und niemals wieder angezeigt oder protokolliert.':'The token is transmitted once over HTTPS, stored by the privileged host service with restricted file permissions, and is never displayed or logged.','Fine-grained GitHub-Token':'Fine-grained GitHub token','Token sicher speichern':'Store token securely','Token entfernen':'Remove token','Nach der ersten erfolgreichen Updateprüfung werden hier die Release Notes des neuesten GitHub-Releases angezeigt.':'The release notes for the latest GitHub release appear here after the first successful update check.','Release auf GitHub öffnen':'View release on GitHub'},map=lang==='de'?de:en,walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT),node;while((node=walker.nextNode())){var value=node.nodeValue.trim();if(Object.prototype.hasOwnProperty.call(map,value))node.nodeValue=node.nodeValue.replace(value,map[value]);}var install=document.querySelector('form input[value="install"]');if(install&&install.form)install.form.onsubmit=function(){return confirm(lang==='de'?'Update jetzt herunterladen, installieren und FabricNavigator neu starten?':'Download and install the update now, then restart FabricNavigator?');};}
function translateUpdateSchedule(){if(location.pathname!=='/admin/update.jsp'||lang!=='en')return;var replacements={'Automatisch alle 6 Stunden auf Updates prüfen':'Automatically check for updates every 6 hours','Einstellung speichern':'Save setting'};document.querySelectorAll('.schedule-form label,.schedule-form button').forEach(function(element){var value=element.textContent.trim();if(replacements[value]){if(element.tagName==='LABEL'){var checkbox=element.querySelector('input');element.textContent='';if(checkbox)element.appendChild(checkbox);element.appendChild(document.createTextNode(' '+replacements[value]));}else element.textContent=replacements[value];}});}
function polishUpdateEnglish(){if(lang!=='en'||location.pathname!=='/admin/update.jsp')return;var status=document.querySelector('.status');if(status&&status.textContent.trim()==='idle')status.textContent='Ready';document.querySelectorAll('.hint').forEach(function(hint){if(hint.textContent.trim().indexOf('Trage FABRICNAVIGATOR_GITHUB_REPOSITORY=')===0)hint.innerHTML='Set <code>FABRICNAVIGATOR_GITHUB_REPOSITORY=OWNER/REPOSITORY</code> in the installation directory\'s <code>.env</code> file, then restart the container.';});document.querySelectorAll('.alert.success,.alert.error').forEach(function(alert){var value=alert.textContent.trim(),match=value.match(/^FabricNavigator ist aktuell \(Version ([^)]+)\)\.$/);if(match)alert.textContent='FabricNavigator is up to date (version '+match[1]+').';else{match=value.match(/^Neue Version (.+) ist verfügbar\.$/);if(match)alert.textContent='New version '+match[1]+' is available.';else if(value.indexOf('Updatepr')===0&&value.indexOf('abgeschlossen')>0)alert.textContent='Update check completed.';}});}
function enhanceCredentialEditor(doc,selectId,formId){var select=doc.getElementById(selectId),form=doc.getElementById(formId);if(!select||!form)return;var section=form.closest('section')||doc.body;section.querySelectorAll('input[name="credentialId"]').forEach(function(input){var deleteForm=input.form,row=deleteForm&&deleteForm.closest('tr'),cell=row&&row.lastElementChild;if(!deleteForm||!cell||cell.querySelector('.fn-credential-edit'))return;var edit=doc.createElement('button');edit.type='button';edit.className='fn-credential-edit small';edit.textContent=lang==='de'?'Bearbeiten':'Edit';edit.setAttribute('aria-label',(lang==='de'?'Profil bearbeiten: ':'Edit profile: ')+(row.cells[0]?row.cells[0].textContent.trim():''));edit.onclick=function(){select.value=input.value;select.dispatchEvent(new doc.defaultView.Event('change',{bubbles:true}));form.scrollIntoView({behavior:'smooth',block:'start'});setTimeout(function(){var label=form.querySelector('input[name="label"]');if(label)label.focus();},260);};cell.insertBefore(edit,deleteForm);cell.style.whiteSpace='nowrap';deleteForm.style.display='inline-block';});}
function initCredentialEditors(){if(location.pathname!=='/admin/')return;enhanceCredentialEditor(document,'snmp-existing','snmp-form');enhanceCredentialEditor(document,'ssh-existing','ssh-form');var frame=document.querySelector('.fn-webview-admin-panel iframe');if(!frame)return;function apply(){try{var doc=frame.contentDocument;if(!doc||!doc.body)return;enhanceCredentialEditor(doc,'web-existing','web-form');var style=doc.getElementById('fn-credential-edit-style');if(!style){style=doc.createElement('style');style.id='fn-credential-edit-style';style.textContent='.fn-credential-edit{margin-right:8px;background:#fff!important;color:#6f18d1!important;border:1px solid #6f18d1!important;border-radius:9px!important}.actions{flex-wrap:wrap}';doc.head.appendChild(style);}}catch(error){}}frame.addEventListener('load',apply);apply();}
function fitAdminFrame(frame){try{var doc=frame.contentDocument;if(!doc||!doc.body)return;doc.documentElement.style.overflow='hidden';doc.body.style.overflow='hidden';var height=Math.max(doc.body.scrollHeight,doc.body.offsetHeight,doc.documentElement.scrollHeight,doc.documentElement.offsetHeight);if(height>0&&Math.abs(frame.offsetHeight-height)>2)frame.style.height=(height+4)+'px';}catch(error){}}
function enhanceUpdateAdminUi(){if(location.pathname!=='/admin/update.jsp')return;var cards=document.querySelectorAll('.update-card');if(cards.length>1){var installation=cards[1],values=Array.prototype.map.call(installation.querySelectorAll('.version-line strong'),function(item){return item.textContent.trim().toLowerCase();}),state=values[0]||'',version=values[1]||'',changed=values[2]||'',notStarted=(state===''||state==='wartet'||state==='waiting'||state==='idle')&&(version===''||version==='—'||version==='-')&&(changed===''||changed==='—'||changed==='-');if(notStarted){installation.hidden=true;cards[0].classList.add('wide');}}
var check=document.querySelector('form input[value="check"]'),form=check&&check.form,submit=form&&form.querySelector('button[type="submit"]');if(!form||!submit)return;var style=document.getElementById('fn-update-check-style');if(!style){style=document.createElement('style');style.id='fn-update-check-style';style.textContent='.fn-update-spinner{display:none;width:15px;height:15px;margin-left:9px;vertical-align:-3px;border:2px solid currentColor;border-right-color:transparent;border-radius:50%;animation:fn-update-spin .7s linear infinite}.fn-update-checking .fn-update-spinner{display:inline-block}@keyframes fn-update-spin{to{transform:rotate(360deg)}}';document.head.appendChild(style);}if(!submit.querySelector('.fn-update-spinner')){var spinner=document.createElement('span');spinner.className='fn-update-spinner';spinner.setAttribute('aria-hidden','true');submit.appendChild(spinner);}form.addEventListener('submit',function(){submit.disabled=true;submit.classList.add('fn-update-checking');submit.setAttribute('aria-busy','true');var text=Array.prototype.find.call(submit.childNodes,function(node){return node.nodeType===3;});if(text)text.nodeValue=(lang==='de'?'Prüfung läuft':'Checking')+' ';});}
function initAdminFrameSizing(){if(location.pathname!=='/admin/')return;document.querySelectorAll('.fn-admin-system-frame').forEach(function(frame){var timer=null;function fit(){clearTimeout(timer);timer=setTimeout(function(){fitAdminFrame(frame);},20);}frame.addEventListener('load',function(){fit();setTimeout(fit,150);setTimeout(fit,700);try{var doc=frame.contentDocument;if(doc&&doc.body&&window.ResizeObserver){var observer=new ResizeObserver(fit);observer.observe(doc.body);observer.observe(doc.documentElement);frame._fnResizeObserver=observer;}}catch(error){}});fit();});}
function initAdminGroups(){if(location.pathname!=='/admin/'||document.querySelector('.fn-admin-credentials-panel'))return;var nav=document.querySelector('.fn-admin-tabs'),grid=document.querySelector('main.page .grid');if(!nav||!grid)return;function original(key){return grid.querySelector('.fn-admin-tab-panel[aria-labelledby="fn-admin-tab-'+key+'"]');}var panels={snmp:original('snmp'),ssh:original('ssh'),webview:original('webview'),toolbar:original('toolbar')};if(!panels.snmp||!panels.ssh||!panels.webview||!panels.toolbar)return;var oldSaved=saved('fnAdminTab')||'users',credentialsTab=button(lang==='de'?'Zugangsdaten':'Credentials','fn-admin-tab'),designTab=button('Design','fn-admin-tab');credentialsTab.dataset.tab='credentials';credentialsTab.id='fn-admin-tab-credentials';credentialsTab.setAttribute('role','tab');designTab.dataset.tab='design';designTab.id='fn-admin-tab-design';designTab.setAttribute('role','tab');var devicesTab=nav.querySelector('[data-tab="devices"]'),updatesTab=nav.querySelector('[data-tab="updates"]'),systemTab=nav.querySelector('[data-tab="system"]');nav.insertBefore(credentialsTab,devicesTab||updatesTab||systemTab||null);nav.insertBefore(designTab,updatesTab||systemTab||null);['snmp','ssh','webview','toolbar'].forEach(function(key){var old=nav.querySelector('[data-tab="'+key+'"]');if(old)old.remove();});function makeGroup(kind,label){var section=document.createElement('section');section.className='fn-admin-tab-panel fn-admin-group-panel fn-admin-'+kind+'-panel';section.hidden=true;section.setAttribute('role','tabpanel');section.setAttribute('aria-labelledby','fn-admin-tab-'+kind);section.innerHTML='<div class="fn-admin-group-heading"><h2>'+label+'</h2></div><nav class="fn-admin-subtabs" role="tablist"></nav><div class="fn-admin-subcontent"></div>';grid.appendChild(section);return section;}var credentials=makeGroup('credentials',lang==='de'?'Zugangsdaten':'Credentials'),design=makeGroup('design','Design'),credentialNav=credentials.querySelector('.fn-admin-subtabs'),credentialContent=credentials.querySelector('.fn-admin-subcontent'),designNav=design.querySelector('.fn-admin-subtabs'),designContent=design.querySelector('.fn-admin-subcontent');[['snmp',lang==='de'?'SNMP-Zugangsdaten':'SNMP credentials'],['ssh',lang==='de'?'SSH-Zugangsdaten':'SSH credentials'],['webview',lang==='de'?'WebView-Zugangsdaten':'WebView credentials']].forEach(function(item){var child=panels[item[0]],tab=button(item[1],'fn-admin-subtab');tab.type='button';tab.dataset.subtab=item[0];tab.setAttribute('role','tab');credentialNav.appendChild(tab);credentialContent.appendChild(child);tab.onclick=function(){activateSub(credentials,item[0],'fnAdminCredentialTab');};});designContent.appendChild(panels.toolbar);var toolbarSub=button(lang==='de'?'SSH-Werkzeugleiste':'SSH toolbar','fn-admin-subtab');toolbarSub.type='button';toolbarSub.dataset.subtab='toolbar';toolbarSub.setAttribute('role','tab');designNav.appendChild(toolbarSub);var imageSub=button(lang==='de'?'Gerätebilder':'Device images','fn-admin-subtab');imageSub.type='button';imageSub.dataset.subtab='images';imageSub.setAttribute('role','tab');designNav.appendChild(imageSub);var imagePanel=document.createElement('section');imagePanel.className='fn-admin-design-images';imagePanel.hidden=true;imagePanel.innerHTML='<h2>'+(lang==='de'?'Gerätebilder in der Topologie':'Topology device images')+'</h2><p>'+(lang==='de'?'Wähle, ob Extreme-Switches mit dem passenden Produktfoto oder mit dem generischen FabricEngine-/SwitchEngine-Bild dargestellt werden.':'Choose whether Extreme switches use their matching product photo or the generic FabricEngine/SwitchEngine image.')+'</p><label>'+(lang==='de'?'Darstellung für Extreme-Switches':'Extreme switch appearance')+'<select><option value="product">'+(lang==='de'?'Passende Produktfotos':'Matching product photos')+'</option><option value="generic">'+(lang==='de'?'Generische Engine-Bilder':'Generic engine images')+'</option></select></label><p class="fn-admin-design-status" role="status"></p>';designContent.appendChild(imagePanel);var imageSelect=imagePanel.querySelector('select'),imageStatus=imagePanel.querySelector('.fn-admin-design-status');imageSelect.value=saved('fnExtremeSwitchImages')==='generic'?'generic':'product';imageSelect.onchange=function(){store('fnExtremeSwitchImages',imageSelect.value==='generic'?'generic':'product');imageStatus.textContent=lang==='de'?'Gespeichert. Die Auswahl wird beim Öffnen der Topologie verwendet.':'Saved. The selection is used when the topology is opened.';};toolbarSub.onclick=function(){activateSub(design,'toolbar','fnAdminDesignTab');};imageSub.onclick=function(){activateSub(design,'images','fnAdminDesignTab');};function activateSub(group,key,storageKey){var content=group.querySelector('.fn-admin-subcontent');Array.prototype.forEach.call(content.children,function(child){var childKey=child===panels.toolbar?'toolbar':child===imagePanel?'images':Object.keys(panels).filter(function(name){return panels[name]===child;})[0];child.hidden=childKey!==key;});group.querySelectorAll('.fn-admin-subtab').forEach(function(tab){var active=tab.dataset.subtab===key;tab.classList.toggle('active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.tabIndex=active?0:-1;});store(storageKey,key);if(key==='webview'||key==='toolbar')window.dispatchEvent(new Event('resize'));}function hideGroupsUnless(tab){if(tab!==credentialsTab)credentials.hidden=true;if(tab!==designTab)design.hidden=true;}function activateGroup(kind){var target=kind==='credentials'?credentials:design,targetTab=kind==='credentials'?credentialsTab:designTab;Array.prototype.forEach.call(grid.children,function(child){if(child.classList&&child.classList.contains('fn-admin-tab-panel'))child.hidden=child!==target;});nav.querySelectorAll(':scope > [role="tab"]').forEach(function(tab){var active=tab===targetTab;tab.classList.toggle('active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.tabIndex=active?0:-1;});target.hidden=false;store('fnAdminTab',kind);var subKey=kind==='credentials'?(saved('fnAdminCredentialTab')||'snmp'):(saved('fnAdminDesignTab')||'images');if(kind==='credentials'&&['snmp','ssh','webview'].indexOf(subKey)<0)subKey='snmp';if(kind==='design'&&['toolbar','images'].indexOf(subKey)<0)subKey='images';activateSub(target,subKey,kind==='credentials'?'fnAdminCredentialTab':'fnAdminDesignTab');}credentialsTab.onclick=function(){activateGroup('credentials');};designTab.onclick=function(){activateGroup('design');};nav.addEventListener('click',function(event){var root=event.target.closest('.fn-admin-tabs > [role="tab"]');if(root)hideGroupsUnless(root);},true);if(['snmp','ssh','webview'].indexOf(oldSaved)>=0){store('fnAdminCredentialTab',oldSaved);activateGroup('credentials');}else if(oldSaved==='toolbar'){store('fnAdminDesignTab','toolbar');activateGroup('design');}else if(oldSaved==='credentials'||oldSaved==='design')activateGroup(oldSaved);}
function initUpdateInstallProgress(){if(location.pathname!=='/admin/update.jsp')return;var installInput=document.querySelector('form input[value="install"]'),installForm=installInput&&installInput.form,grid=document.querySelector('.update-grid'),first=grid&&grid.querySelector('.update-card');if(!installForm||!grid||!first)return;var key='fnUpdateInstallRequestedAt',style=document.createElement('style');style.textContent='.fn-install-progress{grid-column:1/-1;background:#f8f3ff!important}.fn-install-track{height:16px;border-radius:8px;overflow:hidden;background:#e4d8f3;border:1px solid #c9a9ed}.fn-install-value{height:100%;width:0;background:#721fc4;transition:width .45s ease}.fn-install-meta{display:flex;justify-content:space-between;gap:12px;margin-top:10px}.fn-install-message{margin:8px 0 0;min-height:1.5em}.fn-install-restart{color:#5f00c7;font-weight:800}';document.head.appendChild(style);var panel=document.createElement('section');panel.className='update-card fn-install-progress';panel.hidden=true;panel.innerHTML='<h2>'+(lang==='de'?'Update wird installiert':'Installing update')+'</h2><div class="fn-install-track" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="8"><div class="fn-install-value"></div></div><div class="fn-install-meta"><strong class="fn-install-state"></strong><span class="fn-install-percent"></span></div><p class="fn-install-message"></p>';first.insertAdjacentElement('afterend',panel);var track=panel.querySelector('.fn-install-track'),value=panel.querySelector('.fn-install-value'),stateText=panel.querySelector('.fn-install-state'),percentText=panel.querySelector('.fn-install-percent'),message=panel.querySelector('.fn-install-message'),timer=null,failures=0,seenFresh=false;function show(percent,state,detail,restarting){panel.hidden=false;value.style.width=percent+'%';track.setAttribute('aria-valuenow',String(percent));stateText.textContent=state;percentText.textContent=percent+' %';message.textContent=detail||'';message.classList.toggle('fn-install-restart',!!restarting);try{window.parent.postMessage({type:'fn-admin-frame-resize'},location.origin);}catch(ignored){}}function labels(state){var de=lang==='de',map={requested:[8,de?'Update angefordert':'Update requested',de?'Der Host-Updater übernimmt die Anfrage.':'The host updater is accepting the request.'],waiting:[12,de?'Updater wird gestartet':'Starting updater',de?'Die Updateanfrage wartet auf den Host-Updater.':'The update request is waiting for the host updater.'],downloading:[28,de?'Update wird heruntergeladen':'Downloading update',de?'Das verifizierte Updatepaket wird von GitHub geladen.':'The verified update package is being downloaded from GitHub.'],validating:[46,de?'Update wird geprüft':'Validating update',de?'Version und SHA-256-Prüfsumme werden geprüft.':'The version and SHA-256 digest are being verified.'],installing:[68,de?'Update wird installiert':'Installing update',de?'Image und Laufzeitdateien werden installiert.':'The image and runtime files are being installed.'],restarting:[88,de?'Dienst wird neu gestartet':'Restarting service',de?'FabricNavigator startet neu. Die Seite wartet auf den Webserver.':'FabricNavigator is restarting. This page is waiting for the web server.'],success:[100,de?'Update erfolgreich':'Update successful',de?'FabricNavigator wurde aktualisiert. Die Anmeldung wird geöffnet.':'FabricNavigator has been updated. Opening sign-in.'],rollback:[90,de?'Vorherige Version wird wiederhergestellt':'Restoring previous version',de?'Der Healthcheck ist fehlgeschlagen; die vorige Version wird wiederhergestellt.':'The health check failed; the previous version is being restored.'],error:[100,de?'Update fehlgeschlagen':'Update failed',de?'Das Update konnte nicht installiert werden.':'The update could not be installed.']};return map[state]||map.waiting;}function requestedAt(){var raw='';try{raw=sessionStorage.getItem(key)||'';}catch(ignored){}return Number(raw)||0;}function clear(){try{sessionStorage.removeItem(key);}catch(ignored){}}function loginWhenReady(){if(timer)clearInterval(timer);show(90,lang==='de'?'Dienst wird neu gestartet':'Restarting service',lang==='de'?'Warte auf FabricNavigator …':'Waiting for FabricNavigator …',true);var probe=setInterval(function(){fetch('/login.jsp?updated=1',{cache:'no-store',credentials:'same-origin'}).then(function(response){if(!response.ok)throw new Error('not-ready');clearInterval(probe);clear();window.top.location.replace('/login.jsp?next=%2Fadmin%2F&updated=1');}).catch(function(){});},2000);}function poll(){var started=requestedAt();if(!started)return;fetch('/admin/update.jsp?view=status&_='+(Date.now()),{cache:'no-store',credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(response){if(response.url.indexOf('/login.jsp')>=0||!(response.headers.get('content-type')||'').toLowerCase().includes('application/json')){loginWhenReady();throw new Error('login');}if(!response.ok)throw new Error('offline');return response.json();}).then(function(data){failures=0;var changed=Date.parse(data.updatedAt||'')||0;if(changed>=started-1000)seenFresh=true;var state=seenFresh?String(data.state||'waiting').toLowerCase():'requested',label=labels(state);show(label[0],label[1],data.message||label[2],state==='restarting');if(state==='success'){clear();setTimeout(loginWhenReady,1800);}else if(state==='error'){clear();if(timer)clearInterval(timer);} }).catch(function(error){if(error.message==='login')return;failures++;if(failures>=2)loginWhenReady();});}var previous=installForm.onsubmit;installForm.onsubmit=function(event){if(previous&&previous.call(this,event)===false)return false;try{sessionStorage.setItem(key,String(Date.now()));}catch(ignored){}var initial=labels('requested');show(initial[0],initial[1],initial[2],false);return true;};if(requestedAt()){var initial=labels('requested');show(initial[0],initial[1],initial[2],false);poll();timer=setInterval(poll,1200);}}
function initManualTopologyLinks(){if(location.pathname.indexOf('/topology/')!==0||typeof data==='undefined'||typeof renderTopology!=='function')return;var menu=document.querySelector('.fn-node-context-menu');if(!menu)return;function de(){return (document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}function menuItem(label,icon){var item=document.createElement('a'),symbol=document.createElement('span');item.href='#';item.className='fn-node-menu-item fn-manual-link-menu-item';item.setAttribute('role','menuitem');symbol.className='fn-node-menu-icon';symbol.setAttribute('aria-hidden','true');symbol.textContent=icon;item.appendChild(symbol);item.appendChild(document.createTextNode(label));menu.appendChild(item);return item;}var createItem=menuItem(de()?'Manuellen Link erstellen':'Create manual link','＋'),deleteItem=menuItem(de()?'Manuellen Link löschen':'Delete manual link','×'),actions=document.querySelector('.topology-title-actions'),manualButton=button('', 'button secondary fn-manual-link-button'),activeNodes=[],activeLink=null;manualButton.type='button';if(actions)actions.appendChild(manualButton);createItem.hidden=true;deleteItem.hidden=true;var dialog=document.createElement('div');dialog.className='fn-manual-link-dialog';dialog.hidden=true;dialog.innerHTML='<div class="fn-manual-link-card" role="dialog" aria-modal="true" aria-labelledby="fn-manual-link-title"><div class="fn-manual-link-head"><h2 id="fn-manual-link-title"></h2><button type="button" class="fn-manual-link-close" aria-label="Close">×</button></div><p class="fn-manual-link-endpoints"></p><label class="fn-manual-link-field"><span></span><select><option value="vpn">VPN</option></select></label><div class="fn-manual-link-actions"><button type="button" class="secondary fn-manual-link-cancel"></button><button type="button" class="fn-manual-link-create"></button></div><p class="fn-manual-link-status" role="alert"></p></div>';document.body.appendChild(dialog);var title=dialog.querySelector('h2'),endpoints=dialog.querySelector('.fn-manual-link-endpoints'),typeLabel=dialog.querySelector('.fn-manual-link-field span'),cancel=dialog.querySelector('.fn-manual-link-cancel'),create=dialog.querySelector('.fn-manual-link-create'),close=dialog.querySelector('.fn-manual-link-close'),status=dialog.querySelector('.fn-manual-link-status');function nodeName(id){var node=data&&data.nodes&&data.nodes.filter(function(item){return item.id===id;})[0];return node?(node.name||node.ip||node.id):id;}function applyLabels(){var german=de();createItem.lastChild.nodeValue=german?'Manuellen Link erstellen':'Create manual link';deleteItem.lastChild.nodeValue=german?'Manuellen Link löschen':'Delete manual link';manualButton.textContent=german?'Manueller Link':'Manual link';manualButton.title=german?'Zwei markierte Knoten manuell verbinden':'Manually connect two selected nodes';title.textContent=german?'Manuellen Link erstellen':'Create manual link';typeLabel.textContent=german?'Linktyp':'Link type';cancel.textContent=german?'Abbrechen':'Cancel';create.textContent=german?'Link erstellen':'Create link';close.setAttribute('aria-label',german?'Dialog schließen':'Close dialog');}function hideMenu(){menu.hidden=true;createItem.hidden=true;deleteItem.hidden=true;}function positionMenu(event){menu.hidden=false;menu.style.left='0px';menu.style.top='0px';var x=Math.min(event.clientX,innerWidth-menu.offsetWidth-8),y=Math.min(event.clientY,innerHeight-menu.offsetHeight-8);menu.style.left=Math.max(8,x)+'px';menu.style.top=Math.max(8,y)+'px';}function openDialog(nodes){activeNodes=nodes;applyLabels();status.textContent='';endpoints.textContent=nodeName(nodes[0].dataset.nodeId)+' ↔ '+nodeName(nodes[1].dataset.nodeId);dialog.hidden=false;dialog.querySelector('select').focus();}function selectedPair(){var selected=Array.prototype.slice.call(document.querySelectorAll('#topology-graph .node.fn-selected'));return selected.length===2?selected:[];}function closeDialog(){dialog.hidden=true;status.textContent='';}manualButton.addEventListener('click',function(){var nodes=selectedPair();if(nodes.length!==2){window.alert(de()?'Markiere zuerst genau zwei Knoten mit Strg + linker Maustaste.':'First select exactly two nodes with Ctrl + left click.');return;}openDialog(nodes);});document.addEventListener('contextmenu',function(event){var node=event.target.closest&&event.target.closest('#topology-graph .node'),linkTarget=event.target.closest&&event.target.closest('#topology-graph .link.vpn-link, .connection-row.vpn-row');deleteItem.hidden=true;if(node){var selected=selectedPair(),candidates=selected.length===2&&selected.indexOf(node)>=0?selected:[];createItem.hidden=candidates.length!==2;if(candidates.length===2){var allValid=candidates.every(function(candidate){return /^(?:\d{1,3}\.){3}\d{1,3}$/.test(candidate.dataset.fnIp||'');});if(!allValid)Array.prototype.forEach.call(menu.children,function(child){if(child!==createItem)child.hidden=true;});activeNodes=candidates;activeLink=null;event.preventDefault();positionMenu(event);}return;}createItem.hidden=true;if(linkTarget){var index=parseInt(linkTarget.getAttribute('data-link-index'),10),view=isFinite(index)&&typeof linkViews!=='undefined'?linkViews[index]:null;if(view&&view.link&&view.link.manual===true){activeLink=view.link;deleteItem.hidden=false;Array.prototype.forEach.call(menu.children,function(child){if(child!==deleteItem)child.hidden=true;});event.preventDefault();positionMenu(event);}}},true);createItem.addEventListener('click',function(event){event.preventDefault();var nodes=activeNodes.slice();hideMenu();if(nodes.length===2)openDialog(nodes);});deleteItem.addEventListener('click',function(event){event.preventDefault();var link=activeLink;hideMenu();if(!link)return;var question=de()?'Diesen manuellen VPN-Link wirklich löschen?':'Delete this manual VPN link?';if(!window.confirm(question))return;data.links=data.links.filter(function(item){return item!==link;});activeLink=null;renderTopology();if(typeof setStorageStatus==='function')setStorageStatus(de()?'Manueller VPN-Link gelöscht.':'Manual VPN link deleted.');});create.addEventListener('click',function(){if(activeNodes.length!==2)return;var source=activeNodes[0].dataset.nodeId,target=activeNodes[1].dataset.nodeId,type=dialog.querySelector('select').value;if(!source||!target||source===target)return;var exists=data.links.some(function(link){return link.manual===true&&link.manualType===type&&((link.source===source&&link.target===target)||(link.source===target&&link.target===source));});if(exists){status.textContent=de()?'Zwischen diesen Geräten besteht bereits ein manueller VPN-Link.':'A manual VPN link already exists between these devices.';return;}data.links.push({source:source,target:target,sourcePort:'VPN',targetPort:'VPN',speedMbps:0,poe:false,isis:false,fabricAttach:false,manual:true,manualType:type});closeDialog();renderTopology();if(typeof setStorageStatus==='function')setStorageStatus(de()?'Manueller VPN-Link erstellt. Speichere die Topologie, um ihn dauerhaft zu sichern.':'Manual VPN link created. Save the topology to keep it permanently.');});cancel.addEventListener('click',closeDialog);close.addEventListener('click',closeDialog);dialog.addEventListener('pointerdown',function(event){if(event.target===dialog)closeDialog();});document.addEventListener('keydown',function(event){if(event.key==='Escape'&&!dialog.hidden)closeDialog();});var legends=document.querySelector('.link-legends'),vpnLegend=document.createElement('p');vpnLegend.className='vpn-legend hidden';vpnLegend.innerHTML='<span class="vpn-legend-sample" aria-hidden="true"></span><span>VPN</span>';if(legends)legends.appendChild(vpnLegend);var previousRender=renderTopology;renderTopology=function(){previousRender();var visible=!!(data&&data.links&&data.links.some(function(link){return link.manual===true&&link.manualType==='vpn';}));vpnLegend.classList.toggle('hidden',!visible);rows.querySelectorAll('.connection-row.vpn-row').forEach(function(row){row.title=de()?'Manueller VPN-Link':'Manual VPN link';});};applyLabels();if(data)renderTopology();}
function initManualLinkMenuVisibility(){if(location.pathname.indexOf('/topology/')!==0)return;var menu=document.querySelector('.fn-node-context-menu'),createItem=menu&&menu.querySelector('.fn-manual-link-menu-item');if(!menu||!createItem)return;function german(){return (document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}function selectedPair(){return document.querySelectorAll('#topology-graph .node.fn-selected').length===2;}function position(event){menu.hidden=false;menu.style.left='0px';menu.style.top='0px';var x=Math.min(event.clientX,innerWidth-menu.offsetWidth-8),y=Math.min(event.clientY,innerHeight-menu.offsetHeight-8);menu.style.left=Math.max(8,x)+'px';menu.style.top=Math.max(8,y)+'px';}document.addEventListener('contextmenu',function(event){var node=event.target.closest&&event.target.closest('#topology-graph .node');if(!node)return;createItem.hidden=false;if(menu.hidden){Array.prototype.forEach.call(menu.children,function(child){child.hidden=child!==createItem;});event.preventDefault();position(event);}},true);createItem.addEventListener('click',function(event){if(selectedPair())return;event.preventDefault();event.stopImmediatePropagation();menu.hidden=true;window.alert(german()?'Markiere zuerst genau zwei Knoten mit Strg + linker Maustaste und öffne danach erneut das Kontextmenü.':'First select exactly two nodes with Ctrl + left click, then open the context menu again.');},true);}
function initDiscoveryProgress(){if(location.pathname.indexOf('/topology/')!==0)return;var form=document.getElementById('topology-form');if(!form||document.querySelector('.fn-discovery-progress'))return;var overlay=document.createElement('div');overlay.className='fn-discovery-progress';overlay.hidden=true;overlay.innerHTML='<section class="fn-discovery-progress-card" role="status" aria-live="polite" aria-atomic="true"><div class="fn-discovery-spinner" aria-hidden="true"></div><p class="fn-discovery-kicker"></p><h2 class="fn-discovery-title"></h2><p class="fn-discovery-message"></p><div class="fn-discovery-track" aria-hidden="true"><span></span></div><p class="fn-discovery-elapsed"></p><button type="button" class="fn-discovery-cancel"></button></section>';document.body.appendChild(overlay);var kicker=overlay.querySelector('.fn-discovery-kicker'),title=overlay.querySelector('.fn-discovery-title'),message=overlay.querySelector('.fn-discovery-message'),elapsed=overlay.querySelector('.fn-discovery-elapsed'),cancel=overlay.querySelector('.fn-discovery-cancel'),submit=form.querySelector('button[type="submit"]'),timer=null,submitTimer=null;function german(){return (document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}function labels(seconds){var isDe=german(),step=seconds<4?0:seconds<12?1:seconds<28?2:seconds<50?3:4,messages=isDe?['Discovery wird vorbereitet …','Seed-Geräte werden per SNMP abgefragt …','LLDP-Nachbarn und Verbindungen werden erkannt …','Geräteinformationen und Linkeigenschaften werden ausgewertet …','Topologie wird aufgebaut. Dies kann bei vielen Geräten etwas dauern …']:['Preparing discovery …','Querying seed devices through SNMP …','Discovering LLDP neighbors and connections …','Evaluating device information and link properties …','Building the topology. Large networks may take a little longer …'];kicker.textContent=isDe?'NETZWERKERKENNUNG':'NETWORK DISCOVERY';title.textContent=isDe?'Discovery läuft':'Discovery in progress';message.textContent=messages[step];elapsed.textContent=(isDe?'Verstrichene Zeit: ':'Elapsed time: ')+seconds+' s';cancel.textContent=isDe?'Discovery abbrechen':'Cancel discovery';cancel.setAttribute('aria-label',cancel.textContent);}function reset(){if(timer){window.clearInterval(timer);timer=null;}if(submitTimer){window.clearTimeout(submitTimer);submitTimer=null;}window.stop();overlay.hidden=true;document.body.classList.remove('fn-discovery-running');delete form.dataset.fnDiscoverySubmitting;if(submit){submit.disabled=false;submit.removeAttribute('aria-busy');}}cancel.addEventListener('click',reset);form.addEventListener('submit',function(event){event.preventDefault();if(form.dataset.fnDiscoverySubmitting==='true')return;form.dataset.fnDiscoverySubmitting='true';overlay.hidden=false;document.body.classList.add('fn-discovery-running');if(submit){submit.disabled=true;submit.setAttribute('aria-busy','true');}var started=Date.now();labels(0);timer=window.setInterval(function(){labels(Math.floor((Date.now()-started)/1000));},1000);submitTimer=window.setTimeout(function(){submitTimer=null;HTMLFormElement.prototype.submit.call(form);},90);});window.addEventListener('pagehide',function(){if(timer)window.clearInterval(timer);if(submitTimer)window.clearTimeout(submitTimer);});}
var topologyEnhanceBaseV114=topologyEnhance;topologyEnhance=function(){topologyEnhanceBaseV114();var graph=document.getElementById('topology-graph');if(!graph)return;var ns='http://www.w3.org/2000/svg',xlink='http://www.w3.org/1999/xlink';graph.querySelectorAll('.node.type-group').forEach(function(node){var image=node.querySelector('.fn-node-front');if(!image){image=document.createElementNS(ns,'image');node.insertBefore(image,node.querySelector('text:not(.sub):not(.edm-link)'));}image.setAttribute('href','/assets/topology-group.svg');image.setAttributeNS(xlink,'xlink:href','/assets/topology-group.svg');image.setAttribute('x','-36');image.setAttribute('y','-43');image.setAttribute('width','72');image.setAttribute('height','72');image.setAttribute('preserveAspectRatio','xMidYMid meet');image.setAttribute('class','fn-node-front fn-topology-group-image');node.classList.add('fn-node-has-photo','fn-node-group-card');node.dataset.fnInsetX='78';node.dataset.fnInsetTop='50';node.dataset.fnInsetBottom='50';var label=node.querySelector('text:not(.sub):not(.edm-link)');if(label)label.setAttribute('y','39');var hit=node.querySelector('.fn-node-hitbox');if(!hit){hit=document.createElementNS(ns,'rect');hit.setAttribute('class','fn-node-hitbox');node.insertBefore(hit,image);}hit.setAttribute('x','-78');hit.setAttribute('y','-50');hit.setAttribute('width','156');hit.setAttribute('height','100');hit.setAttribute('rx','12');function openGroupFromNode(event){var groups=window.FabricNavigatorTopologyGroups,id=String(node.dataset.nodeId||'').replace(/^fn-group-/,'');if(!groups||typeof groups.openGroup!=='function'||!id)return false;event.preventDefault();event.stopPropagation();groups.openGroup(id);return true;}node.addEventListener('pointerdown',function(event){if(event.button!==0)return;var id=String(node.dataset.nodeId||''),now=Date.now(),previous=graph._fnGroupPointer||{};graph._fnGroupPointer={id:id,at:now};if(previous.id===id&&now-Number(previous.at||0)<=500){event.stopImmediatePropagation();openGroupFromNode(event);graph._fnGroupPointer=null;}},true);node.addEventListener('dblclick',openGroupFromNode);});function syncEnhancedGeometry(){if(typeof updateAllGeometry==='function'){updateAllGeometry();return;}if(typeof linkViews!=='undefined'&&typeof updateLinkGeometry==='function'){linkViews.forEach(updateLinkGeometry);if(typeof updateMltGeometry==='function')updateMltGeometry();if(typeof resolvePortBadgeOverlaps==='function')resolvePortBadgeOverlaps();}}syncEnhancedGeometry();window.requestAnimationFrame(syncEnhancedGeometry);window.setTimeout(syncEnhancedGeometry,120);};
function initTopologySearchAndGroups(){
 if(location.pathname.indexOf('/topology/')!==0||!document.getElementById('topology-graph')||window.FabricNavigatorTopologyGroups)return;
 var GROUP_KEY='fnTopologyGroupsV1',groups=[],activeGroupId=null,result=document.getElementById('topology-result'),titleRow=result&&result.querySelector('.topology-title-row'),nameInput=document.getElementById('topology-name'),savedSelect=document.getElementById('saved-topologies'),saveButton=document.getElementById('save-topology'),loadButton=document.getElementById('load-topology'),deleteButton=document.getElementById('delete-topology'),svg=document.getElementById('topology-graph'),wrap=svg&&svg.parentElement;
 if(!result||!titleRow||!svg)return;
 function german(){return (document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}
 function topologyKey(){if(window.demoActive)return'__demo__';var value=(nameInput&&nameInput.value||savedSelect&&savedSelect.value||'').trim();return value||'__working__';}
 function readAll(){try{var value=JSON.parse(saved(GROUP_KEY)||'{}');return value&&typeof value==='object'&&!Array.isArray(value)?value:{};}catch(e){return {};}}
 function sanitize(list){var raw=window.data,ids={};if(raw&&Array.isArray(raw.nodes))raw.nodes.forEach(function(node){ids[node.id]=true;});return(Array.isArray(list)?list:[]).map(function(group){return{id:String(group.id||'').slice(0,80),name:String(group.name||'').slice(0,50),nodeIds:(Array.isArray(group.nodeIds)?group.nodeIds:[]).filter(function(id,index,all){return ids[id]&&all.indexOf(id)===index;}),collapsed:group.collapsed===true};}).filter(function(group){return group.id&&group.name&&group.nodeIds.length>1;});}
function loadGroups(defaultGroups){var stored=Array.isArray(defaultGroups)?undefined:readAll()[topologyKey()];groups=sanitize(stored===undefined?defaultGroups:stored);activeGroupId=null;renderGroupList();renderLevel();}
 function persist(){var all=readAll();all[topologyKey()]=groups.map(function(group){return{id:group.id,name:group.name,nodeIds:group.nodeIds.slice(),collapsed:group.collapsed===true};});store(GROUP_KEY,JSON.stringify(all));}
 var tools=document.createElement('div');tools.className='fn-topology-tools';tools.setAttribute('role','toolbar');tools.setAttribute('aria-label',german()?'Topologie-Werkzeuge':'Topology tools');tools.innerHTML='<div class="fn-topology-search"><span class="fn-search-icon" aria-hidden="true">⌕</span><input type="search" autocomplete="off"><button type="button" class="fn-shell-button fn-topology-search-button"></button><span class="fn-topology-search-status" role="status"></span></div><div class="fn-topology-command-actions"></div><div class="fn-topology-group-actions"><button type="button" class="fn-shell-button fn-topology-group-create"></button><span class="fn-topology-group-hint"></span></div><div class="fn-topology-level" hidden><button type="button" class="fn-shell-button fn-topology-level-back"></button><strong></strong></div><div class="fn-topology-group-list"></div>';
 titleRow.insertAdjacentElement('afterend',tools);var titleActions=titleRow.querySelector('.topology-title-actions'),commandActions=tools.querySelector('.fn-topology-command-actions'),labelToggle=document.querySelector('.fn-label-toggle'),searchHost=tools.querySelector('.fn-topology-search'),groupActionsHost=tools.querySelector('.fn-topology-group-actions'),searchStatusHost=tools.querySelector('.fn-topology-search-status');if(searchHost&&groupActionsHost)searchHost.insertBefore(groupActionsHost,searchStatusHost||null);if(labelToggle){labelToggle.classList.add('fn-topology-label-control');commandActions.appendChild(labelToggle);}if(titleActions)while(titleActions.firstChild){var action=titleActions.firstChild;if(action.nodeType===1&&action.matches('button')){action.classList.remove('button','secondary');action.classList.add('fn-shell-button');}commandActions.appendChild(action);}titleRow.remove();
 var search=tools.querySelector('input'),searchButton=tools.querySelector('.fn-topology-search-button'),searchStatus=tools.querySelector('.fn-topology-search-status'),groupButton=tools.querySelector('.fn-topology-group-create'),groupHint=tools.querySelector('.fn-topology-group-hint'),groupList=tools.querySelector('.fn-topology-group-list'),levelBox=tools.querySelector('.fn-topology-level'),levelBack=tools.querySelector('.fn-topology-level-back'),levelName=levelBox.querySelector('strong'),matches=[],matchIndex=-1;
 function applyLabels(){var de=german();search.placeholder=de?'Gerät nach Name oder IP suchen':'Search device by name or IP';search.setAttribute('aria-label',search.placeholder);searchButton.textContent=de?'Suchen':'Search';groupButton.textContent=de?'Auswahl gruppieren':'Group selection';groupHint.textContent=de?'Markiere mindestens zwei Knoten. Gruppen per Doppelklick öffnen.':'Select at least two nodes. Double-click a group to open it.';levelBack.textContent=de?'← Übergeordnete Topologie':'← Parent topology';renderGroupList();renderLevel();}
function renderGroupList(){if(!groupList)return;groupList.textContent='';groups.forEach(function(group){var chip=document.createElement('div'),open=document.createElement('button'),remove=document.createElement('button');chip.className='fn-topology-group-chip';open.type='button';open.className='fn-topology-group-toggle';open.textContent='▣ '+group.name+' · '+group.nodeIds.length;open.title=german()?'Gruppe öffnen':'Open group';open.onclick=function(){openGroup(group.id);};remove.type='button';remove.className='fn-topology-group-remove';remove.textContent='×';remove.title=german()?'Gruppe auflösen':'Remove group';remove.setAttribute('aria-label',remove.title);remove.onclick=function(){var message=german()?'Gruppe „'+group.name+'“ auflösen? Die '+group.nodeIds.length+' Geräte werden wieder einzeln in der Topologie angezeigt.':'Remove group “'+group.name+'”? Its '+group.nodeIds.length+' devices will become individually visible in the topology again.',title=german()?'Gruppe auflösen':'Remove group',confirmation=window.fnPageConfirm?window.fnPageConfirm(message,title):Promise.resolve(false);confirmation.then(function(ok){if(!ok)return;var groupId=group.id;groups=groups.filter(function(item){return item.id!==groupId;});if(activeGroupId===groupId)activeGroupId=null;persist();renderGroupList();renderLevel();window.renderTopology();});};chip.appendChild(open);chip.appendChild(remove);groupList.appendChild(chip);});}
 function renderLevel(){var group=groups.filter(function(item){return item.id===activeGroupId;})[0];levelBox.hidden=!group;if(group)levelName.textContent=(german()?'Gruppe: ':'Group: ')+group.name+' · '+group.nodeIds.length;}
 function openGroup(id){if(!groups.some(function(group){return group.id===id;}))return;activeGroupId=id;renderLevel();window.renderTopology();}
 function buildView(raw,visibleNodes,visibleLinks){var memberMap={},synthetic=[],parallelCounts={};groups=sanitize(groups);if(activeGroupId){var active=groups.filter(function(group){return group.id===activeGroupId;})[0];if(!active){activeGroupId=null;renderLevel();}else{var members={};active.nodeIds.forEach(function(id){members[id]=true;});return{nodes:visibleNodes.filter(function(node){return members[node.id];}),links:visibleLinks.filter(function(link){return members[link.source]&&members[link.target];}),expanded:[]};}}visibleLinks.forEach(function(link){var pair=[link.source,link.target].sort().join('\u0000');parallelCounts[pair]=(parallelCounts[pair]||0)+1;});groups.forEach(function(group){var visibleMembers=group.nodeIds.filter(function(id){return visibleNodes.some(function(node){return node.id===id;});});if(!visibleMembers.length)return;var id='fn-group-'+group.id;visibleMembers.forEach(function(member){memberMap[member]=id;});synthetic.push({id:id,ip:'',name:group.name,status:'reachable',credential:'',sysDescr:(german()?'Topologiegruppe':'Topology group')+' · '+visibleMembers.length+' '+(german()?'Geräte':'devices'),fabricEngine:false,sshEnabled:false,type:'group',depth:0,groupId:group.id,memberCount:visibleMembers.length});});var nodes=visibleNodes.filter(function(node){return !memberMap[node.id];}).concat(synthetic),links=[],seen={};visibleLinks.forEach(function(link,index){var source=memberMap[link.source]||link.source,target=memberMap[link.target]||link.target,mapped=source!==link.source||target!==link.target;if(source===target)return;if(!mapped){links.push(Object.assign({},link));return;}var originalPair=[link.source,link.target].sort().join('\u0000'),explicitMlt=/(?:mlt|lag|trunk)/i.test((link.sourcePort||'')+' '+(link.targetPort||'')),preserveParallel=explicitMlt||parallelCounts[originalPair]>1,key=[source,target].sort().join('\u0000')+(preserveParallel?'\u0001'+index:''),existing=seen[key],copy;if(existing){existing.poe=existing.poe||link.poe;existing.isis=existing.isis||link.isis;existing.fabricAttach=existing.fabricAttach||link.fabricAttach;existing.speedMbps=Math.max(Number(existing.speedMbps)||0,Number(link.speedMbps)||0);existing.groupedLinkCount=(existing.groupedLinkCount||1)+1;existing.sourcePort=existing.targetPort=(german()?existing.groupedLinkCount+' Links':existing.groupedLinkCount+' links');return;}copy=Object.assign({},link,{source:source,target:target});if(!preserveParallel){if(source!==link.source)copy.sourcePort=german()?'Gruppe':'Group';if(target!==link.target)copy.targetPort=german()?'Gruppe':'Group';}copy.groupedLinkCount=1;seen[key]=copy;links.push(copy);});return{nodes:nodes,links:links,expanded:[]};}
 function afterRender(graph,view){graph.querySelectorAll('.fn-topology-group-frame').forEach(function(frame){frame.remove();});if(!view||!view.expanded)return;var anchor=graph.querySelector('.link,.node');view.expanded.forEach(function(group){var bounds=null;group.nodeIds.forEach(function(id){var node=graph.querySelector('.node[data-node-id="'+CSS.escape(id)+'"]');if(!node)return;var box,matrix;try{box=node.getBBox();matrix=node.transform.baseVal.consolidate();matrix=matrix&&matrix.matrix;}catch(e){return;}var x=(matrix?matrix.e:0)+box.x,y=(matrix?matrix.f:0)+box.y,right=x+box.width,bottom=y+box.height;if(!bounds)bounds={left:x,top:y,right:right,bottom:bottom};else{bounds.left=Math.min(bounds.left,x);bounds.top=Math.min(bounds.top,y);bounds.right=Math.max(bounds.right,right);bounds.bottom=Math.max(bounds.bottom,bottom);}});if(!bounds)return;var ns='http://www.w3.org/2000/svg',frame=document.createElementNS(ns,'g'),rect=document.createElementNS(ns,'rect'),label=document.createElementNS(ns,'text');frame.setAttribute('class','fn-topology-group-frame');rect.setAttribute('x',String(bounds.left-26));rect.setAttribute('y',String(bounds.top-40));rect.setAttribute('width',String(bounds.right-bounds.left+52));rect.setAttribute('height',String(bounds.bottom-bounds.top+66));rect.setAttribute('rx','18');label.setAttribute('x',String(bounds.left-12));label.setAttribute('y',String(bounds.top-17));label.textContent=group.name+' · '+group.nodeIds.length;frame.appendChild(rect);frame.appendChild(label);graph.insertBefore(frame,anchor);});}
 window.FabricNavigatorTopologyGroups={buildView:buildView,afterRender:afterRender,loadForCurrent:loadGroups,openGroup:openGroup};
 function runSearch(){var query=search.value.trim().toLowerCase(),raw=window.data;if(!query||!raw||!Array.isArray(raw.nodes)){matches=[];matchIndex=-1;searchStatus.textContent='';svg.querySelectorAll('.fn-search-hit').forEach(function(node){node.classList.remove('fn-search-hit');});return;}var nextMatches=raw.nodes.filter(function(node){return String(node.name||'').toLowerCase().indexOf(query)>=0||String(node.ip||'').toLowerCase().indexOf(query)>=0||String(node.id||'').toLowerCase().indexOf(query)>=0;});if(nextMatches.map(function(node){return node.id;}).join('|')!==matches.map(function(node){return node.id;}).join('|')){matches=nextMatches;matchIndex=-1;}if(!matches.length){searchStatus.textContent=german()?'Kein Treffer':'No match';return;}matchIndex=(matchIndex+1)%matches.length;var target=matches[matchIndex],owner=groups.filter(function(group){return group.nodeIds.indexOf(target.id)>=0;})[0];activeGroupId=owner?owner.id:null;renderLevel();window.renderTopology();window.setTimeout(function(){svg.querySelectorAll('.fn-search-hit').forEach(function(node){node.classList.remove('fn-search-hit');});var node=svg.querySelector('.node[data-node-id="'+CSS.escape(target.id)+'"]');if(!node)return;node.classList.add('fn-search-hit');node.focus({preventScroll:true});var nr=node.getBoundingClientRect(),wr=wrap.getBoundingClientRect();wrap.scrollLeft+=nr.left-wr.left-wr.width/2+nr.width/2;wrap.scrollTop+=nr.top-wr.top-wr.height/2+nr.height/2;searchStatus.textContent=(matchIndex+1)+' / '+matches.length;},120);}
searchButton.onclick=runSearch;search.addEventListener('keydown',function(event){if(event.key==='Enter'){event.preventDefault();runSearch();}});search.addEventListener('input',function(){matches=[];matchIndex=-1;if(!search.value.trim())runSearch();});levelBack.onclick=function(){activeGroupId=null;renderLevel();window.renderTopology();};var groupPointer=null;svg.addEventListener('pointerdown',function(event){if(event.button!==0)return;var node=event.target.closest&&event.target.closest('.node.type-group');if(!node){groupPointer=null;return;}var id=String(node.dataset.nodeId||'').replace(/^fn-group-/,''),now=Date.now();if(groupPointer&&groupPointer.id===id&&now-groupPointer.at<=500){event.preventDefault();event.stopImmediatePropagation();groupPointer=null;openGroup(id);return;}groupPointer={id:id,at:now};},true);svg.addEventListener('dblclick',function(event){var node=event.target.closest&&event.target.closest('.node.type-group');if(!node)return;event.preventDefault();openGroup(String(node.dataset.nodeId||'').replace(/^fn-group-/,''));});
 groupButton.onclick=function(){var selected=Array.prototype.slice.call(svg.querySelectorAll('.node.fn-selected')).filter(function(node){return !node.classList.contains('type-group');}),ids=selected.map(function(node){return node.dataset.nodeId;}).filter(Boolean),assigned={};groups.forEach(function(group){group.nodeIds.forEach(function(id){assigned[id]=true;});});if(ids.length<2){if(window.fnPageInfo)window.fnPageInfo(german()?'Markiere mindestens zwei Knoten mit Strg + linker Maustaste oder dem Auswahlrahmen.':'Select at least two nodes with Ctrl + left click or the selection rectangle.',german()?'Auswahl gruppieren':'Group selection');return;}if(ids.some(function(id){return assigned[id];})){if(window.fnPageInfo)window.fnPageInfo(german()?'Mindestens ein ausgewählter Knoten gehört bereits zu einer Gruppe.':'At least one selected node already belongs to a group.',german()?'Auswahl kann nicht gruppiert werden':'Selection cannot be grouped');return;}if(!window.fnPagePrompt)return;window.fnPagePrompt(german()?'Gib einen Namen für die ausgewählten '+ids.length+' Geräte ein.':'Enter a name for the '+ids.length+' selected devices.',german()?'Neue Topologiegruppe':'New topology group','Area '+(groups.length+1)).then(function(name){if(!name||!name.trim())return;groups.push({id:Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,7),name:name.trim().slice(0,50),nodeIds:ids,collapsed:false});persist();renderGroupList();window.renderTopology();});};
 if(saveButton)saveButton.addEventListener('click',function(){window.setTimeout(persist,0);});
 if(loadButton)loadButton.addEventListener('click',function(){window.setTimeout(function(){loadGroups();window.renderTopology();},0);});
 if(deleteButton)deleteButton.addEventListener('click',function(){var key=topologyKey();window.setTimeout(function(){var all=readAll();delete all[key];store(GROUP_KEY,JSON.stringify(all));groups=[];renderGroupList();},0);});
 var languageButton=document.getElementById('language-toggle');if(languageButton)languageButton.addEventListener('click',function(){window.setTimeout(applyLabels,0);});
 loadGroups();applyLabels();window.renderTopology();
}
function initDeviceSearch(){if((location.pathname!=='/devices/'&&location.pathname!=='/devices/index.jsp')||document.querySelector('.fn-device-search'))return;var list=document.getElementById('device-list');if(!list)return;var card=list.closest('.card'),heading=card&&card.querySelector('h2'),box=document.createElement('label');box.className='fn-device-search';box.innerHTML='<span aria-hidden="true">⌕</span><input type="search" autocomplete="off"><strong role="status"></strong>';if(heading)heading.insertAdjacentElement('afterend',box);var input=box.querySelector('input'),status=box.querySelector('strong');function german(){return (document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}function labels(){input.placeholder=german()?'Gerät nach Name oder IP suchen':'Search device by name or IP';input.setAttribute('aria-label',input.placeholder);filter();}function filter(){var query=input.value.trim().toLowerCase(),visible=0,total=0;list.querySelectorAll('.device').forEach(function(row){total++;var name=(row.querySelector('.device-name')||{}).textContent||'',ip=row.dataset.ip||'',match=!query||name.toLowerCase().indexOf(query)>=0||ip.toLowerCase().indexOf(query)>=0;row.hidden=!match;if(match)visible++;});status.textContent=query?visible+' / '+total:'';}input.addEventListener('input',filter);new MutationObserver(function(){if(input.value)filter();}).observe(list,{subtree:true,childList:true,characterData:true});var languageButton=document.getElementById('language');if(languageButton)languageButton.addEventListener('click',function(){window.setTimeout(labels,0);});labels();}
function initManualDevicePanel(){if(location.pathname!=='/devices/'&&location.pathname!=='/devices/index.jsp')return;var form=document.getElementById('device-form'),card=form&&form.closest('aside.card');if(!form||!card||card.querySelector('.fn-manual-device-panel'))return;var heading=card.querySelector('h2'),panel=document.createElement('details'),summary=document.createElement('summary'),label=document.createElement('span'),note=document.createElement('p');panel.className='fn-manual-device-panel';label.dataset.i18n='add';summary.appendChild(label);panel.appendChild(summary);note.className='fn-manual-device-note';panel.appendChild(note);panel.appendChild(form);if(heading)heading.remove();card.classList.add('fn-manual-device-card');card.appendChild(panel);function labels(){var german=(document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;label.textContent=german?'Gerät manuell hinzufügen':'Add device manually';note.textContent=german?'Nur für Geräte, die nicht automatisch durch die Discovery übernommen werden.':'Only for devices that are not added automatically through discovery.';}var languageButton=document.getElementById('language');if(languageButton)languageButton.addEventListener('click',function(){setTimeout(labels,0);});labels();}
function initUpdateInstallExperience(){if(location.pathname!=='/admin/update.jsp')return;var installInput=document.querySelector('form input[value="install"]'),form=installInput&&installInput.form,submit=form&&form.querySelector('button[type="submit"]'),panel=document.querySelector('.fn-install-progress');if(!form||!submit||!panel)return;var previousSubmit=form.onsubmit,confirmed=false,dialog=document.createElement('div');dialog.className='fn-update-confirm';dialog.hidden=true;dialog.innerHTML='<section class="fn-update-confirm-card" role="dialog" aria-modal="true" aria-labelledby="fn-update-confirm-title"><div class="fn-update-confirm-icon" aria-hidden="true">↻</div><h2 id="fn-update-confirm-title"></h2><p></p><div class="fn-update-confirm-actions"><button type="button" class="fn-update-confirm-cancel"></button><button type="button" class="fn-update-confirm-start"></button></div></section>';document.body.appendChild(dialog);var title=dialog.querySelector('h2'),description=dialog.querySelector('p'),cancel=dialog.querySelector('.fn-update-confirm-cancel'),start=dialog.querySelector('.fn-update-confirm-start');function german(){return (document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}function applyLabels(){var de=german();title.textContent=de?'Softwareupdate installieren?':'Install software update?';description.textContent=de?'FabricNavigator lädt das verifizierte Update herunter, installiert es und startet den Dienst neu. Die Seite wird anschließend automatisch aktualisiert.':'FabricNavigator downloads and installs the verified update, then restarts the service. The page refreshes automatically afterward.';cancel.textContent=de?'Abbrechen':'Cancel';start.textContent=de?'Update starten':'Start update';}function closeDialog(){dialog.hidden=true;}function openDialog(){applyLabels();dialog.hidden=false;start.focus();}form.onsubmit=function(event){if(!confirmed){openDialog();return false;}confirmed=false;var nativeConfirm=window.confirm;window.confirm=function(){return true;};try{return previousSubmit?previousSubmit.call(this,event):true;}finally{window.confirm=nativeConfirm;}};start.addEventListener('click',function(){confirmed=true;closeDialog();if(form.requestSubmit)form.requestSubmit(submit);else form.submit();});cancel.addEventListener('click',closeDialog);dialog.addEventListener('pointerdown',function(event){if(event.target===dialog)closeDialog();});document.addEventListener('keydown',function(event){if(event.key==='Escape'&&!dialog.hidden)closeDialog();});var spinner=document.createElement('div'),kicker=document.createElement('p'),elapsed=document.createElement('p'),refresh=document.createElement('p'),heading=panel.querySelector('h2');spinner.className='fn-discovery-spinner';spinner.setAttribute('aria-hidden','true');kicker.className='fn-update-kicker';elapsed.className='fn-update-elapsed';refresh.className='fn-update-refresh';panel.insertBefore(spinner,panel.firstChild);panel.insertBefore(kicker,heading);panel.appendChild(elapsed);panel.appendChild(refresh);var progressCard=document.createElement("div");progressCard.className="fn-install-progress-card fn-discovery-progress-card";while(panel.firstChild)progressCard.appendChild(panel.firstChild);panel.appendChild(progressCard);var clock=null,refreshAfter=120,lastRefreshKey='fnUpdateLastAutoRefreshAt';function startedAt(){try{return Number(sessionStorage.getItem('fnUpdateInstallRequestedAt')||0)||Date.now();}catch(ignored){return Date.now();}}function updateClock(){if(panel.hidden)return;var seconds=Math.max(0,Math.floor((Date.now()-startedAt())/1000)),remaining=Math.max(0,refreshAfter-seconds),de=german();kicker.textContent=de?'SOFTWAREUPDATE':'SOFTWARE UPDATE';elapsed.textContent=(de?'Verstrichene Zeit: ':'Elapsed time: ')+seconds+' s';refresh.textContent=remaining>0?(de?'Automatische Aktualisierung in etwa ':'Automatic refresh in about ')+remaining+' s':(de?'Die Seite prüft, ob FabricNavigator wieder erreichbar ist …':'Checking whether FabricNavigator is available again …');if(seconds<refreshAfter)return;var last=0;try{last=Number(sessionStorage.getItem(lastRefreshKey)||0)||0;}catch(ignored){}if(Date.now()-last<30000)return;try{sessionStorage.setItem(lastRefreshKey,String(Date.now()));}catch(ignored){}fetch('/health.jsp?_='+(Date.now()),{cache:'no-store',credentials:'same-origin'}).then(function(response){if(!response.ok)throw new Error('not-ready');window.top.location.reload();}).catch(function(){});}function sync(){document.body.classList.toggle('fn-update-running',!panel.hidden);if(!panel.hidden&&!clock){updateClock();clock=setInterval(updateClock,1000);}else if(panel.hidden&&clock){clearInterval(clock);clock=null;}}new MutationObserver(sync).observe(panel,{attributes:true,attributeFilter:['hidden']});sync();}
setTimeout(function(){try{initUpdateInstallExperience();}catch(error){document.documentElement.setAttribute('data-fn-update-experience-error',String(error&&error.message||error));}},0);
function restoreOpenedSessionDialog(dialog,windowSelector,minimizeSelector){if(!dialog)return;var win=dialog.querySelector(windowSelector);dialog.hidden=false;if(win&&win.classList.contains('minimized')){var minimize=dialog.querySelector(minimizeSelector);if(minimize)minimize.click();else{win.classList.remove('minimized');dialog.classList.remove('minimized-shell');}}}
var openSshBeforeAutoRestoreV117=openSsh;
openSsh=function(url,nameHint){openSshBeforeAutoRestoreV117(url,nameHint);var dialog=ensureDialog();restoreOpenedSessionDialog(dialog,'.fn-ssh-window','.fn-dialog-minimize');var parsed=new URL(url,location.href),key=encodeURIComponent(parsed.searchParams.get('device')||'SSH');activate(key);};
var openTunneledWebviewBeforeAutoRestoreV117=openTunneledWebview;
openTunneledWebview=function(device,name){openTunneledWebviewBeforeAutoRestoreV117(device,name);var dialog=ensureWebviewDialog();restoreOpenedSessionDialog(dialog,'.fn-webview-window','.fn-webview-minimize');activateWebview(encodeURIComponent(device));};
function initDiscoveryStepDetails(){if(location.pathname.indexOf('/topology/')!==0)return;var overlay=document.querySelector('.fn-discovery-progress'),message=overlay&&overlay.querySelector('.fn-discovery-message'),elapsed=overlay&&overlay.querySelector('.fn-discovery-elapsed'),track=overlay&&overlay.querySelector('.fn-discovery-track'),bar=track&&track.querySelector('span');if(!overlay||!message||!elapsed||!track||!bar)return;var step=document.createElement('p');step.className='fn-discovery-step';message.insertAdjacentElement('beforebegin',step);track.removeAttribute('aria-hidden');track.setAttribute('role','progressbar');track.setAttribute('aria-valuemin','1');track.setAttribute('aria-valuemax','5');function update(){var match=elapsed.textContent.match(/(\d+)\s*s/),seconds=match?Number(match[1]):0,index=seconds<4?1:seconds<12?2:seconds<28?3:seconds<50?4:5,isDe=(document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;step.textContent=(isDe?'Schritt ':'Step ')+index+'/5';step.setAttribute('aria-label',(isDe?'Discovery-Schritt ':'Discovery step ')+index+(isDe?' von 5':' of 5'));track.setAttribute('aria-valuenow',String(index));track.setAttribute('aria-valuetext',step.textContent);bar.style.width=(index*20)+'%';}new MutationObserver(update).observe(elapsed,{subtree:true,childList:true,characterData:true});update();}
function initDiscoveryAdmin(){if(location.pathname!=='/admin/'||document.querySelector('.fn-discovery-admin-panel'))return;var nav=document.querySelector('.fn-admin-tabs'),grid=document.querySelector('main.page .grid'),updates=nav&&nav.querySelector('[data-tab="updates"]'),system=nav&&nav.querySelector('[data-tab="system"]');if(!nav||!grid)return;var tab=button('Discovery','fn-admin-tab');tab.dataset.tab='discovery';tab.setAttribute('role','tab');tab.id='fn-admin-tab-discovery';nav.insertBefore(tab,updates||system||null);var panel=document.createElement('section');panel.className='fn-admin-tab-panel fn-admin-system-panel fn-discovery-admin-panel';panel.hidden=true;panel.setAttribute('role','tabpanel');panel.setAttribute('aria-labelledby',tab.id);panel.innerHTML='<iframe class="fn-admin-system-frame" title="Discovery" src="/admin/discovery.jsp"></iframe>';grid.appendChild(panel);function show(){grid.querySelectorAll('.fn-admin-tab-panel').forEach(function(x){x.hidden=x!==panel;});nav.querySelectorAll('[role="tab"]').forEach(function(x){var active=x===tab;x.classList.toggle('active',active);x.setAttribute('aria-selected',active?'true':'false');x.tabIndex=active?0:-1;});store('fnAdminTab','discovery');}tab.onclick=show;nav.addEventListener('click',function(event){var other=event.target.closest('[role="tab"]');if(other&&other!==tab)panel.hidden=true;},true);if(saved('fnAdminTab')==='discovery')show();}
function initCredentialDefaults(){if(location.pathname!=='/admin/')return;function enhance(doc,kind,defaultId,csrf){var panel=kind==='ssh'?document.querySelector('.fn-admin-tab-panel[aria-labelledby="fn-admin-tab-ssh"]'):doc.body,heading=Array.prototype.slice.call(panel.querySelectorAll('h2')).filter(function(node){return kind==='ssh'?/SSH-Credentials/i.test(node.textContent):/WebView-Anmeldeprofile/i.test(node.textContent);})[0],table=heading&&heading.parentElement.querySelector('table');if(!table)return;var note=heading.parentElement.querySelector('.secret-note');if(note&&!note.dataset.defaultNote){note.dataset.defaultNote='1';note.textContent+=' '+(lang==='de'?'Das Standardprofil wird erfolgreich per SNMP erkannten Geräten automatisch zugeordnet.':'The default profile is automatically assigned to devices successfully discovered through SNMP.');}table.querySelectorAll('tbody tr').forEach(function(row){var idInput=row.querySelector('input[name="credentialId"]'),cell=row.lastElementChild;if(!idInput||!cell||cell.querySelector('.fn-default-credential'))return;var id=idInput.value,isDefault=id===defaultId,form=document.createElement('form');form.className='fn-default-credential';form.method='post';form.innerHTML='<input type="hidden" name="csrfToken"><input type="hidden" name="kind"><input type="hidden" name="credentialId"><button class="muted-button small" type="submit"></button>';form.elements.csrfToken.value=csrf;form.elements.kind.value=kind;form.elements.credentialId.value=isDefault?'':id;form.querySelector('button').textContent=isDefault?(lang==='de'?'Standard entfernen':'Clear default'):(lang==='de'?'Als Standard':'Make default');if(isDefault){var badge=document.createElement('span');badge.className='badge';badge.textContent=lang==='de'?'Standard':'Default';row.firstElementChild.appendChild(document.createTextNode(' '));row.firstElementChild.appendChild(badge);}form.onsubmit=function(event){event.preventDefault();var body=new URLSearchParams(new FormData(form));fetch('/admin/credential-defaults.jsp',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'},body:body.toString()}).then(function(response){if(!response.ok)throw new Error();location.reload();}).catch(function(){alert(lang==='de'?'Standardprofil konnte nicht gespeichert werden.':'The default profile could not be saved.');});};var actions=cell.querySelector('.actions');(actions||cell).insertBefore(form,(actions||cell).firstChild);});}
fetch('/admin/credential-defaults.jsp',{credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(response){if(!response.ok)throw new Error();return response.json();}).then(function(settings){enhance(document,'ssh',settings.sshCredentialId||'',settings.csrf||'');var frame=document.querySelector('.fn-webview-admin-panel iframe');if(frame){var apply=function(){try{enhance(frame.contentDocument,'web',settings.webCredentialId||'',settings.csrf||'');}catch(ignored){}};frame.addEventListener('load',apply);apply();}}).catch(function(){});}
function polishAdminDescriptionFields(){if(location.pathname!=='/admin/')return;function polish(root){if(!root)return;root.querySelectorAll('.fn-admin-tab-panel,.fn-toolbar-admin-panel,.fn-admin-design-images,.card').forEach(function(panel){var heading=panel.querySelector(':scope > h2'),description=heading&&heading.nextElementSibling;if(description&&description.tagName==='P'&&!description.classList.contains('alert'))description.classList.add('secret-note');});}polish(document);document.querySelectorAll('.fn-webview-admin-panel iframe,.fn-discovery-admin-panel iframe').forEach(function(frame){var apply=function(){try{polish(frame.contentDocument);}catch(ignored){}};frame.addEventListener('load',apply);apply();});}
function translateAdminEnhancements(){if(location.pathname!=='/admin/'||lang!=='en')return;var map={'Wähle mehrere Geräte aus und weise SNMP-, SSH- oder WebView-Zugangsdaten unabhängig voneinander zu. Nicht ausgewählte Zugangsarten bleiben unverändert.':'Select multiple devices and assign SNMP, SSH, or WebView credentials independently. Credential types not selected remain unchanged.','SNMP zuweisen':'Assign SNMP','SSH zuweisen':'Assign SSH','WebView zuweisen':'Assign WebView','SSH-Zuweisung entfernen':'Remove SSH assignment','WebView-Zuweisung entfernen':'Remove WebView assignment','Geräte nach IP oder Name suchen':'Search devices by IP or name','IP oder Gerätename':'IP or device name','IP-Adresse':'IP address','Gerätename':'Device name','WebView-Profil':'WebView profile','Wähle mehrere Geräte aus, prüfe ihre Hostschlüssel und gib die unabhängig verglichenen Fingerprints anschließend gemeinsam frei. Nicht erreichbare Geräte verhindern die Verarbeitung der übrigen Auswahl nicht.':'Select multiple devices, scan their host keys, and approve the independently verified fingerprints together. Unreachable devices do not prevent the remaining selection from being processed.'};var walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT),node;while((node=walker.nextNode())){var value=node.nodeValue.trim();if(map[value])node.nodeValue=node.nodeValue.replace(value,map[value]);}document.querySelectorAll('[placeholder="IP oder Gerätename"]').forEach(function(input){input.placeholder='IP or device name';});}
function initAdminImageAvailability(){if(location.pathname!=='/admin/')return;var panel=document.querySelector('.fn-admin-design-images'),select=panel&&panel.querySelector('select'),status=panel&&panel.querySelector('.fn-admin-design-status');if(!select)return;var product=select.querySelector('option[value="product"]');fetch('/profile/plugin-status.jsp',{credentials:'same-origin',cache:'no-store'}).then(function(response){if(!response.ok)throw new Error();return response.json();}).then(function(data){var installed=!!data.installed;if(product)product.disabled=!installed;if(!installed){select.value='generic';if(saved('fnExtremeSwitchImages')!=='generic')store('fnExtremeSwitchImages','generic');if(status)status.textContent=lang==='de'?'Ohne installiertes Bilderpaket werden generische Engine-Bilder verwendet.':'Generic engine images are used until a device image package is installed.';}else{select.value=saved('fnExtremeSwitchImages')==='generic'?'generic':'product';}}).catch(function(){if(product)product.disabled=true;select.value='generic';});}
function initUserProfileNavigation(){var menu=document.querySelector('.fn-main-menu');if(!menu)return;menu.querySelectorAll(':scope > a[href="/profile/"]').forEach(function(link){link.remove();});initMissingAccountControls();}
function initMissingAccountControls(){var menu=document.querySelector('.fn-main-menu');if(!menu)return;if(!menu.querySelector('.fn-theme-toggle,#theme-toggle')){var themeButton=button('','fn-theme-toggle');themeButton.addEventListener('click',toggleTheme);menu.appendChild(themeButton);setIconButton(themeButton,'theme');}if(!menu.querySelector('.fn-language-toggle,#language-toggle,#language')){var languageButton=button('','fn-language-toggle');languageButton.addEventListener('click',toggleLanguage);menu.appendChild(languageButton);setIconButton(languageButton,'language');}}
function initUserAccountMenu(){if(window.self!==window.top||location.pathname.indexOf('/terminal/')===0)return;var menu=document.querySelector('.fn-main-menu');if(!menu)return;function german(){return(document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}function build(){if(menu.querySelector('.fn-account-menu'))return true;var logout=menu.querySelector('form[action*="logout"]'),badge=menu.querySelector('.fn-current-user');if(!logout||!badge)return false;var account=document.createElement('div'),trigger=document.createElement('button'),panel=document.createElement('div'),profile=document.createElement('a');account.className='fn-account-menu';trigger.type='button';trigger.className='fn-shell-button fn-account-trigger';trigger.setAttribute('aria-haspopup','true');trigger.setAttribute('aria-expanded','false');trigger.innerHTML='<span class="fn-account-label"></span><span class="fn-account-caret" aria-hidden="true">▾</span>';trigger.querySelector('.fn-account-label').textContent=badge.textContent;trigger.title=badge.title||badge.textContent;panel.className='fn-account-panel';panel.hidden=true;profile.href='/profile/';profile.className='fn-account-profile';profile.textContent=german()?'Profil':'Profile';if(location.pathname.indexOf('/profile/')===0)profile.classList.add('active');logout.classList.add('fn-account-logout');var logoutButton=logout.querySelector('button');if(logoutButton)logoutButton.textContent=german()?'Abmelden':'Sign out';panel.appendChild(profile);panel.appendChild(logout);account.appendChild(trigger);account.appendChild(panel);badge.remove();var theme=menu.querySelector('.fn-theme-toggle');menu.insertBefore(account,theme||null);function close(){account.classList.remove('open');panel.hidden=true;trigger.setAttribute('aria-expanded','false');}trigger.addEventListener('click',function(event){event.preventDefault();event.stopPropagation();var open=panel.hidden;document.querySelectorAll('.fn-account-menu.open').forEach(function(other){if(other!==account){other.classList.remove('open');var otherPanel=other.querySelector('.fn-account-panel'),otherTrigger=other.querySelector('.fn-account-trigger');if(otherPanel)otherPanel.hidden=true;if(otherTrigger)otherTrigger.setAttribute('aria-expanded','false');}});panel.hidden=!open;account.classList.toggle('open',open);trigger.setAttribute('aria-expanded',open?'true':'false');});document.addEventListener('pointerdown',function(event){if(!account.contains(event.target))close();},true);document.addEventListener('keydown',function(event){if(event.key==='Escape')close();});return true;}if(build())return;var observer=new MutationObserver(function(){if(build())observer.disconnect();});observer.observe(menu,{childList:true,subtree:true});window.setTimeout(function(){observer.disconnect();build();},4000);}
function removeLegacyAdminDesign(){if(location.pathname!=='/admin/')return;var tab=document.querySelector('.fn-admin-tabs [data-tab="design"]'),panel=document.querySelector('.fn-admin-design-panel');if(!tab&&!panel)return;var active=tab&&tab.classList.contains('active');if(tab)tab.remove();if(panel)panel.remove();if(active||saved('fnAdminTab')==='design'||saved('fnAdminTab')==='toolbar'){store('fnAdminTab','users');var users=document.querySelector('.fn-admin-tabs [data-tab="users"]');if(users)users.click();}}
function relocateAdminActionAlert(){if(location.pathname!=='/admin/')return;document.querySelectorAll('.alert[data-fn-alert-target="hostkeys"]').forEach(function(alert){var panel=document.querySelector('.fn-admin-tab-panel[aria-labelledby="fn-admin-tab-hostkeys"]');if(!panel)return;alert.classList.add('fn-admin-inline-alert');panel.insertBefore(alert,panel.firstChild);});}
function initAdminManagedDeviceTables(){if(location.pathname!=='/admin/')return;var tables=Array.prototype.slice.call(document.querySelectorAll('.fn-managed-device-table'));if(!tables.length)return;window.fnToggleVisibleSelection=function(source,selector){document.querySelectorAll(selector).forEach(function(box){var row=box.closest('tr');if(row&&!row.hidden)box.checked=source.checked;});};function ipValue(value){return String(value||'').split('.').reduce(function(total,item){return total*256+(parseInt(item,10)||0);},0);}function filter(table){var input=document.querySelector('[data-filter-table="'+table.dataset.managedTable+'"]'),query=input?input.value.trim().toLowerCase():'',visible=0;table.querySelectorAll('tbody tr').forEach(function(row){var match=!query||row.dataset.ip.toLowerCase().indexOf(query)>=0||row.dataset.name.toLowerCase().indexOf(query)>=0;row.hidden=!match;if(match)visible++;});if(input)input.setAttribute('data-visible-count',String(visible));}tables.forEach(function(table){var input=document.querySelector('[data-filter-table="'+table.dataset.managedTable+'"]');if(input)input.addEventListener('input',function(){filter(table);});table.querySelectorAll('.fn-sort-button').forEach(function(button){button.addEventListener('click',function(){var key=button.dataset.sort,direction=button.dataset.direction==='asc'?'desc':'asc',body=table.tBodies[0],rows=Array.prototype.slice.call(body.rows);table.querySelectorAll('.fn-sort-button').forEach(function(item){delete item.dataset.direction;item.removeAttribute('aria-sort');});button.dataset.direction=direction;button.setAttribute('aria-sort',direction==='asc'?'ascending':'descending');rows.sort(function(left,right){var a=key==='ip'?ipValue(left.dataset.ip):left.dataset.name.toLowerCase(),b=key==='ip'?ipValue(right.dataset.ip):right.dataset.name.toLowerCase(),result=a<b?-1:a>b?1:0;return direction==='asc'?result:-result;});rows.forEach(function(row){body.appendChild(row);});});});});var pending={},queue=[],active=0;function requestName(ip){if(pending[ip])return pending[ip];pending[ip]=new Promise(function(resolve){queue.push({ip:ip,resolve:resolve});pump();});return pending[ip];}function pump(){while(active<3&&queue.length){let item=queue.shift();active++;fetch('/snmp/get-variables.jsp',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'},body:new URLSearchParams({command:'getVariables',ipAddr:item.ip,varNames:'sysName.0'}).toString()}).then(function(response){if(!response.ok)throw new Error();return response.text();}).then(function(text){var name='';text.split(/\r?\n/).some(function(line){var split=line.indexOf(':');if(split>0&&/^sysName(?:\.0)?$/.test(line.slice(0,split))){name=line.slice(split+1).trim();return true;}return false;});item.resolve(name);}).catch(function(){item.resolve('');}).finally(function(){active--;pump();});}}var unique={};tables.forEach(function(table){table.querySelectorAll('tbody tr[data-ip]').forEach(function(row){var ip=row.dataset.ip;(unique[ip]||(unique[ip]=[])).push(row);});});Object.keys(unique).forEach(function(ip){requestName(ip).then(function(name){unique[ip].forEach(function(row){row.dataset.name=name;var cell=row.querySelector('.fn-managed-device-name');if(cell)cell.textContent=name||'—';var table=row.closest('table');if(table)filter(table);});});});}
function restoreScannedHostKeySelection(){if(location.pathname!=='/admin/')return;var table=document.querySelector('[data-managed-table="host-keys"]');if(!table)return;table.querySelectorAll('tbody tr').forEach(function(row){var cells=row.cells,checkbox=row.querySelector('.fn-hostkey-select');if(checkbox&&cells.length>5&&cells[5].textContent.trim()!=='—')checkbox.checked=true;});}
function enforceRoleNavigation(){if(window.self!==window.top)return;var onAdminPage=location.pathname.indexOf('/admin/')===0,serverAdminLink=onAdminPage||!!document.querySelector('a.fn-promoted-main-link[href^="/admin/"]'),pendingLinks=Array.prototype.slice.call(document.querySelectorAll('.fn-main-menu a[href^="/admin/"]'));function removeAdminLinks(){document.querySelectorAll('.fn-main-menu a[href^="/admin/"],a.fn-promoted-main-link[href^="/admin/"]').forEach(function(link){link.remove();});}if(!serverAdminLink)pendingLinks.forEach(function(link){link.hidden=true;});fetch('/session-info.jsp',{credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json'}}).then(function(response){if(!response.ok)throw new Error();return response.json();}).then(function(info){if(!info.authenticated||String(info.role||'user').toLowerCase()!=='admin'){removeAdminLinks();return;}pendingLinks.forEach(function(link){link.hidden=false;});}).catch(function(){if(!onAdminPage&&!serverAdminLink)removeAdminLinks();});}
function polishCredentialActions(){if(location.pathname!=='/admin/')return;function polish(doc){if(!doc)return;['snmp-existing','ssh-existing','web-existing'].forEach(function(id){var select=doc.getElementById(id),label=select&&select.closest('label');if(label){label.hidden=true;label.classList.add('fn-credential-profile-selector');}});doc.querySelectorAll('.fn-credential-edit').forEach(function(edit){var cell=edit.closest('td');if(cell)cell.classList.add('fn-credential-actions');});}polish(document);var frame=document.querySelector('.fn-webview-admin-panel iframe');if(frame){var apply=function(){try{polish(frame.contentDocument);}catch(ignored){}};frame.addEventListener('load',apply);setTimeout(apply,0);}}
function removeDuplicateWebviewAssignmentColumn(){if(location.pathname!=='/admin/')return;var table=document.querySelector('[data-managed-table="device-assignments"]');if(!table||!table.querySelector('select[name="webCredentialId"]'))return;function clean(){table.querySelectorAll('[data-fn-webview-heading],[data-fn-webview-cell]').forEach(function(item){item.remove();});table.querySelectorAll('input[data-fn-webview-value]').forEach(function(item){item.remove();});}clean();new MutationObserver(clean).observe(table,{childList:true,subtree:true});}
function initTopologyControlTooltips(){if(location.pathname.indexOf('/topology/')!==0||document.querySelector('.fn-control-hover-card'))return;var host=document.querySelector('.fn-topology-tools');if(!host)return;var card=document.createElement('div');card.className='fn-node-hover-card fn-control-hover-card';card.setAttribute('role','tooltip');card.hidden=true;card.innerHTML='<strong></strong><p></p>';document.body.appendChild(card);function german(){return(document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}function supported(control){return control&&control.matches('.fn-label-toggle,#demo-topology,#rediscover-topology,.fn-manual-link-button');}function description(control){if(control.matches('.fn-label-toggle'))return german()?'Schaltet die Beschriftung der Geräte zwischen Gerätename und IP-Adresse um.':'Switches device labels between device name and IP address.';if(control.id==='demo-topology')return german()?'Zeigt eine simulierte Topologie mit Beispielgeräten und verschiedenen Linkarten.':'Shows a simulated topology with example devices and different link types.';if(control.id==='rediscover-topology')return german()?'Erkennt die vorhandene Topologie erneut und behält gespeicherte Knotenpositionen bei.':'Rediscovers the existing topology while preserving saved node positions.';return german()?'Verbindet genau zwei ausgewählte Geräte mit einem manuellen Link.':'Connects exactly two selected devices with a manual link.';}function place(x,y){if(card.hidden)return;var width=card.offsetWidth,height=card.offsetHeight;card.style.left=Math.max(8,Math.min(innerWidth-width-8,x+16))+'px';card.style.top=Math.max(8,Math.min(innerHeight-height-8,y+16))+'px';}function show(control,x,y){if(!supported(control))return;var title=control.getAttribute('title');if(title){control.dataset.fnNativeTitle=title;control.removeAttribute('title');}card.querySelector('strong').textContent=control.textContent.trim()||(control.getAttribute('aria-label')||'');card.querySelector('p').textContent=description(control);card.hidden=false;place(x,y);}host.addEventListener('pointerover',function(event){var control=event.target.closest&&event.target.closest('button');if(supported(control))show(control,event.clientX,event.clientY);});host.addEventListener('pointermove',function(event){if(!card.hidden)place(event.clientX,event.clientY);});host.addEventListener('pointerout',function(event){var control=event.target.closest&&event.target.closest('button');if(supported(control)&&(!event.relatedTarget||!control.contains(event.relatedTarget)))card.hidden=true;});host.addEventListener('focusin',function(event){var control=event.target.closest&&event.target.closest('button');if(!supported(control))return;var rect=control.getBoundingClientRect();show(control,rect.right,rect.top);});host.addEventListener('focusout',function(event){if(supported(event.target))card.hidden=true;});}
function initTopologyDirtyTracking(){if(location.pathname.indexOf('/topology/')!==0||typeof window.renderTopology!=='function'||typeof window.fnMarkTopologyDirty!=='function')return;function signature(){var links=window.data&&Array.isArray(window.data.links)?window.data.links.map(function(link){return[link.source,link.target,link.sourcePort,link.targetPort,Number(link.speedMbps)||0,!!link.poe,!!link.isis,!!link.fabricAttach,!!link.manual,String(link.manualType||'')].join('\u0001');}).sort():[];return JSON.stringify(links)+'\u0002'+(saved('fnTopologyGroupsV1')||'');}var last=signature(),render=window.renderTopology;window.renderTopology=function(){var next=signature();if(next!==last)window.fnMarkTopologyDirty();render();last=next;};document.addEventListener('click',function(event){if(event.target.closest&&event.target.closest('.fn-node-rediscover-menu-item')&&window.fnAllowTopologyUnload)window.fnAllowTopologyUnload();},true);}
function initMainNavigationTooltips(){var host=document.querySelector('.fn-main-menu');if(!host||document.querySelector('.fn-nav-hover-card'))return;var card=document.createElement('div');card.className='fn-node-hover-card fn-nav-hover-card';card.setAttribute('role','tooltip');card.hidden=true;card.innerHTML='<strong></strong><p></p>';document.body.appendChild(card);function german(){return(document.documentElement.lang||lang||'en').toLowerCase().indexOf('de')===0;}function supported(link){return link&&link.matches('a.fn-main-nav-item[href^="/topology/"],a.fn-main-nav-item[href^="/devices/"],a.fn-main-nav-item[href^="/admin/"]');}function details(link){var path=link.getAttribute('href')||'';if(path.indexOf('/topology/')===0)return german()?'Öffnet die Netzwerktopologie mit Discovery-, Such- und Darstellungsfunktionen.':'Opens the network topology with discovery, search, and visualization tools.';if(path.indexOf('/devices/')===0)return german()?'Öffnet die Liste der verwalteten und erkannten Netzwerkgeräte.':'Opens the list of managed and discovered network devices.';return german()?'Öffnet die Verwaltung für Zugangsdaten, Updates, System, Discovery und Design.':'Opens administration for credentials, updates, system, discovery, and design.';}function place(x,y){if(card.hidden)return;var width=card.offsetWidth,height=card.offsetHeight;card.style.left=Math.max(8,Math.min(innerWidth-width-8,x+14))+'px';card.style.top=Math.max(8,Math.min(innerHeight-height-8,y+14))+'px';}function show(link,x,y){if(!supported(link))return;var title=link.getAttribute('title')||link.getAttribute('aria-label')||'';if(link.hasAttribute('title')){link.dataset.fnNativeTitle=link.getAttribute('title');link.removeAttribute('title');}card.querySelector('strong').textContent=title;card.querySelector('p').textContent=details(link);card.hidden=false;place(x,y);}host.addEventListener('pointerover',function(event){var link=event.target.closest&&event.target.closest('a.fn-main-nav-item');if(supported(link))show(link,event.clientX,event.clientY);});host.addEventListener('pointermove',function(event){if(!card.hidden)place(event.clientX,event.clientY);});host.addEventListener('pointerout',function(event){var link=event.target.closest&&event.target.closest('a.fn-main-nav-item');if(supported(link)&&(!event.relatedTarget||!link.contains(event.relatedTarget)))card.hidden=true;});host.addEventListener('focusin',function(event){var link=event.target.closest&&event.target.closest('a.fn-main-nav-item');if(!supported(link))return;var rect=link.getBoundingClientRect();show(link,rect.right,rect.bottom);});host.addEventListener('focusout',function(event){if(supported(event.target))card.hidden=true;});}
[enhanceNav,initMainMenu,initMainNavigationTooltips,initUserProfileNavigation,initUserAccountMenu,enhanceAuthControls,useInformalGerman,translateStatic,translateExtended,observeTopology,initNodeHoverCard,enhanceTopologyZoom,arrangeTopologyControls,relocateTopologyLegends,relocateTopologyDiscovery,relocateTopologyFilters,initTopologySidebarCollapse,initConnectionsCollapse,guardInvalidNodeContextMenu,initNodeContextMenu,initManualTopologyLinks,initManualLinkMenuVisibility,initTopologySearchAndGroups,initTopologyDirtyTracking,initTopologyControlTooltips,initTopologyNotices,initDiscoveryProgress,initDiscoveryStepDetails,initAdminTabs,integrateAdminSystem,initWebviewAdmin,initWebviewDeviceAssignments,initToolbarAdmin,initUpdateAdmin,initDiscoveryAdmin,labelAdminTabs,polishToolbarAdmin,initAdminGroups,removeLegacyAdminDesign,relocateAdminActionAlert,polishAdminDescriptionFields,initAdminImageAvailability,initAdminManagedDeviceTables,restoreScannedHostKeySelection,initCredentialDefaults,initAdminLocalization,initCredentialEditors,initAdminFrameSizing,translateBulkAdmin,translateAdminEnhancements,polishGermanAdmin,translateUpdateAdmin,translateUpdateSchedule,enhanceUpdateAdminUi,initUpdateInstallProgress,polishUpdateEnglish,initManualDevicePanel,initDeviceListActions,initDeviceSearch,initBuildNumber,restoreSshState].forEach(function(init){try{init();}catch(error){document.documentElement.setAttribute('data-fn-error',(document.documentElement.getAttribute('data-fn-error')||'')+init.name+':'+String(error&&error.message||error)+';');}});
function initCredentialDefaultsReliable(){if(location.pathname!=='/admin/')return;function enhance(doc,kind,defaultId,csrf){var editor=doc.getElementById(kind==='ssh'?'ssh-form':'web-form'),panel=editor&&editor.closest('section'),table=panel&&panel.querySelector('table');if(!table)return;var note=panel.querySelector('.secret-note');if(note&&!note.dataset.defaultNote){note.dataset.defaultNote='1';note.textContent+=' '+(lang==='de'?'Das Standardprofil wird erfolgreich per SNMP erkannten Geräten automatisch zugeordnet.':'The default profile is automatically assigned to devices successfully discovered through SNMP.');}table.querySelectorAll('tbody tr').forEach(function(row){var idInput=row.querySelector('input[name="credentialId"]'),cell=row.lastElementChild;if(!idInput||!cell||cell.querySelector('.fn-default-credential'))return;var id=idInput.value,isDefault=id===defaultId,form=doc.createElement('form');form.className='fn-default-credential';form.method='post';form.innerHTML='<input type="hidden" name="csrfToken"><input type="hidden" name="kind"><input type="hidden" name="credentialId"><button class="muted-button small" type="submit"></button>';form.elements.csrfToken.value=csrf;form.elements.kind.value=kind;form.elements.credentialId.value=isDefault?'':id;form.querySelector('button').textContent=isDefault?(lang==='de'?'Standard entfernen':'Clear default'):(lang==='de'?'Als Standard':'Make default');if(isDefault&&!row.querySelector('.fn-default-badge')){var badge=doc.createElement('span');badge.className='badge fn-default-badge';badge.textContent=lang==='de'?'Standard':'Default';row.firstElementChild.appendChild(doc.createTextNode(' '));row.firstElementChild.appendChild(badge);}form.onsubmit=function(event){event.preventDefault();var body=new URLSearchParams(new FormData(form));fetch('/admin/credential-defaults.jsp',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'},body:body.toString()}).then(function(response){if(!response.ok)throw new Error();window.location.reload();}).catch(function(){alert(lang==='de'?'Standardprofil konnte nicht gespeichert werden.':'The default profile could not be saved.');});};cell.insertBefore(form,cell.firstChild);});}fetch('/admin/credential-defaults.jsp',{credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(response){if(!response.ok)throw new Error();return response.json();}).then(function(settings){enhance(document,'ssh',settings.sshCredentialId||'',settings.csrf||'');var frame=document.querySelector('.fn-webview-admin-panel iframe');if(frame){var apply=function(){try{enhance(frame.contentDocument,'web',settings.webCredentialId||'',settings.csrf||'');}catch(ignored){}};frame.addEventListener('load',apply);apply();}}).catch(function(){});}
try{initCredentialDefaultsReliable();}catch(error){document.documentElement.setAttribute('data-fn-error',(document.documentElement.getAttribute('data-fn-error')||'')+'initCredentialDefaultsReliable:'+String(error&&error.message||error)+';');}
try{enforceRoleNavigation();}catch(error){document.documentElement.setAttribute('data-fn-error',(document.documentElement.getAttribute('data-fn-error')||'')+'enforceRoleNavigation:'+String(error&&error.message||error)+';');}
try{polishCredentialActions();removeDuplicateWebviewAssignmentColumn();}catch(error){document.documentElement.setAttribute('data-fn-error',(document.documentElement.getAttribute('data-fn-error')||'')+'polishCredentialActions:'+String(error&&error.message||error)+';');}
function revealShell(){document.documentElement.classList.remove('fn-shell-loading');}
if(location.pathname.indexOf('/profile/')===0&&document.documentElement.classList.contains('fn-shell-loading')){var revealProfileShell=function(){requestAnimationFrame(function(){requestAnimationFrame(revealShell);});};if(document.readyState==='complete')revealProfileShell();else window.addEventListener('load',revealProfileShell,{once:true});window.setTimeout(revealShell,1500);}else revealShell();
})();
(function(){if(location.pathname.indexOf('/admin/')!==0||document.querySelector('script[data-fn-offline-packages]'))return;var script=document.createElement('script');script.src='/assets/offline-packages.js?v=20260831-172-acli1';script.dataset.fnOfflinePackages='1';document.head.appendChild(script);}());
(function(){
 function send(active){if(window.parent===window)return;try{window.parent.postMessage({type:'fn-update-fullscreen',active:!!active},location.origin);}catch(ignore){}}
 if(location.pathname==='/admin/update.jsp'){
  function sync(){var progress=document.querySelector('.fn-install-progress'),confirm=document.querySelector('.fn-update-confirm'),active=!!((progress&&!progress.hidden)||(confirm&&!confirm.hidden));document.documentElement.classList.toggle('fn-update-child-fullscreen',active);send(active);}
  new MutationObserver(function(records){records.forEach(function(record){if(record.type==='childList'||record.attributeName==='hidden')sync();});}).observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden']});sync();
 }
 if(location.pathname==='/admin/')window.addEventListener('message',function(event){if(event.origin!==location.origin||!event.data||event.data.type!=='fn-update-fullscreen')return;var frame=document.querySelector('.fn-update-admin-panel .fn-admin-system-frame');if(!frame||event.source!==frame.contentWindow)return;document.documentElement.classList.toggle('fn-update-fullscreen',!!event.data.active);document.body.classList.toggle('fn-update-fullscreen',!!event.data.active);});
}());
(function(){
 if(location.pathname!=='/admin/'||document.querySelector('.fn-acli-admin-panel'))return;
 var nav=document.querySelector('.fn-admin-tabs'),grid=document.querySelector('main.page .grid'),discovery=nav&&nav.querySelector('[data-tab="discovery"]'),updates=nav&&nav.querySelector('[data-tab="updates"]'),system=nav&&nav.querySelector('[data-tab="system"]');if(!nav||!grid)return;
 var de=(document.documentElement.lang||'de').toLowerCase().indexOf('de')===0,tab=document.createElement('button');tab.type='button';tab.className='fn-admin-tab';tab.dataset.tab='acli';tab.setAttribute('role','tab');tab.id='fn-admin-tab-acli';tab.textContent='ACLI';nav.insertBefore(tab,discovery||updates||system||null);
 var panel=document.createElement('section');panel.className='fn-admin-tab-panel fn-admin-system-panel fn-acli-admin-panel';panel.hidden=true;panel.setAttribute('role','tabpanel');panel.setAttribute('aria-labelledby',tab.id);panel.innerHTML='<iframe class="fn-admin-system-frame" title="ACLI '+(de?'Einstellungen':'settings')+'" src="/admin/acli-settings.jsp"></iframe>';grid.appendChild(panel);
 var frame=panel.querySelector('iframe'),resizeObserver=null,resizeTimer=null;function fitAcliFrame(){window.clearTimeout(resizeTimer);resizeTimer=window.setTimeout(function(){try{var doc=frame.contentDocument;if(!doc||!doc.body)return;doc.documentElement.style.overflow='hidden';doc.body.style.overflow='hidden';var height=Math.max(doc.body.scrollHeight,doc.body.offsetHeight,doc.documentElement.scrollHeight,doc.documentElement.offsetHeight);if(height>0)frame.style.height=(height+8)+'px';}catch(ignored){}},30);}frame.addEventListener('load',function(){fitAcliFrame();window.setTimeout(fitAcliFrame,180);window.setTimeout(fitAcliFrame,700);try{var doc=frame.contentDocument;if(doc&&doc.body&&window.ResizeObserver){if(resizeObserver)resizeObserver.disconnect();resizeObserver=new ResizeObserver(fitAcliFrame);resizeObserver.observe(doc.body);resizeObserver.observe(doc.documentElement);}}catch(ignored){}});
 function show(){grid.querySelectorAll('.fn-admin-tab-panel').forEach(function(item){item.hidden=item!==panel;});nav.querySelectorAll('[role="tab"]').forEach(function(item){var active=item===tab;item.classList.toggle('active',active);item.setAttribute('aria-selected',active?'true':'false');item.tabIndex=active?0:-1;});fitAcliFrame();try{localStorage.setItem('fnAdminTab','acli');}catch(ignored){}}
 tab.addEventListener('click',show);nav.addEventListener('click',function(event){var other=event.target.closest('[role="tab"]');if(other&&other!==tab)panel.hidden=true;},true);try{if(localStorage.getItem('fnAdminTab')==='acli')show();}catch(ignored){}
}());
(function(){
 function addCreditsLink(){var panel=document.querySelector('.fn-account-panel');if(!panel||panel.querySelector('.fn-account-credits'))return false;var profile=panel.querySelector('.fn-account-profile'),credits=document.createElement('a');credits.href='/credits/';credits.className='fn-account-credits';credits.textContent='Credits';if(location.pathname.indexOf('/credits/')===0)credits.classList.add('active');if(profile)profile.insertAdjacentElement('afterend',credits);else panel.insertBefore(credits,panel.firstChild);return true;}
 if(addCreditsLink())return;var menu=document.querySelector('.fn-main-menu');if(!menu)return;var observer=new MutationObserver(function(){if(addCreditsLink())observer.disconnect();});observer.observe(menu,{childList:true,subtree:true});window.setTimeout(function(){observer.disconnect();addCreditsLink();},4500);
}());
